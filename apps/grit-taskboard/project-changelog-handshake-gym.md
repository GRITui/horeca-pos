# Project Changelog Handshake — Personal Gym Trainer (squad: gym)

## System Metadata
- Target Goal: **Sidekick** (formerly "Freelanz Gym") — this fork, originally a dedicated single-purpose personal-gym-trainer app, has been **rebranded and repositioned as the flagship general-freelancer app**, replacing the old multi-persona "Freelanz" app. Persona-specific copy/seeding stays gym-flavored for now (a later pass may generalize it further); the identity/branding layer is fully general-purpose as of the rebrand.
- Squad / trunk branch: `personal-gym-trainer-freelanz` (this squad's own trunk, kept alive per explicit user instruction — "Keep this fork" — even after its content is promoted to become `main`, see the rebrand Milestone entry below). Feature work happens on topic branches off this trunk, landed via PR — never pushed to the trunk directly.
- Fork point: `c955d3c` (general Freelanz app, "Add Quote → Invoice conversion"). Feature-drift tracking below predates the rebrand/promotion and is kept for history.
- Co-hosting: this app is deployed to the same GitHub Pages site as the general app, at `/gym/` instead of `/`. Both are built from a single combined workflow (`.github/workflows/deploy-pages.yml`, kept in sync across both squads' trunks) that checks out both branches on every run so a push to either can never wipe out the other's live pages. **Post-promotion, `/` also serves this squad's (Sidekick's) content** — `main` was promoted from this branch — while `/gym/` continues to exist as its own, currently-identical, branch per "Keep this fork."
- Storage isolation: because both apps share one origin, this app's IndexedDB/localStorage/sessionStorage/Cache Storage keys were namespaced (`freelanz-gym-v1` DB, `gym_`-prefixed storage keys, `freelanz-gym-shell-*` cache) so the two apps' local data could never collide. **Renamed as part of the Sidekick rebrand** to `sidekick-v1` DB / `sidekick_`-prefixed storage keys / `sidekick-shell-*` cache, with a one-time automatic migration (`migrateLegacyStorageIfNeeded()`) copying every store + relevant localStorage key forward on first load so existing installs lose no local data — see the rebrand Milestone entry for detail. `DB_VER` currently **7** (`memberTags` store added, then `usageEvents`; `memberTags`'s feature was later retired — its creation was removed from `onupgradeneeded` without a version bump, since new installs simply never get the store and existing installs' now-unused store is harmless; 6→7 added `packages`/`progressLogs`).
- Identity: **Sidekick** (rebranded from "Freelanz Gym") — new teal/marigold "S"-mark icon, tagline "Get booked. Get hired. Get paid.", title/manifest/sidebar all updated. See the rebrand Milestone entry for the full naming rationale (locked persona rule: Sidekick is a support role that acts on the user's input, never marketed as an autonomous assistant).
- Live URL: `https://gritui.github.io/Sidekickz/gym/` (requires the `github-pages` environment's deployment-branch policy to allow this branch — a one-time manual repo-settings step, already done). Post-promotion, the flagship app is also live at `https://gritui.github.io/Sidekickz/`. **GitHub Pages is the sole canonical host** — the previously-prepped Hostinger deploy plan (`.htaccess`, subpath-mount verification) was formally dropped 2026-07-11 per user decision; `app/.htaccess` removed as dead config. **Repo renamed 2026-07-11**: `GRITui/FreeLanz` → `GRITui/Sidekickz` (GitHub-side rename; `origin` remote repointed accordingly, Pages URL updated to match).
- Last Sync Timestamp: 2026-07-17
- App version: **0.9.37** (`APP_VERSION`/`SW_VERSION`/`?v=` kept in lockstep across `app.js`/`sw.js`/`index.html`/`login.html`/`book.html`).

## Execution Mode Policy (applies repo-wide, both squads — set 2026-07-10)
- **Autonomous, PR-only.** Work proceeds without stopping to ask permission for each step, but git changes only ever land as an **opened pull request** — never a direct push to a trunk branch (`main` for the general-app squad, `personal-gym-trainer-freelanz` for this squad).
- **User merges.** PRs are never self-merged going forward; the user reviews and merges themselves. (A prior one-off self-merge of PR #4 predates this policy and was done under explicit one-time authorization — not a standing pattern.)
- **Handshake files: one per squad.** This file tracks the gym squad only; the general app's `project-changelog-handshake.md` tracks that squad only. Don't cross-pollinate state between them.
- Open precision question flagged back to the user: "no push" is being read as *no push to a protected trunk branch* — pushing a topic/feature branch (required to open any PR at all) is assumed still fine. Flagging this reading explicitly in case it's not what was intended.

## Milestones
- [x] **Persona strip** — removed the onboarding/Settings work-type picker, `PERSONAS`/`UNIT_WORD`/custom-work-type escape hatch, and all non-gym `CUSTOMER_INTAKE`/`SEED_SERVICES`/i18n entries; consolidated the `@gym` i18n suffix keys into base keys since only one persona now exists. Boot auto-sets `workType='gym'`. **DONE + browser-verified** (commit `848c4e8`).
- [x] **Ship as a separate live app** — combined-build Pages workflow (`/` = general app, `/gym/` = this squad), storage-namespace isolation (DB/localStorage/sessionStorage/cache), distinct app identity (title/manifest/branding). **DONE + live-verified** — deploy ran green end-to-end on GitHub Actions. (commit `fee0028`)
- [x] **Comparison/regression testing before next version** — full local regression of this app (18/18 checks, 0 console errors) across sessions/customers/services/invoices/tax/bookings/followups/portfolio/research/backup-roundtrip, plus a smoke-test comparison against the general app to catalog feature drift since the fork point. **DONE.**
- [x] **Member tags** (merged, PR #8) — the session form's free-text "Member" field auto-saves each new name as a reusable, id-backed tag (datalist autocomplete on the form; `memberTags` IndexedDB store, `DB_VER` 4→5). A Settings ▸ Member tags screen lists tags with session counts, supports manual add, **rename with propagation** (renaming a tag rewrites every past session's `client` text), and delete (unlinks `memberTagId` from past sessions without touching their stored member name). **DONE + live-verified** (9/9 automated checks, 0 console errors).
- [x] **Pipeline / engagement board — backported + promoted to the main nav** (branch `gym/pipeline`) — ported the general app's Kanban engagement-pipeline feature: each session is now an engagement with a stage (default order for this squad is **prepaid**: Invoice → Paid → Session, i.e. charge before training someone), a Kanban board with one-tap next-action buttons wired into invoices.js/docgen.js, a stage badge on the sessions list, and a Settings ▸ Workflow section (reorder + optional Quote stage — no business-model preset picker, unlike the general app, since this squad only ever has one persona). **Promoted Pipeline to the bottom nav**, swapping out the Docs slot (Docs is still fully reachable via a Settings ▸ Manage row, not removed). No `DB_VER` bump needed — `stage`/`stageOrder`/`complete`/`invoiceId`/`quoteDocId` are just new optional fields on existing `jobs` records. Deliberately NOT ported: the general app's "save customer from session" banner (a separate M2.5 sub-feature, not required for Pipeline to function) — noted in Backlog below rather than silently dropped. **DONE + live-verified**: 13/13 new Pipeline checks (full lifecycle: new session → Invoice column → Send invoice → auto-advance to Paid → Mark paid → auto-advance to Session → Mark done → complete; reorder guard rejects Paid-before-Invoice; Quote stage toggle) plus all 18 gym-regression + 9 member-tags checks still pass unchanged (40/40 total across all three suites), 0 console errors. Merged PR #9.
- [x] **Booking day-view: jump to any date** (branch `gym/booking-date-picker`, PR #10, merged) — added a calendar-icon button next to the existing prev/today/next day-nav controls that opens a native date picker (`showPicker()`, with a `click()` fallback) so the user can jump straight to an arbitrary date instead of stepping one day at a time. Live-verified (2/2 checks), 0 console errors.
- [x] **Pipeline stage redesign: fixed 6-stage sequence** (branch `gym/pipeline-stages`, PR #11, merged) — replaced the prior "prepaid" 3-stage/optional-Quote model with the user's exact required sequence: **Pitch → Quote → Invoice → Paid → Delivery → Extend**, all six mandatory (Settings ▸ Workflow is now reorder-only, no add/remove toggle, still guarding that Paid can never precede Invoice). Legacy `stage:'service'` records fall back gracefully via existing `jobStage()` logic. Live-verified (14/14 new checks), all prior suites still pass, 0 console errors.
- [x] **Customer database: unique Member ID** (branch `gym/customer-id`, PR #12, merged) — added a human-visible, stable, sequential Member ID (`M-0001`, `M-0002`, …, never reused) to every customer record, shown as a read-only field in the customer modal and in the customer list subtitle, plus the first column of the customers CSV export. Existing customers backfilled in creation order on load. Live-verified (7/7 new checks), 0 console errors.
- [x] **Remove Sessions screen; Home replaces quick actions with "Pipeline at a glance"** (branch `gym/home-pipeline-summary`) — assessed and recommended: Pipeline's Kanban board already gives full CRUD over the same underlying job records the old chronological Sessions list showed, making Sessions redundant (Booking is a distinct calendar/scheduling concern and was left untouched). Removed the Sessions bottom-nav item, its screen, `renderJobs()`, the dead `newInvoice()` quick-action handler, and now-orphaned i18n keys. Home's "New session"/"New invoice" quick-action buttons were replaced with a **Pipeline-at-a-glance strip**: six clickable pills (one per stage) showing live open-engagement counts, each jumping straight into the Pipeline board on tap. `APP_VERSION`/`SW_VERSION` bumped 0.8.2 → 0.8.3. Live-verified: 23/23 gym-regression + 9/9 member-tags + 14/14 pipeline-stages + 7/7 customer-id (53/53 total across all four suites), 0 console errors. Merged PR #13.
- [x] **Session form cleanup + client management promoted to main nav** (branch `gym/client-mgmt-and-form-cleanup`) — three changes: (1) removed the Add Session form's start-time/end-time fields and the duration display that depended on them (`calcDuration()`/`sessionHours()`/`fmtHours()`/`inferEndDate()` all removed as dead code); (2) the session form's Client dropdown now has a **"+ Add a new client…"** option that opens the client modal stacked on top of the still-open session form — saving the new client auto-selects it back in the dropdown and fills the Member field, no need to leave the form; (3) **Customers promoted from a buried Settings row to a main bottom-nav tab** ("Clients", between Pipeline and Book), and all user-facing "Customer" strings renamed to "Client" to match trainer terminology (internal ids/functions/IndexedDB store names unchanged). `APP_VERSION`/`SW_VERSION` bumped 0.8.3 → 0.8.4. Live-verified: 30/30 gym-regression (7 new checks) + 9/9 member-tags + 14/14 pipeline-stages + 7/7 customer-id (60/60 total), 0 console errors.
- [x] **Local usage insights (no backend)** — asked the user to scope this before building: local on-device insights vs. cross-device telemetry to a backend (a much bigger project — new infra, hosting, consent flow). User chose **local, on-device only**. Added a `usageEvents` IndexedDB store (`DB_VER` 5→6) and a `logEvent()` helper fired at natural touchpoints — every screen visit, a session logged, a client added, each Pipeline stage a job enters, a backup export — all captured via a single fire-and-forget call, nothing ever leaves the device (no network calls at all). A new Settings ▸ **Insights** screen (`renderInsights()`) shows: sessions logged, clients added, active days in the last 30, a ranked "Feature usage" list (which screens get opened most — the direct signal for what to build next), a "Pipeline activity" breakdown (which stage jobs pass through most), and a "Clear usage data" reset button. Deliberately **not** added to the JSON backup/export set — this is diagnostic data about a specific device's usage, not business data worth migrating between devices. `APP_VERSION`/`SW_VERSION` bumped 0.8.4 → 0.8.5. Live-verified: 8/8 new Insights checks + 30/30 gym-regression + 9/9 member-tags + 14/14 pipeline-stages + 7/7 customer-id (68/68 total), 0 console errors. (One test-script fragility was caught and fixed along the way, not an app bug: since Insights' feature-usage list can render a screen's label — e.g. "Member tags" — as plain text once that screen has been visited, an older test's generic `text=Member tags` selector started matching two places at once; scoped it to `.settings-row:has-text(...)`.)
- [x] **Pipeline: skip Quote/Invoice, and a distinct "Finished" outcome for Extend** — assessed and recommended before building: Quote and Invoice currently *block* progress (a card can't leave those columns without opening the linked docgen/invoice flow), which is a real gap for engagements that don't need formal paperwork; Paid should stay mandatory since it's the revenue-tracking checkpoint Home's earnings are built on. Recommended a per-card **"Skip"** action on Quote/Invoice only, instead of reviving the old global Quote on/off toggle removed in the stage redesign (PR #11) — user agreed, plus asked for the Extend stage to support marking an engagement **"Finished"** (ended, no renewal) as a distinct outcome from "Extended" (renewed). Built: `STAGE_META.quote`/`.invoice` flagged `skippable`; a new `skipJobStage()` (reuses `advanceJobStage()`'s existing "just move forward, no linked record" mechanics — no new state machine needed) wired to a secondary "Skip" button, never shown on Paid; a new `finishJobStage()` sets `job.outcome = 'finished'` (vs `'extended'` stamped by the normal complete-via-`advanceJobStage()` path) so the completed badge and the Insights pipeline-activity breakdown can tell the two outcomes apart. `moveJobStageBack()` clears `outcome` when un-completing a card. `APP_VERSION`/`SW_VERSION` bumped 0.8.5 → 0.8.6. Live-verified: 13/13 new checks (skip on Quote/Invoice, no skip on Paid, Finished vs Extended badges, Insights breakdown) + all 68 prior checks still pass (81/81 total), 0 console errors.
- [x] **Insights hidden from end users, developer-only** — user clarified: Insights is a tool for the app owner to decide what to build next, not a feature end users should see or reach. Removed the visible "Insights" Settings row (kept the underlying screen and, critically, kept `logEvent()` collection running unconditionally — hiding the viewer doesn't mean losing the data). Reveal is now via the same pattern Android uses for Developer Options: tapping the Version number 7 times sets a persistent `settings.insightsUnlocked` flag (one-time, survives reload, per device), which shows the Settings row again. `switchScreen()` also bounces a direct `switchScreen('insights')` call back to Settings if the flag isn't set, as a second layer (not a real security boundary in a client-only app — just belt-and-suspenders against stumbling in). `APP_VERSION`/`SW_VERSION` bumped 0.8.6 → 0.8.7. Live-verified: 7/7 new checks (hidden by default, direct-navigation bounce, unlock-by-tapping, persists across reload) + all 81 prior checks still pass (88/88 total), 0 console errors.
- [x] **Pipeline board redesign: left-hand stage rail (no horizontal scroll); Home glance strip as a fixed grid; referable running numbers on Quote + a new Receipt doc type** — three related changes from one user request:
  1. **Pipeline board** — the old 6-column horizontal-scroll-snap Kanban (each column ~84vw, "too long" to swipe through on mobile) is replaced with a left-hand rail listing all 6 stages (icon/label/count) plus a single-column card list for whichever stage is currently selected — only one stage's cards ever render at once, so there's nothing to horizontally scroll. Every stage-mutating function (`advanceJobStage`/`skipJobStage`/`finishJobStage`/`moveJobStageBack`/`markJobPaid`/the invoice+quote engagement-linking hooks) now also sets the active rail stage to wherever the card just landed, so the rail visibly "follows" the card the way the old side-by-side board did. New `openPipelineAt(stage)` lets Home's glance pills jump straight to a specific stage. Removed the now-dead `.kanban`/`.kb-col`/`.kb-col-head`/`.kb-col-body` CSS.
  2. **Home's "Pipeline at a glance"** — the pill row was itself a horizontal scroller (6 pills overflowing one line); changed to a fixed 3-column CSS grid (2 rows × 3 pills) so all 6 stages are visible with no scrolling at all.
  3. **Referable document numbers** — user pointed out Invoice's existing running-number scheme ("INV-2026-0001") is good and asked to reuse the same idea for Quote and Receipt. Extracted invoices.js's number generator into a shared `nextDocNumber(rows, prefix)` in app.js (invoices.js's own `nextNumber()` is now a one-line wrapper around it); docgen.js's Quote type now gets a `QUO-2026-0001`-style number on save (immutable on edit, shown read-only in the form, in the saved-documents list, and baked into the printed/viewed content), and a brand-new **Receipt** document type was added (amount received, payment method, payment date, optional reference/notes) with its own `REC-2026-0001` sequence — Contract/NDA deliberately don't get one (not the kind of document a client "references" later).
  `APP_VERSION`/`SW_VERSION` bumped 0.8.7 → 0.8.8. Live-verified: 20/20 pipeline-stages (rewritten for the rail) + 16/16 pipeline-skip (rewritten for the rail) + 11/11 new doc-numbering checks + 30/30 gym-regression + 9/9 member-tags + 7/7 customer-id + 8/8 insights + 7/7 insights-hidden (108/108 total across 8 suites), 0 console errors.
- [x] **Merge Member Tags into Client; optional seller + client business info on all documents** (branch `gym/merge-client-tags-and-doc-business-info`) — two related changes from one user request:
  1. **Client/Member Tags merge** — assessed the two parallel "who" identifiers a session could carry (`clientId` from the Client dropdown vs. a free-text Member field that auto-created its own lightweight tag record) and recommended retiring Member Tags now that "+ Add a new client" inline creation removes the friction that originally justified a lighter-weight alternative to full Customer records. User approved ("go ahead"). Removed the Member Tags feature entirely (Settings screen, modal, datalist, `upsertMemberTagIfNeeded`/`renderMemberTags`/etc., the `memberTags` object store's creation on new installs — existing installs' now-orphaned store is left dormant rather than forcing a `DB_VER` bump to drop it) and removed the session form's free-text Member field, leaving the Client dropdown as the single required "who" input (`saveJob()` now blocks with a field error if no client is selected, and always derives the stored `client` name from the selected Client record rather than accepting independently-typed text). `BACKUP_STORES` had `'memberTags'` removed — left in, this would have thrown on every export/restore for new installs that never create the store.
  2. **Optional business info on documents** — per user instruction ("both service seller + client name/tax id/address on doc, but don't make it mandatory, just auto-apply if user input them in their own profile and client profile"), added a new Settings ▸ "Business info (optional)" section (business name, tax ID, address) feeding a shared `sellerBusinessName()` helper (falls back to the account display name if left blank); `docgen.js` now renders both a seller info line and a client info line (address/tax ID) on Quote and Receipt documents whenever the corresponding fields are filled in (Contract already had client-side rendering; Invoice already had client-side auto-fill from the selected customer). Nothing is required — a document with no business info filled in renders exactly as before.
  `APP_VERSION`/`SW_VERSION` bumped 0.8.8 → 0.8.9. Live-verified: 8/8 new client-merge checks + 7/7 new business-info checks + 30/30 gym-regression (rewritten to drive the Client dropdown instead of the removed Member field) + 20/20 pipeline-stages + 16/16 pipeline-skip + 8/8 insights + 7/7 customer-id + 11/11 doc-numbering (117/117 total across 8 suites), 0 console errors. `check-member-tags.js` retired from the active suite rotation (tested a feature that no longer exists).
- [x] **Booking redesigned as a full month calendar** (branch `gym/booking-month-calendar`) — per user request (a screenshot of the old day-view's "No bookings for this day" empty state, asking to "leverage to display full month calendar with some legend to let user know if some pipeline activity mark on that day... make it minimal but fit to screen size... when user click calendar could expand to see more detail"): replaced the single-day agenda landing view with a month grid (Mon–Sun, current + adjacent-month padding days dimmed). Each cell shows a small legend dot per distinct **pipeline stage** present that day (colored per-stage, matching the Pipeline board — `pitch`/`quote`/`invoice`/`paid`/`delivery`/`extend` each got a `dot` color added to `STAGE_META`, the authoritative stage registry, so any future screen can reuse the same palette) plus a neutral gray dot if a booking is on that day. Multiple engagements on the same day in the same stage still render as a single dot (deduped by stage, not by job count) — the exact list surfaces when the day is expanded, not in the tiny grid cell. The legend itself is scoped to the currently-visible month grid (including its leading/trailing padding days) and only lists what's actually marked there — an empty month shows no legend at all, and a month with only bookings never lists the 6 stage names it has no need to explain. Tapping a cell expands an inline agenda panel directly beneath the grid, now showing **both** domains when relevant: a "Pipeline" section (client name + current stage + amount, tapping a row jumps to that stage on the Pipeline board via `openPipelineAt()`) and a "Bookings" section (the existing `buildDayList`/`rowHtml`/`stripHtml` day-agenda rendering, unchanged, including the cross-midnight travel-buffer warning) — sub-headers only appear when both are present on the same day. Tapping the same cell again collapses it; today's cell is expanded by default on first load. Assessed whether to allow adding a booking from the expanded view — **yes**: the existing "+ New booking" button targets whichever date is expanded (its label shows the date). Month navigation (‹ ›, "Today") replaces the old day-stepping nav; tapping the month label opens a native date picker to jump straight to a distant date (preserving the old jump-to-date capability, `gym/booking-date-picker` PR #10 — its regression test `check-booking-date.js` is superseded and retired). Two small polish requests handled in the same pass: replaced the 📅 emoji (row icon + empty-state icon) with the same outlined SVG calendar icon already used by the nav's Book tab, and the legend labels now read as plain stage/type names ("Pitch", "Booking", etc.) rather than an invented phrase like "Pipeline activity". `APP_VERSION`/`SW_VERSION` bumped 0.8.9 → 0.9.0. Live-verified: 28/28 `check-booking-calendar.js` (grid rendering, scoped legend, per-stage dot dedup, day-panel pipeline section with clickable jump-to-stage, expand/collapse, month nav, jump-to-date, adjacent-month cell click) + 1/1 `check-calendar-midnight.js` + all 117 prior checks across 8 suites still pass (146/146 total), 0 console errors, verified at both desktop and mobile (390px) viewport widths with no horizontal scroll.

  **Follow-up** — per user request, replaced the "2 same-stage engagements → still just 1 dot" dedup behavior with a numeric **count badge** (a small pill, `.cal-count`) whenever 2+ pipeline engagements land on the same day: single-color if they're all the same stage, brand-teal if the day mixes stages (a single dot/badge can't represent more than one color, and the exact per-engagement breakdown is one tap away in the day panel regardless). Sized to stay a true circle for 1-digit counts and widen into a pill for 2 digits without breaking the grid (`min-width` + `border-radius: height/2`, the same technique already used by the Pipeline rail's stage-count badge) — verified with a 12-engagement same-day case seeded directly via IndexedDB (12 UI-created engagements would have been slow to seed through the form). Live-verified: 31/31 `check-booking-calendar.js` (3 new checks: badge replaces dots at 2+, reads "2", 2-digit "12" case stays inside the cell) + all prior checks unchanged (149/149 total), 0 console errors.

  Also assessed, not yet built (flagged back to the user rather than assumed): (a) whether any of the 6 stage names could be more precise — e.g. "Delivery" reads a bit logistics/shipping-flavored for a personal-training context (a gym-specific term like "Sessions" might fit this squad better, though "Delivery" stays more portable if this pipeline model is ever shared back with the general multi-persona app), and "Extend" is serviceable but "Renewal" is the more standard CRM/subscription term for the same "offer to continue the engagement" action — renaming a *label* (display text only, not the internal `stage` id persisted on existing jobs) would be a low-risk cosmetic change if wanted; (b) whether to add Week/Work-week/Quarter/Year view modes alongside Month — recommended Month (shipped) + a Week view (actual hour-slot grid, useful for spotting schedule gaps/conflicts a dot can't show; untimed pipeline engagements would render as small all-day chips above the hour grid, similar to Google Calendar's all-day row) as the two genuinely useful modes for a solo operator, and recommended against Work-week/Quarter/Year as added view-switcher complexity with little day-to-day value for a single person's own schedule (those patterns exist mainly to compare *other people's* schedules in team calendar tools).

- [x] **Collapsible Settings sections** (branch `gym/collapsible-settings`) — per user request for a cleaner look on the increasingly long More/Settings screen, wrapped each section (Manage, Workflow, Preferences, Business info, Tax defaults, Data) in a native `<details>`/`<summary>` element — zero JS needed for the collapse mechanics themselves, browsers handle it natively — with a CSS-only chevron (rotates via `details[open]`, no image asset) replacing the default disclosure marker. **Deliberately defaulted every section to `open`**, i.e. visually and functionally identical to before this change, rather than starting anything collapsed: an audit of the existing regression suites found 6 of 8 actively fill/click fields inside what would become the Workflow/Preferences/Business-info/Tax-defaults/Data sections (e.g. `regress-gym.js` clicks "Backup JSON", `check-pipeline-stages.js` drives the Workflow reorder buttons, `check-business-info.js` fills the seller-info fields) — defaulting those closed would have broken all of them for a purely cosmetic change. The Account row, the Total-jobs/Version row, and Log out stay outside any `<details>` — always visible, no reason to hide identity info or the developer-unlock tap target. `APP_VERSION`/`SW_VERSION` bumped 0.8.9 → 0.9.0. Live-verified: 8/8 new `check-collapsible-settings.js` (6 sections render, all open by default confirming no regression, one section collapses/re-expands independently of the others, previously-passing field interactions remain unaffected) + all 106 prior checks unchanged (114/114 total), 0 console errors.

- [x] **Payment channels: generalized "PromptPay ID" into a saved list of payment methods** (branch `gym/payment-channels`) — per user instruction to go with "the actual 'payment channel' generalization: a method selector (PromptPay / Bank transfer / Cash / Other) where only PromptPay gets a QR and the rest are just plain reference text," plus "allow to save multiple payment channel too": replaced the single `settings.promptpayId` field with `settings.paymentChannels` (an array of `{id, type, label, detail}`), each type registered once in `PAYMENT_CHANNEL_TYPES`/`PAYMENT_CHANNEL_ICONS` (app.js) — `promptpay` (QR), `bank`, `cash`, `other` (all three of the latter render as plain reference text, never a QR). A new Settings ▸ "Payment channels" section (list + add/edit/delete modal, `#modal-paychannel`) replaces the old single input. Existing installs migrate once, automatically, in `enterApp()`: a non-empty legacy `promptpayId` becomes the first saved channel. Invoices snapshot the *whole* channel list at issue time (JSON-deep-copy, same pattern the old code used for the single `promptpayId` snapshot) so a later Settings change never rewrites what a client already saw on an issued invoice; `invoicePaymentChannels()` falls back live-settings → legacy single-field synthesis for invoices saved before this feature existed, so nothing already issued goes payment-method-blank. Every configured channel renders on an invoice (no per-invoice picker — showing all of them lets the client pick whichever is convenient, matching how printed invoices commonly list several payment options at once). **Also fixed a pre-existing gap while in there**: the printed/PDF invoice previously showed no payment information at all (only the live in-app detail modal drew the QR onto a visible canvas) — `printInvoice()` now renders the same channel list, converting the PromptPay QR to a `canvas.toDataURL()` data-URI `<img>` so it survives into the print output. `APP_VERSION`/`SW_VERSION` bumped 0.8.9 → 0.9.0 (branched directly off trunk, independent of the still-open `gym/booking-month-calendar` PR #19). Live-verified: 9/9 new `check-payment-channels.js` (old field gone, add/edit/delete PromptPay + bank channels, both render on an invoice — QR canvas for PromptPay, reference text for bank) + all 106 prior checks across 7 calendar-independent suites still pass (115/115 total), 0 console errors. Print-output QR embedding verified by code review (reuses the same tested `invoicePaymentChannels()`/`normalizePromptPay()`/`buildPromptPayPayload()`/`qrGenerate()`/`drawQr()` chain as the already-tested live view) rather than by automated test — headless Chromium's `window.print()` opens a real OS print flow that can't be safely stubbed/driven from Playwright.

  Also assessed per the same message, not yet built (flagged back to the user): (1) a **shareable invoice link** the client can open to attach proof-of-payment themselves, with a fallback for the trainer to attach it manually at the "Paid" pipeline stage if the client doesn't — this is a materially bigger feature (needs *some* way for an anonymous client link to read/write one invoice's data despite the app being local-first/no-backend today, e.g. a signed export blob + a tiny serverless endpoint, or an explicit "this needs a backend" conversation) rather than a quick addition; (2) **service package/bundle deduction** for gym multi-session packages (e.g. "buy 10, track remaining") — how to decrement remaining sessions on delivery, surface remaining balance on the client card, and handle the "did they rebook or not" follow-up gap — overlaps with the already-backlogged "Session package tracking" item; (3) a **notifications feature** for pipeline/payment/proof-of-payment events — needs a delivery mechanism decision first (in-app only vs. push, which requires a backend) before a design is worth committing to.

- [x] **Notifications: in-app Action Queue + app-triggered OS notifications** (branch `gym/notifications`) — per user request for a notifications feature covering pipeline/payment events, with a follow-up question asked back: real server-triggered push (fires even after the app's been closed for days, e.g. an invoice going overdue overnight) needs data synced to a small backend plus a scheduled job — a standing infrastructure commitment — versus app-triggered push (fires only while the tab is open or freshly backgrounded, no backend, ships today). **User chose app-triggered now, server-triggered noted as the next milestone.** Built: `computeNotificationConditions()` (app.js) is the single source of truth for three conditions — overdue invoices (status ≠ paid, past `dueDate`), bookings starting within 60 minutes, and pipeline engagements with no stage movement (`updatedAt`) for 3+ days — used by both Home's Action Queue (now a *list* of independent nudges rather than one mutually-exclusive slot with the backup reminder, so several can show at once) and a new OS-level notification checker. A Settings ▸ "Notifications" toggle requests `Notification` permission on enable; `checkAndFireNotifications()` runs on every `reload()` (data change) and every 60s thereafter via `setInterval`, calling `registration.showNotification()` through the service worker so it works even while the tab is merely backgrounded, not just focused. Dedup is a `settings.notifiedIds` map keyed per-condition (`inv:<id>`, `bk:<id>`, `job:<id>:<stage>`) that's rebuilt from scratch every check — any key no longer present in the current condition set is dropped, so a resolved condition (invoice paid, booking passed, stage advanced) naturally re-arms for the next time the same kind of event happens, without a stale "already notified, never again" flag lingering forever. `sw.js` gained a `notificationclick` handler (focus an existing tab, else open one) — standard PWA click behavior. `APP_VERSION`/`SW_VERSION` bumped 0.8.9 → 0.9.0. Live-verified: 8/8 new `check-notifications.js` (toggle grants permission, queue shows all three conditions simultaneously alongside the backup reminder, tapping a queue row navigates to the right screen) + all 106 prior calendar-and-payment-channel-independent checks still pass (114/114 total), 0 console errors. Branched directly off trunk, independent of the two other still-open PRs (#19 calendar, #20 payment channels).

  **Follow-up in the same PR** — per user request, moved the backup reminder off Home entirely and into More/Settings (`renderBackupReminder()`, a new `#backup-reminder-body` container right above the "Manage" section, next to the Backup JSON/Restore JSON actions it's nudging toward) so it no longer competes with the pipeline/payment nudges for attention on Home. Added a small dot on the More nav icon (`updateMoreNavBadge()`, reusing the previously-unused `.nav-badge` CSS class already in the stylesheet) so a due reminder stays discoverable without opening Settings. Live-verified: 6/6 new `check-backup-relocation.js` (no longer on Home, appears in More, nav badge shows/clears correctly, backing up clears both) + all 120 prior checks unchanged, 0 console errors.

- [x] **Four queued housekeeping items, picked up together** (branch `gym/calendar-rename-and-docs-reorg`) — per standing user request to work through the backlog flagged after the "Merge and ship" round:
  1. **Renamed the Booking nav tab/screen to "Calendar"** — user-facing labels only (`nav_book`/`book_title` i18n strings, nav span, screen `<h1>`); the underlying booking *entity* (appointments, `bookings` store, `openBookingForm()`, etc.) keeps its existing name throughout, since only the screen identity was in scope.
  2. **Trimmed the More/Settings "Manage" list** — split it into two collapsible sections: "Manage" (Services, Invoices, Docs — kept open by default, the daily-use items) and a new "More tools" section (Follow-ups, Portfolio, Research, the hidden dev-only Insights row — collapsed by default) to cut the number of always-visible rows.
  3. **Folded Tax into Documents** — removed Tax's standalone Manage row; the WHT/VAT calculator (unchanged, still owned by `tax.js`) is now reached via a new "Tools ▸ Tax calculator" tile on the Documents screen, with a back arrow on the Tax screen returning to Docs instead of relying on the bottom nav.
  4. **Added an A4/A5 document page-size setting** — a Preferences ▸ "Document page size" select (`settings.docPageSize`, default A4) feeds a shared `docPageSizeCss()` helper (app.js) that both `invoices.js`'s and `docgen.js`'s print flows inject as a fresh `@page{ size; margin }` rule right before `window.print()`, so both invoices and generated documents (contracts/NDAs/quotes/receipts) honor the same page-size choice; A5 uses a tighter 10mm margin vs. A4's 16mm.

  `APP_VERSION`/`SW_VERSION` bumped 0.9.0 → 0.9.1. Updated `regress-gym.js`'s Tax/Follow-ups/Portfolio/Research navigation steps (now via Docs ▸ Tools and an expand-"More tools"-first click) and `check-collapsible-settings.js`'s section-count assertions (7 → 8 sections, one now collapsed by default) to match; all other active suites needed no changes. Live-verified: all active suites green post-rewrite, 0 console errors.

- [x] **Four more items from the same backlog batch** (branch `gym/incoming-pipeline-and-thai-menu`), per direct user instruction:
  1. **Home's Action Queue replaced by "Incoming pipeline"** — the nudge-style list (overdue invoices / imminent bookings / stale engagements) is gone from Home; in its place, `renderIncomingPipeline()` lists up to 6 active (non-complete) engagements sorted by stage then by how long they've sat untouched, each showing client/stage/amount and tapping straight into Pipeline at that stage — reusing the exact row style bookings.js's day panel already used for pipeline rows. `computeNotificationConditions()`/`checkAndFireNotifications()` (the OS-level notification checker) are untouched and still fire independently — only Home's own in-app view changed.
  2. **Calendar's "+ New booking" → "+ New session"** — the main button now opens the same job/session modal used by Home/Pipeline's FAB (`openAddJob()`, extended to take an optional date so it can pre-fill the tapped calendar day), since a calendar day's primary action is logging pipeline work, not scheduling a time slot. The original duration/travel-buffer booking form (a materially bigger feature with its own cross-midnight overlap warnings, still fully built and tested) stays reachable via a small secondary "+ Schedule a booking" link in the day panel, so nothing already built became dead/unreachable.
  3. **Calendar's day panel moved beside the grid on desktop** — a new `.cal-layout` two-column CSS grid (`@media (min-width:900px)`, following the same convention as Pipeline's existing desktop max-width rule) puts the month grid + legend + button on the left and the day panel on the right (`position:sticky`), so switching days no longer requires scrolling down on wide viewports; mobile is unaffected (the wrapper divs just stack as before). An empty-state placeholder ("Tap a day to see what's on it.") fills the right column when no day is expanded, so the layout never looks broken.
  4. **Thai language toggle** — a Preferences ▸ "Language" select (`settings.lang`, reusing i18n plumbing that already existed in `app.js` — `curLang()`/`t()`/`applyLang()` — but had no second locale until now) adds a full `I18N.th` dictionary covering the app's static chrome: nav, all Settings/More menu section titles and rows (including ones added since the original i18n pass — Docs/More tools/Document page size/Notifications/Payment channels/Export invoices CSV, newly wired with `data-i18n`), dashboard labels, form fields, and toast/validation messages. Screens whose content is built dynamically in JS without going through `t()` (docgen.js/invoices.js generated documents, bookings.js's day-panel text, tax.js) stay English — `t()` already falls back to `en` for any missing key, so nothing breaks either way; this was scoped as translating the static menu, not full deep localization.

  `APP_VERSION`/`SW_VERSION` bumped 0.9.1 → 0.9.2. Rewrote `regress-gym.js`'s and `check-doc-numbers.js`'s stale `.dg-tpl-btn:has-text("Tax calculator")` selector (latent breakage from the previous PR's `.dg-tool-btn` rename, caught here), `check-booking-calendar.js`/`check-calendar-midnight.js`'s booking-creation steps (now via the secondary schedule-a-booking link), and `check-backup-relocation.js`/`check-notifications.js`'s `#queue-body`→`#incoming-pipeline-body` id rename plus notifications' assertions (rewritten to check `computeNotificationConditions()` directly for the OS-notification path, since Home's own UI no longer surfaces invoice/booking nudges). New `check-incoming-pipeline-calendar-lang.js` (27/27). Live-verified: 198/198 checks across all active suites, 0 console errors.

- [x] **Backlog batch: session packages, progress log, quick check-in** (branch `gym/backlog-packages-progress-checkin`) — per user instruction to complete the remaining gym-fork backlog:
  1. **Session package tracking** — a new `packages` store (`{clientId, totalSessions, price, purchasedDate}`), one per purchase/renewal (history preserved, never overwritten). Remaining is always *computed live* from `jobs` (never stored/decremented, so it can't drift) via a new `jobDelivered(j)` helper — a session counts against a package once its stage reaches Delivery or later (or it's otherwise complete), matching the pipeline's own meaning of "delivered." `activePackageFor(clientId)` returns the most recent purchase that still has sessions left. Surfaced: a "X/Y left" badge on each Client-list row (`packageRemaining()`), a "Session package" section in the Customer modal (current status + progress bar + purchase history + a "+ New/Renew package" inline form), and an "Apply to package" checkbox on the Add/Edit Session form that appears only when the selected client has one (pre-fills the check when adding a new session, matches the session's own stored `packageId` on edit — a session already linked to a since-exhausted package still shows *that* package rather than silently switching).
  2. **Client progress log** — a new `progressLogs` store (`{clientId, date, weight, notes}` — kept intentionally simple: weight plus a free-text notes field for other measurements like chest/waist, rather than a rigid multi-field measurement schema). A "Progress log" section in the Customer modal lists entries newest-first with a computed delta vs. the previous entry (colored red/green), plus an inline "+ Add entry" form.
  3. **Quick session check-in** — a ⚡ button on every Client-list row (`quickCheckIn()`) logs a session dated today in one tap: reuses the client's most recently used service/amount, skips straight to the Delivery stage (bypassing Pitch/Quote/Invoice/Paid — a repeat visit isn't a new sale), and auto-applies their active package if they have one, since "did this client show up for their package session today" is exactly the moment this exists for.
  4. **"Save customer from session" banner — assessed, deliberately not built.** Confirmed (via direct code inspection) that this fork's job form has no state for the banner to act on: `saveJob()` hard-rejects if no real `clients` record is selected, and the only way to introduce a new client mid-form is the "+ Add a new client" flow, which creates a real record immediately — there's no free-text-name-without-a-record path left for a later "save this as a customer?" prompt to apply to. Building the banner would mean reintroducing that free-text path first, which is out of scope here.

  A package running out now also surfaces as a Follow-ups candidate (`followups.js`'s `buildQueue()`, group 3, key `pkg-empty:<clientId>:<packageId>`) — "ask about renewing" — using the same snooze/dismiss mechanism as its other candidate types, not a separately stored record. `DB_VER` bumped 6→7 (`packages`, `progressLogs`); both added to `BACKUP_STORES` so JSON export/import/restore round-trip them like every other store. `APP_VERSION`/`SW_VERSION` bumped 0.9.1 → 0.9.2 (this branch was independently built off the same trunk point as the "incoming pipeline / Thai menu" batch above — the version bump was merged, not stacked, on top of it).

- [x] **Rebrand to Sidekick + promote this fork to be the new `main`** (branch `rebrand-sidekick`) — per user instruction, uploading a naming/positioning brief ("2852e221-sidekickrebrandsummary.md") and asking to "Do rebrand ui/ux update / And merge this branch as main. Keep this fork." Clarified two decision points before building: (1) which codebase gets the rebrand — user chose this fork's tech stack as the whole app's codebase going forward, no longer gym-focused, since "Older main fork no longer fit to market"; (2) what happens to the two branches — user chose "Promote gym fork to be the new main."
  1. **Identity rename, with zero local-data loss.** `DB_NAME` (`freelanz-gym-v1` → `sidekick-v1`), `SESSION_KEY` (`freelanz_gym_uid` → `sidekick_uid`), and all `gym_`-prefixed localStorage keys (guest username/counter, UI lang/theme, post-login toast) renamed to `sidekick_`-prefixed. Because an IndexedDB rename normally orphans every existing user's data (opening a new name just gets a fresh empty DB), added `migrateLegacyStorageIfNeeded()`: on boot, if the legacy `freelanz-gym-v1` DB exists and a one-time `sidekick_migrated_from_freelanz_gym` flag isn't set, every store is copied into the new DB via `put()` (not `add()`, to preserve literal ids and cross-store references like `jobs.clientId` → `clients.id`), the 5 relevant localStorage keys are copied to their new names, and the flag is set so it never re-runs. `indexedDB.databases()` feature-detected (guarded, not universally supported) so this degrades to a no-op rather than crashing boot on older browsers. Backup-file `app` tag changed `'FreelanzGym'` → `'Sidekick'`, but the import validator accepts either value so a pre-rebrand backup file still restores correctly.
  2. **Naming/branding throughout**: `<title>`/manifest name/apple-mobile-web-app-title/`<h1>`/desktop sidebar wordmark (a CSS `content:` pseudo-element string, easy to miss with JS/HTML-only greps — caught via a final `--include="*.css"` sweep) all → "Sidekick"; new tagline "Get booked. Get hired. Get paid." (English + Thai) replaces "Freelance admin, handled."; new teal/marigold "S"-mark icon (rendered from a hand-authored SVG, PNGs regenerated via a Playwright/Chromium screenshot workaround since no SVG-to-PNG converter was installed); CSV export / backup filenames / error-toast copy / module header comments across `app.js`, `sw.js`, `bookings.js`, `docgen.js`, `followups.js`, `invoices.js`, `portfolio.js`, `research.js`, `tax.js`, `api/draft-followup.js`, `dev-server.js` all updated; `.github/workflows/deploy-pages.yml` comments updated to describe the new `main` (Sidekick) / `/gym/` (kept fork) relationship — no functional branch-ref changes.
  3. **Deliberately not done this pass** (flagged, not silently dropped): domain/social-handle/trademark verification for "Sidekick" and a Thai transliteration check — both noted as outstanding in the source brief, unrelated to code.
  `APP_VERSION`/`SW_VERSION` bumped 0.9.2 → 0.9.3. Live-verified: 13/13 new `check-sidekick-rebrand.js` (identity strings, DB name, sidebar wordmark, backup format + old-format-still-restores) + 13/13 new `check-sidekick-migration.js` (a from-scratch simulated legacy install — seeded a real `freelanz-gym-v1` DB + old localStorage keys in a fresh browser context, then loaded the rebranded app for the first time — verifying full migration correctness, id/cross-reference preservation, and idempotency on reload) + all 9 existing regression suites re-run and still passing with zero regressions from the rename (one stale test assertion in `regress-gym.js` updated to expect the new `'Sidekick'` backup tag — an expected test-staleness fix, not an app bug), 0 console errors.

- [x] **Drop M-AI (AI-drafted follow-up messages) entirely** (branch `remove-m-ai-feature`) — per explicit user decision on the post-promotion backlog review (Drop, not pause): removed the whole feature rather than leaving it dormant. Deleted `api/draft-followup.js`, `vercel.json`, `dev-server.js` (its only purpose was locally testing this exact endpoint), and `.env.example` (the AI Gateway env var template). Removed the "✨ Draft" button, its click handler, and the entire `openDraftModal()`/`requestDraft()`/`closeModal()` block from `followups.js`, along with the module-level comment block describing it as "the ONLY network call anywhere in Sidekick" — that's no longer true of anything, since Sidekick is now a fully local-first app again with zero network calls of any kind. Snooze/dismiss/undo on the follow-up queue are untouched. `APP_VERSION`/`SW_VERSION` bumped 0.9.3 → 0.9.4. Live-verified: 6/6 new `check-remove-mai.js` (Draft button gone, no lingering modal, snooze/dismiss unaffected, zero requests to the now-deleted `/api/draft-followup` endpoint) + `regress-gym.js` 30/30 + `check-notifications.js` 9/9 + `check-backup-relocation.js` 6/6, 0 console errors.

- [x] **Recurring bookings (weekly/biweekly repeat)** (branch `recurring-bookings`) — per user decision to start now on the post-promotion backlog review. Added a "Repeat" section to the NEW-booking form only (never on edit — a recurring series is defined at creation time, not retrofitted onto an existing single booking, matching how most calendar apps scope this): a select (Does not repeat / Weekly / Every 2 weeks) that reveals a "Repeat until" date field. On save, if a repeat cadence is chosen, the app generates one extra `bookings` row per 7- or 14-day step from the original date up through (and including) the end date — each with its own fresh `id`/`cuid`/`createdAt`, otherwise identical to the original (same title/time/duration/location/notes/client) — capped at `MAX_REPEAT_OCCURRENCES` (52 extra rows) so an accidental far-future end date can't silently flood the database. Validation requires an end date when a cadence is picked and rejects one earlier than the start date. The existing month-calendar/day-panel rendering, travel-buffer warnings, and edit/delete flows needed no changes — each generated row is just an ordinary independent booking to the rest of the app; there's no "series" concept to manage (edit/delete only ever affects the one row you tapped). `APP_VERSION`/`SW_VERSION` bumped 0.9.3 → 0.9.4 (branched independently off `main`, alongside the also-independent M-AI-removal branch — one of the two version bumps was reconciled at merge time, since both landed on the same target number). Live-verified: 13/13 new `check-recurring-bookings.js` (repeat UI show/hide, blocks save with no end date, correct weekly/biweekly date math, generated rows are independent+distinct, edit form has no repeat controls) + `regress-gym.js` 30/30 + `check-booking-calendar.js` 31/31 + `check-calendar-midnight.js` 1/1 + `check-incoming-pipeline-calendar-lang.js` 27/27, 0 console errors.

- [x] **Real Premium/paywall gating for Research (PromptPay QR + manual verify)** (branch `premium-gating-promptpay`) — per user decision on the post-promotion backlog review (Start now, scoped explicitly to PromptPay + manual verify, no Stripe/Omise). Since Sidekick is local-first with zero backend, there's no automated payment-gateway callback available to verify a real charge — the honest, buildable-without-a-backend design is a manual, self-attested unlock: "Subscribe" opens a modal showing a PromptPay QR for `฿99` (built via a **newly exposed** `window.renderPaymentChannelsInto` — reusing invoices.js's exact existing QR-rendering pipeline rather than duplicating it, driven off the account's own configured payment channels), plus clear copy stating Sidekick can't verify the payment automatically. An "I've paid — unlock Premium" button sets `settings.premiumUnlocked = true` (persisted via the existing `saveSetting()`, so it's per-device and survives reload). Real gating now actually applies: opening a Premium article while locked shows a ~140-character teaser + a paywall CTA instead of the full body; unlocked, the full body renders normally. Deliberately **left unrestricted regardless of lock state**: the Edit/Delete buttons on the article detail modal — this is the account owner's own content library (seeded articles are Sidekick's own writing, but the owner can add/edit/delete freely), and gating content-management actions behind a "subscription" the owner is themselves managing would be a confusing, self-defeating trap, not a real product distinction. The old M5-era honesty disclaimer in `research.js`'s header comment (real gating "paused" pending a backend) is now stale and was updated to describe the actual manual-verify design. `APP_VERSION`/`SW_VERSION` bumped 0.9.3 → 0.9.4 (branched independently off `main`, alongside two other independent post-promotion branches — one of the three version bumps was reconciled at merge time, since all three landed on the same target number). Live-verified: 13/13 new `check-premium-gating.js` (locked article shows teaser not full body, free articles unaffected, Subscribe modal renders a real PromptPay QR via the shared renderer, confirming unlocks + persists across reload, full body renders once unlocked, Edit stays available regardless of lock state) + `regress-gym.js` 30/30 + `check-payment-channels.js` 9/9 + `check-doc-numbers.js` 11/11 + `check-business-info.js` 7/7, 0 console errors.

- [x] **CI redesign: "Minimal & Professional"** (branch `redesign-ci-minimal-professional`) — per a design-handoff bundle (`design_handoff_sidekick_rebrand.zip`, high-fidelity, CSS/meta-only, no JS logic) uploaded by the user and applied as instructed. Replaces the teal-gradient + marigold identity with a muted **ink**/**pine**/**brass**/**paper** system: flat `var(--bg)` headers (no more gradient bands), pine `#22554B` as the sole primary-action color, brass `#C08A3E` reserved for "needs attention" states (nav badge, due/overdue) instead of flooding the FAB, and the FAB itself switched from a marigold circle to solid ink. New lowercase wordmark "sidekick." with a brass full stop; new **"sk."** app-icon glyph (ink rounded square, paper "sk", brass period dot). Typography: **Schibsted Grotesk** for UI text, **Spline Sans Mono** for every money figure/invoice №/date (`.tnum`). All 420 lines of `styles.css` kept the same structure — purely token/value substitutions (confirmed via diff before applying, matching the handoff's own diff summary exactly) — so this pass touched zero JS files and needed no new tests, only full-suite regression to confirm the CSS-only nature.
  1. **Applied the 8 bundled file replacements** (`styles.css`, `index.html`, `login.html`, `manifest.json`, `icons/icon.svg` + 3 PNGs) as specified in the handoff's own task list.
  2. **Regenerated the PNG icons with the real font** rather than keeping the bundle's Helvetica-approximation placeholders: downloaded the actual Schibsted Grotesk variable-font file and used the same Playwright/Chromium screenshot technique from the original Sidekick rebrand to render the "sk." mark at 512/192/180px with the correct typeface, verified visually.
  3. **Self-hosted both Google Fonts** (recommended, not just optional, per the app's own "no external assets" principle): fetched the Schibsted Grotesk and Spline Sans Mono **variable-font** woff2 files (one file each covers the full weight range, confirmed via the `wght@400..900`-style CSS2 request — 46KB + 36KB total, not 10 separate static-weight files) into a new `app/fonts/`, added `@font-face` rules to `styles.css`, removed the `fonts.googleapis.com`/`fonts.gstatic.com` `<link>` tags from both HTML entry points, and added both font files to `sw.js`'s precache list so they're available fully offline like every other shell asset.
  4. **Repo rename discovered mid-task**: GitHub reported `GRITui/FreeLanz` had been renamed to `GRITui/Sidekickz` partway through this work (unrelated to this PR) — confirmed with the user before repointing the local `origin` remote, updated the Live URL references in this file's System Metadata accordingly.
  `APP_VERSION`/`SW_VERSION` bumped 0.9.4 → 0.9.5. Live-verified: 10/10 new `check-ci-redesign.js` (new pine/paper tokens applied, both fonts actually loaded — not just linked — self-hosted with zero requests to Google's CDN, manifest theme_color updated) + full regression battery (`regress-gym.js` 30/30, `check-recurring-bookings.js` 13/13, `check-premium-gating.js` 13/13, `check-remove-mai.js` 6/6, `check-booking-calendar.js` 31/31) all still green, 0 console errors, screenshotted across login/home/calendar/clients/settings to confirm the visual result matches the handoff.

- [x] **Un-paused dark mode, now the default** (branch `add-dark-mode`) — per a follow-up design-handoff bundle asking the app to default to dark. The dark CSS tokens themselves already existed (bundled in the very first CI-redesign handoff), but `applyTheme()` had a standing M1.5-era decision to **force light unconditionally on every boot regardless of OS or any stored preference** — so the bundle's literal one-line ask (flip the pre-paint inline script's hardcoded theme) would have done nothing on its own, silently overridden a moment later. Actually un-paused it instead: `applyTheme()` now reads a real `sidekick_ui_theme` value (`'light' | 'dark' | 'auto'`, kept in `localStorage` rather than the per-uid `settings` DB object specifically so the pre-paint inline script in `index.html`/`login.html` can read it synchronously before IndexedDB is even open, avoiding a flash of the wrong theme) and a new Settings ▸ Preferences ▸ **Theme** selector (Dark/Light/Auto) wires it up — reusing i18n keys (`theme`/`theme_auto`/`theme_light`/`theme_dark`) that had sat dormant in the dictionary since before the pause. "Auto" simply omits the `data-theme` attribute entirely, letting the already-existing `prefers-color-scheme` media query in `styles.css` track the OS live. **One-time migration** (`sidekick_dark_default_migrated_v1` flag, same guard-flag pattern used elsewhere in this app): every pre-existing install has `'light'` sitting in that same storage key already, force-written there every boot while the feature was paused — since that was never a genuine user choice, it gets flipped to `'dark'` exactly once; any explicit choice a user makes afterward (including picking Light again) is respected permanently and never re-migrated. Also fixed two small leftovers missed by the earlier CSS-only redesign pass because they're hex literals embedded in JS, not CSS tokens: the Pipeline/Calendar "Delivery" stage's dot color (`#0F766E`, the old teal brand, → `#22554B` pine) and the boot-failure fallback page's text color (`#13201C` old ink → `#1A2421` new ink). `APP_VERSION`/`SW_VERSION` bumped 0.9.5 → 0.9.6. Live-verified: 14/14 new `check-dark-mode.js` (fresh installs default to dark with no flash, Settings toggle actually switches rendering — not just the attribute, Auto correctly clears the forced attribute, the one-time migration flips a pre-existing forced-light install exactly once, and — the tricky edge case — an explicit Light choice made *after* migration is never re-overridden) + full regression battery still green, 0 console errors.

## Feature Drift vs. the general app (since fork point `c955d3c`)
| General app has (since fork) | This squad has it? |
|---|---|
| Pipeline / Kanban engagement board | **Yes** — backported, then redesigned per user instruction to a fixed 6-stage sequence (Pitch → Quote → Invoice → Paid → Delivery → Extend), all mandatory — diverges from the general app's per-persona preset pickers, since this squad only ever has one persona |
| Save-customer-from-session banner | No — separate M2.5 sub-feature, deliberately not ported (see Backlog) |
| `/info/` how-to page linked from login | No |
| Emoji-free SVG line-icons throughout | No — still emoji icons from the fork point |
| AI Draft button gated behind `window.FREELANZ_AI` | No — untouched from fork point (M-AI remains globally paused per the general app's handshake anyway) |

## Backlog (self-researched, not yet built)
*(All four items below were completed — see the Milestones entry "Backlog batch: session packages, progress log, quick check-in" and Delta Change Log. Kept here struck through for history rather than deleted.)*
- ~~Session package tracking (N-session bundles, e.g. "buy 10, track remaining")~~ — built.
- ~~Client progress log (weight/measurements over time)~~ — built.
- ~~Quick session check-in (one-tap log for a recurring client)~~ — built.
- ~~Save-customer-from-session banner (the general app's other M2.5 sub-feature)~~ — assessed and deliberately **not** built: this fork's job form has no free-text-customer state for the banner to act on (the Client dropdown is mandatory and always resolves to a real `clients` record, via either picking an existing one or the inline "+ Add a new client" flow that creates one immediately) — see Milestones entry for detail.

## Delta Change Log
- 2026-07-10 · orchestrator · Forked `personal-gym-trainer-freelanz` from the general app at `c955d3c` per user request ("Fork this to another branch. 'Personal Gym Trainer FreeLanz'").
- 2026-07-10 · orchestrator · BUILD — stripped multi-persona code, hardcoded `workType='gym'`, consolidated i18n. Live-verified. Commit `848c4e8`.
- 2026-07-10 · orchestrator · BUILD — shipped as a separate live app at `/gym/` on the shared GitHub Pages site: combined-build workflow, storage-namespace isolation (real bug caught and fixed before shipping — origin-scoped storage would have silently shared data with the general app), distinct branding. Commit `fee0028`. Required one manual step (widening the `github-pages` environment's branch policy) that the user completed. Companion PR #7 opened against `main` so a future push to `main` doesn't wipe out `/gym/`.
- 2026-07-10 · orchestrator · ASSESS — full regression test of the gym app (18/18 pass, 0 errors) + smoke-test comparison against the general app, cataloging feature drift since the fork point. No code shipped this pass, per user direction to stay in research/assess.
- 2026-07-10 · orchestrator · BUILD — Member tags feature: auto-save-as-tag, autocomplete, rename-with-propagation, delete-unlinks-only, Settings management screen, `memberTags` added to the JSON backup/restore set. `DB_VER` 4→5. Live-verified (9/9). Merged via PR #8.
- 2026-07-10 · orchestrator · POLICY — recorded the repo-wide "autonomous, PR-only, user-merges, one-handshake-file-per-squad" execution mode per user instruction (screenshot). Flagged one precision question back to the user (scope of "no push").
- 2026-07-10 · orchestrator · ASSESS — evaluated the general app's Pipeline/Kanban feature for this squad: what it does (stage-based engagement board with a "prepaid" preset already mapped to the gym persona in the general app's code), where it lives today (quick-action button + Settings row, not the bottom nav), and the tradeoff of backporting it (real port work vs. a more useful daily view than a plain session list). Recommended backporting + promoting to nav; user approved ("Go").
- 2026-07-10 · orchestrator · BUILD (branch `gym/pipeline`) — backported the Pipeline engagement board, simplified to a single default stage order (no per-persona preset picker needed). Promoted to the bottom nav in place of Docs (Docs relocated to a Settings row, not removed). Wired invoices.js/docgen.js engagement-linking hooks. No `DB_VER` bump (new fields on existing job records only). Live-verified: 13/13 new checks + all 27 pre-existing checks from the other two suites still pass (40/40 total), 0 console errors. Merged PR #9.
- 2026-07-10 · orchestrator · BUILD (branch `gym/booking-date-picker`) — added a jump-to-date calendar button to the Booking day-view nav bar, per user request to make date selection easier than one-day-at-a-time arrows. Live-verified (2/2), 0 console errors. Merged PR #10.
- 2026-07-10 · orchestrator · BUILD (branch `gym/pipeline-stages`) — replaced the Pipeline's stage set with the user's exact required sequence (Pitch → Quote → Invoice → Paid → Delivery → Extend), all mandatory; Settings ▸ Workflow simplified to reorder-only. Live-verified (14/14 new + all prior suites), 0 console errors. Merged PR #11.
- 2026-07-10 · orchestrator · BUILD (branch `gym/customer-id`) — added a stable, sequential, human-visible Member ID to the customer database, backfilled for existing customers, surfaced in the customer form/list/CSV export. Live-verified (7/7 new), 0 console errors. Merged PR #12.
- 2026-07-10 · orchestrator · ASSESS — evaluated removing the Sessions screen (redundant with Pipeline) and replacing Home's session/invoice quick-actions with something more useful; recommended removing Sessions entirely and adding a Pipeline-at-a-glance strip to Home. User approved ("Go").
- 2026-07-10 · orchestrator · BUILD (branch `gym/home-pipeline-summary`) — removed the Sessions nav item/screen/`renderJobs()`/dead `newInvoice()` handler and orphaned i18n keys; added a 6-pill "Pipeline at a glance" strip to Home (stage counts, tap-to-jump-to-Pipeline) in place of the old quick-action buttons. `APP_VERSION` 0.8.2 → 0.8.3. Live-verified: 53/53 checks across all four regression suites (gym-regression, member-tags, pipeline-stages, customer-id), 0 console errors. Merged PR #13.
- 2026-07-10 · orchestrator · BUILD (branch `gym/client-mgmt-and-form-cleanup`) — removed start/end time + duration from the Add Session form (dead-code cleanup: `calcDuration()`/`sessionHours()`/`fmtHours()`/`inferEndDate()`/`isoOf()` all removed); added a "+ Add a new client…" option to the session form's Client dropdown that opens the client modal on top of the (still-open) session form and links the new client back in on save; promoted Customers to a main-nav "Clients" tab (removed the Settings row) and renamed user-facing "Customer" text to "Client" throughout. `APP_VERSION` 0.8.3 → 0.8.4. Live-verified: 60/60 checks across all four regression suites, 0 console errors.
- 2026-07-10 · orchestrator · CLARIFY — asked the user whether "analytics to help ship future features" meant local, on-device insights or cross-device telemetry to a new backend (a materially bigger, separate project). User chose local, on-device only.
- 2026-07-10 · orchestrator · BUILD (same branch `gym/client-mgmt-and-form-cleanup`) — added local-only usage insights: `usageEvents` store (`DB_VER` 5→6), `logEvent()` instrumented at screen views / session logged / client added / Pipeline stage transitions / backup export, and a Settings ▸ Insights screen summarizing feature usage + pipeline-stage activity, with a manual clear button. No network calls, not included in JSON backup (deliberately — it's per-device diagnostic data, not business data). `APP_VERSION` 0.8.4 → 0.8.5. Live-verified: 8/8 new Insights checks + all 60 prior checks still pass (68/68 total), 0 console errors. Merged PR #14.
- 2026-07-10 · orchestrator · ASSESS — evaluated whether to let engagements skip pipeline stages (e.g. skip Quote); recommended a per-card "Skip" action limited to Quote/Invoice (paperwork steps) with Paid staying mandatory (revenue-tracking checkpoint), rather than reviving the global Quote toggle removed in PR #11. User agreed and additionally asked for a distinct "Finished" outcome at the Extend stage (vs "Extended").
- 2026-07-10 · orchestrator · BUILD (branch `gym/pipeline-skip-and-finish`) — `STAGE_META.quote`/`.invoice` flagged skippable; new `skipJobStage()` (reuses `advanceJobStage()`'s existing bare-advance mechanics) wired to a "Skip" button, never on Paid; new `finishJobStage()` stamps `job.outcome='finished'` (vs `'extended'` from the normal complete path) so the done-badge and Insights pipeline-activity breakdown distinguish the two outcomes; `moveJobStageBack()` clears `outcome` on un-complete. `APP_VERSION` 0.8.5 → 0.8.6. Live-verified: 13/13 new checks + all 68 prior checks still pass (81/81 total), 0 console errors. Merged PR #15.
- 2026-07-10 · orchestrator · BUILD (branch `gym/insights-dev-only`) — per explicit user instruction ("dont let user use, im the one who gonna use that"), hid the Insights Settings row from normal navigation while leaving `logEvent()` collection running unconditionally. Reveal via tapping the Version number 7 times (Android developer-options pattern), a persistent per-device `settings.insightsUnlocked` flag; `switchScreen()` also bounces a direct `'insights'` call back to Settings when locked. `APP_VERSION` 0.8.6 → 0.8.7. Live-verified: 7/7 new checks + all 81 prior checks still pass (88/88 total), 0 console errors. Merged PR #16.
- 2026-07-10 · orchestrator · BUILD (branch `gym/pipeline-rail-and-doc-numbers`) — three related changes from one user request: (1) Pipeline board's 6-column horizontal-scroll Kanban replaced with a left-hand stage rail + single-active-stage card list (no more scrolling through columns), with every stage-mutating function updated to make the rail follow a card to wherever it lands; (2) Home's "Pipeline at a glance" pill row changed from a horizontal scroller to a fixed 3-column grid so all 6 stages show with no scrolling; (3) extracted invoices.js's running-number logic into a shared `nextDocNumber()` in app.js, applied the same scheme to docgen.js's Quote type (`QUO-2026-0001`), and added a brand-new Receipt document type with its own `REC-2026-0001` numbering. `APP_VERSION` 0.8.7 → 0.8.8. Live-verified: 108/108 checks across 8 suites (20/20 pipeline-stages + 16/16 pipeline-skip, both rewritten for the rail; 11/11 new doc-numbering checks; all prior suites unchanged), 0 console errors. Opened as a PR, awaiting merge.
- 2026-07-10 · orchestrator · ASSESS — evaluated whether Client and Member Tags should both exist as separate "who" identifiers on a session; recommended merging (retire Member Tags, make the Client dropdown the sole required source of truth) since the friction argument for a lighter free-text alternative evaporated once inline "+ Add a new client" creation shipped. User approved ("go ahead") in the same message as a second request: add optional seller + client business info (name/tax ID/address) to generated documents, auto-applied from each party's own profile, never mandatory.
- 2026-07-10 · orchestrator · BUILD (branch `gym/merge-client-tags-and-doc-business-info`) — removed the Member Tags feature entirely (screen, modal, datalist, helper functions, its object-store creation for new installs) and the session form's free-text Member field, leaving the Client dropdown as the sole "who" input with a required-field validation; added a Settings ▸ "Business info (optional)" section and a shared `sellerBusinessName()` helper, and updated `docgen.js` to render both seller and client info lines on Quote/Receipt documents whenever the corresponding fields are filled in, with nothing required. Caught and fixed a real bug during testing: the first draft of the seller-info renderer omitted the seller's own name from the document body entirely (only address/tax ID rendered) — fixed so the business name always appears first. Also caught and fixed a latent regression-suite risk: `BACKUP_STORES` still listed the now-never-created `'memberTags'` store, which would have thrown on every backup export/restore for new installs. `APP_VERSION`/`SW_VERSION` bumped 0.8.8 → 0.8.9. Live-verified: 117/117 checks across 8 suites (8/8 new client-merge + 7/7 new business-info + 30/30 gym-regression, pipeline-stages, pipeline-skip, insights, customer-id, doc-numbering all rewritten/re-passing), 0 console errors. `check-member-tags.js` retired from the active suite rotation. Merged PR #18.
- 2026-07-10 · orchestrator · BUILD (branch `gym/booking-month-calendar`, PR #19) — replaced the Booking day-view's empty-state landing screen with a full month calendar (dot-legend for pipeline vs. booking activity per day, tap-to-expand inline day agenda, "+ New booking" scoped to the expanded date, jump-to-date via the month label). Also swapped the 📅 emoji for the nav's existing outlined SVG calendar icon and shortened the legend's "Pipeline activity" label to "Pipeline", per two quick follow-up requests in the same conversation. `APP_VERSION`/`SW_VERSION` bumped 0.8.9 → 0.9.0. Live-verified: 149/149 checks total, 0 console errors, checked at both desktop and 390px mobile widths.
- 2026-07-10 · orchestrator · BUILD (branch `gym/payment-channels`, PR #20) — generalized the single "PromptPay ID" field into a saved list of payment channels (PromptPay/bank/cash/other), with per-invoice snapshotting and payment info now also appearing on the printed/PDF invoice (a pre-existing gap). `APP_VERSION`/`SW_VERSION` bumped 0.8.9 → 0.9.0. Live-verified: 115/115 checks, 0 console errors.
- 2026-07-10 · orchestrator · ASSESS — three items requested alongside the payment-channels build: (1) a shareable invoice link letting clients attach their own proof-of-payment, with the trainer able to attach it manually at the Paid stage as a fallback — scoped as a bigger, backend-touching feature, not built; (2) session-package/bundle usage deduction (decrement remaining sessions on delivery, show remaining balance on the client card, handle the "did they rebook" follow-up gap) — overlaps the existing backlogged "Session package tracking" item, not built; (3) a notifications feature for pipeline/payment/proof-of-payment events — needs a delivery-mechanism decision (in-app vs. push/backend) before a design is worth committing to, not built. All three written up in the Milestones entry above and left for the user to prioritize.
- 2026-07-10 · orchestrator · BUILD (branch `gym/notifications`, PR #21) — built `computeNotificationConditions()` as a single source of truth (overdue invoices, bookings starting within 60 min, pipeline engagements stale 3+ days) feeding both a redesigned Home Action Queue and a new OS-notification checker (`checkAndFireNotifications()`, a Settings toggle requesting `Notification` permission, `setInterval`-driven re-checks every 60s, dedup via a settings-stored map that self-prunes so resolved conditions can re-notify next time; `sw.js` gained a `notificationclick` handler) — app-triggered only, fires while the tab is open/backgrounded, no backend; server-triggered push noted as the next milestone. Also relocated the backup reminder from Home to More/Settings with a nav-icon badge dot. `APP_VERSION`/`SW_VERSION` bumped 0.8.9 → 0.9.0. Live-verified: 120/120 checks, 0 console errors.
- 2026-07-10 · orchestrator · BUILD (branch `gym/collapsible-settings`, PR #22, merged) — wrapped each Settings section in a native `<details>`/`<summary>` for a cleaner, collapsible More screen, per user request. Defaulted every section open (identical to the prior behavior) after finding 6 of 8 existing regression suites interact with fields that would otherwise start hidden. `APP_VERSION`/`SW_VERSION` bumped 0.8.9 → 0.9.0. Live-verified: 8/8 new checks + 106 prior checks (114/114 total), 0 console errors. Merged PR #22.
- 2026-07-10 · orchestrator · MERGE — per explicit user instruction ("Merge and ship"), merged PRs #19, #20, and #21 in sequence on top of the already-merged #22, resolving the resulting cross-branch conflicts locally for each (all four branches diverged from the same trunk point and touch overlapping areas of `index.html`/`app.js`).
- 2026-07-11 · orchestrator · DIRECTION — user uploaded a naming/positioning brief locking the new brand name "Sidekick" and asked to "Do rebrand ui/ux update / And merge this branch as main. Keep this fork." Clarified two decision points before building (AskUserQuestion): which codebase gets the rebrand (user: this fork's tech stack, no longer gym-focused — "Older main fork no longer fit to market") and what happens to the two branches (user: "Promote gym fork to be the new main").
- 2026-07-11 · orchestrator · BUILD (branch `rebrand-sidekick`, PR #26, merged) — full identity rebrand to Sidekick (DB/session/storage-key rename with a one-time zero-data-loss migration, new icon/tagline, all branding strings across `app.js`/`sw.js`/every module/both HTML entry points/`styles.css`'s sidebar wordmark). `APP_VERSION`/`SW_VERSION` bumped 0.9.2 → 0.9.3. Live-verified: 13/13 new `check-sidekick-rebrand.js` + 13/13 new `check-sidekick-migration.js` + all 9 pre-existing regression suites still green.
- 2026-07-11 · orchestrator · PROMOTE (branch `promote-sidekick-to-main`, PR #27, merged) — per "Promote gym fork to be the new main," used `git merge -s ours origin/main --allow-unrelated-histories` off the now-rebranded `personal-gym-trainer-freelanz` so the resulting tree is exactly this fork's tree (verified zero diff) while `main`'s prior history stays reachable as an ancestor — no force-push, no history lost. `personal-gym-trainer-freelanz` itself was left untouched and keeps deploying at `/gym/`, per "Keep this fork." Confirmed the GitHub Pages deploy for `main` went green post-merge.
- 2026-07-11 · orchestrator · DIRECTION — asked the user explicit drop/pause/start verification questions (AskUserQuestion) for every open backlog item and blocker surfaced after the promotion. Decisions: **timer for hourly billing → dropped**; **recurring bookings → start now**; **M5 Premium research-content gating → start now, scoped to PromptPay QR + manual owner-verification** (no Stripe/Omise integration — this app stays backend-free); **M-AI (AI-drafted follow-up messages) → dropped entirely** (remove the dormant code path, not just leave it paused); **Hostinger deploy plan → dropped, GitHub Pages is the canonical host** (already live at both `/` and `/gym/`); **Sidekick domain/social-handle/trademark check → research and report** (registration itself stays a user action); **Thai transliteration check for "Sidekick" → research and report**; **the old `project-changelog-handshake.md` (general-app squad's doc, now describing a retired app) → merge into this file**, done in this same pass — see the new "Pre-Sidekick History" section below for its full prior content, preserved rather than deleted.

## LINE Integration (lead-gen: self-service booking via LINE OA, no chatbot)
- 2026-07-11 · orchestrator · ASSESS — evaluated integrating the LINE OA/LIFF rich menu as a B2B2C premium feature (helps a freelancer's own clients book/pay her, not just Sidekick's own persona-fit) and researched adjacent opportunities beyond the user's own example. Compared two shapes: a conversational LIFF chatbot vs. a plain self-service booking page reached from the rich menu. User picked **self-service booking, no chatbot** for the booking flow itself, keeping the LINE side to acknowledgment + notifications. Also assessed LINE Notify for the notification leg — confirmed via research that **LINE Notify was discontinued (service ended 2025-03-31)**; the Messaging API (Reply/Push) is the only current path, so the MVP plan was built around that instead.
- 2026-07-11 · orchestrator · DIRECTION — listed the concrete prerequisite steps to stand this up (backend resume, LINE provider/channel setup, a datastore decision, webhook build, booking build) and asked the user to complete the account-side steps. User provided a LINE provider ID, Channel ID, and Channel Secret directly in chat despite being asked not to paste live secrets — flagged immediately as compromised (this project had a prior AI Gateway token leak incident) and the user was told to rotate it; user confirmed the secret was rotated. The secret was never written to any file or commit; all code reads it via `process.env.LINE_CHANNEL_SECRET`.
- 2026-07-11 · orchestrator · ASSESS — datastore vendor comparison for the booking page's server-side data (Sidekick is local-first/IndexedDB with zero backend today, which a public booking page can't use). Compared **Neon** (Vercel Marketplace Postgres, HTTP-based serverless driver, no idle auto-pause), **Supabase** (auto-pauses free-tier projects after ~1 week idle — disqualifying for a sporadic pilot's traffic pattern), **Hostinger MySQL** (no static IP for Vercel to allowlist, not built for serverless connection bursts), and **AWS Aurora** (operational complexity mismatch for a single-freelancer pilot). User asked about Hostinger and Supabase/Amazon specifically; both were assessed and ruled out for the reasons above. **User chose Neon.**
- 2026-07-11 · orchestrator · BUILD (branch `line-webhook-mvp`, PR #34) — Step 1: `api/line-webhook.js`, a stateless LINE Messaging API webhook (verifies `x-line-signature` via HMAC-SHA256 over the raw body, replies to a client's first message with a link to the booking page). Written as a Web API `Request`/`Response` handler with `runtime: 'edge'` rather than a classic Vercel `(req, res)` handler, since raw-body access needed for signature verification is unreliable on Vercel's Node helper for plain functions — confirmed via research before choosing the approach. HMAC logic verified locally against Node's own `crypto.createHmac` output before trusting it (exact match; correctly rejects wrong signatures and tampered bodies). `node --check` clean. Not yet deployed or exercised against a real webhook delivery — this session has no Vercel deploy access.
- 2026-07-11 · orchestrator · BUILD (same branch) — Step 0: the actual self-service booking backend, backed by Neon (`@neondatabase/serverless`, this project's first dependency). Added `lib/db.js` (shared connection, HTTP driver to avoid serverless connection-pool exhaustion), `sql/schema.sql` (`services`/`availability_slots`/`bookings`, applied by hand via Neon's SQL console — no migration runner exists yet), `api/booking-availability.js` (public GET, treats an expired hold as available again on read rather than needing a sweep job), and `api/booking-request.js` (public POST — an atomic single-statement `UPDATE ... WHERE status='open' OR (held AND expired) ... RETURNING` soft-holds the slot for 15 minutes as the double-booking guard, no explicit transaction needed since one Postgres statement is already atomic; writes the booking row; best-effort pushes the freelancer a LINE alert via `LINE_FREELANCER_USER_ID`, wrapped so a failed notification can't undo an already-written booking). Client-side confirmation happens directly in this response, not via a LINE reply — a web-form submission has no `replyToken` to reply with. All error paths verified to degrade to a clean JSON error response rather than crash, using a mocked-unreachable `DATABASE_URL`.
- 2026-07-11 · orchestrator · RESOLVE — PR #34 (opened against the pre-drop `main`) came back with a merge conflict against the current `main`. Root cause wasn't a simple text conflict: a separate session's PR #29 ("Drop M-AI entirely," Milestones above) had deleted the *entire* backend/Vercel apparatus this branch also touched — `api/`, `vercel.json`, `dev-server.js`, `.env.example` — and PR #28 had merged `project-changelog-handshake.md` into this file. Rather than resolve unilaterally, asked the user which interpretation was correct. **User confirmed**: the drop decision was specifically about the old AI-draft feature (the "✨ Draft" button on follow-ups) — a separate, narrower thing from the LINE integration, which stays wanted. Resolved by rebasing `line-webhook-mvp` cleanly onto the current `main` (`git checkout -B line-webhook-mvp origin/main`) and re-adding only the LINE/Neon pieces — `lib/line.js`, `lib/db.js`, `sql/schema.sql`, `api/line-webhook.js`, `api/booking-availability.js`, `api/booking-request.js`, `package.json`/`package-lock.json` (description reworded to drop its stale "AI draft proxy" mention), a rebuilt `vercel.json`, and a rebuilt `.env.example` containing only the LINE/Neon vars (the AI Gateway vars from the old template are gone, matching the drop) — without reviving `dev-server.js` or `api/draft-followup.js` or anything AI-draft-related. `LINE_BOOKING_URL`'s fallback updated from the old `gritui.github.io/FreeLanz/` to the current `gritui.github.io/Sidekickz/` to match the repo rename. `node --check` re-run clean on all five files post-rebase; mocked-`DATABASE_URL` error-path check re-verified. Force-pushed the rebased branch to update PR #34 in place rather than opening a new PR.

**Still open, tracked, not started**: connecting the Neon integration in the Vercel dashboard, running `sql/schema.sql` and seeding real pilot services/slots, capturing a real `LINE_FREELANCER_USER_ID` from a live webhook event log, and a first real end-to-end deploy + LINE delivery test — all blocked on Vercel deploy access this session doesn't have. A generic per-freelancer "connect your own LINE OA" flow (vs. today's pilot/single-tenant one-Channel wiring) is deliberately out of scope, noted but not built.

- 2026-07-11 · (a separate concurrent session, commit `4731d5a`, not logged here at the time) · BUILD — added **LINE Login** as a third sign-in option alongside password auth and guest mode: `lib/lineLogin.js`/`api/line-login-start.js`/`api/line-login-callback.js` implement the OAuth2/OIDC round-trip (HMAC-signed `state` carrying a replay-checked `nonce`, local HS256 ID-token verification against LINE's own shared-secret scheme — no round-trip to LINE needed to validate it), handing the verified profile back to `login.html` as a URL fragment (never sent to any server) where `app.js` creates/logs into an ordinary local `users` row keyed `line:<sub>`. This is a distinct LINE Login channel from the Messaging API one `lib/line.js` talks to (separate channel types in the LINE Developers Console). A separate, revived Vercel deploy (`.github/workflows/deploy-vercel.yml`, production alias `https://sidekickz.vercel.app`) now runs these functions — with `vercel.json`'s `outputDirectory: app`, this project also mirrors the full static app, so it's its own separate origin/IndexedDB silo alongside the two GitHub Pages deployments (root + `/gym/`).
- 2026-07-12 · orchestrator · REVIEW — asked to review the already-merged LINE Login implementation above; reported 4 findings ranked by severity: (1) **critical** — `app.js`'s "Continue with LINE" button used a relative URL (`api/line-login-start`), which 404s outright on GitHub Pages (100% static, no `api/` runtime there) — the feature could never have worked from either GitHub Pages deployment, only from the Vercel mirror itself; (2) **security** — the OAuth `state` was HMAC-signed (tamper-evident) but not bound to the initiating browser, a login-CSRF gap where a captured/replayed `state` could force an unintended sign-in on a victim's browser; (3) **security, low severity** — `verifyState`/`verifyLineSignature` compared HMAC signatures with plain `===`, a timing side-channel; (4) **low severity** — nothing stopped a password-auth registration from using a `line:`-prefixed username, colliding with the LINE-account namespace.
- 2026-07-12 · orchestrator · FIX (branch `fix-line-login-cross-origin`) — fixed all four: (1)+(2) combined, since fixing the relative-URL bug in isolation would have introduced a new bug (the callback always landing users back on the Vercel origin's separate local database, regardless of where they started) — `loginWithLine()` now redirects to the absolute `https://sidekickz.vercel.app/api/line-login-start`, passing a `returnTo` query param (the current page's own origin+path) that `line-login-start.js` validates against an exact allowlist (the two GitHub Pages URLs + the Vercel mirror's own) before folding it into the signed `state` payload (`signState`/`verifyState` in `lib/lineLogin.js` generalized to carry `{nonce, returnTo}`); `line-login-callback.js` now redirects to that validated `returnTo` instead of its own request origin (falling back to its own origin only for pre-verification error paths, where no validated `returnTo` exists yet). Also added real browser-binding: `line-login-start.js` sets a short-lived (`Max-Age=600`), `HttpOnly`/`Secure`/`SameSite=Lax` cookie holding the same nonce, scoped to the callback path only; `line-login-callback.js` checks it against the nonce recovered from `state` before proceeding, and clears it on every exit path — this is what actually closes the login-CSRF gap (the signature alone only proves the token wasn't tampered with, not which browser is completing the flow). (3) added a `constantTimeEqual()` helper to `lib/lineLogin.js`, used by both `verifyState` and `lib/line.js`'s `verifyLineSignature`. (4) `submitAuth()`'s registration path now rejects any `line:`-prefixed username with a new `err_reserved_username` i18n string (en+th). `APP_VERSION`/`SW_VERSION` bumped 0.9.7 → 0.9.8. Verified locally against a static file server + Playwright (real end-to-end OAuth needs the user's own live LINE Developers Console credentials and Vercel deploy, out of reach here): confirmed the button now builds the correct absolute cross-origin URL with the right `returnTo`; confirmed a `line:`-prefixed registration attempt is blocked with the new error banner and a normal registration still succeeds; confirmed guest login is unaffected; `node --check` clean on all five touched `api/`/`lib/` files plus `app/app.js`.
- 2026-07-12 · orchestrator · CONFIG + DEPLOY — walked the user through the manual side of standing up LINE Login for real: creating the LINE Login channel (separate from the Messaging API one) in the LINE Developers Console, registering the exact callback URL, generating `LINE_LOGIN_STATE_SECRET`, and setting all four `LINE_LOGIN_*` vars in the Vercel dashboard. Hit a real Vercel constraint along the way: this project's own GitHub Actions workflow produces **prebuilt** deployments (`vercel deploy --prebuilt`), and Vercel refuses to "Redeploy" those in place once new env vars are added ("Prebuilt deployments cannot be redeployed... create a new deployment instead") — because a prebuilt deployment's output is frozen at build time, so redeploying it in place would keep serving the old (missing) env vars rather than actually reading the new ones; only a fresh run of the build pipeline (`vercel pull` → `vercel build` → `vercel deploy --prebuilt`) picks up dashboard env var changes. Since `deploy-vercel.yml` only triggers on `push`/`pull_request` (no `workflow_dispatch`), forced an immediate new deployment with an empty commit directly to `main` (`0184152`) — done as a one-off per explicit user instruction ("force an immediate redeploy"), not a change to this repo's standing PR-only convention. Run `29192840504` completed green. Flagged, not yet done: adding a `workflow_dispatch` trigger to the workflow so future env-var-only changes don't need a throwaway commit.
- 2026-07-12 · orchestrator · BUILD (branch `line-login-profile-step`) — per user request ("after first login also ask user to fill user personal info as well"), asked a clarifying question on scope before building (which fields, required or skippable) — user chose: **name only, required (no skip), and apply the same requirement to email/password registration for parity**. Two changes: (1) `submitAuth()`'s registration path now rejects an empty name field (previously optional) with a new `err_auth_name_required` i18n string, and stamps new password accounts with `profileComplete: true`. (2) A brand-new one-time step gates a **first** LINE login only: `handleLineLoginRedirect()` now creates new LINE accounts with `profileComplete: false`, and — rather than logging straight in — shows a new `#s-line-profile` screen (`login.html`) prefilled with LINE's own display name (editable, required, no skip) via `showLineProfileStep()`; submitting calls `completeLineProfile()`, which sets `profileComplete: true` and only then finishes the login (`finishLineLogin()`, factored out of the old inline logic so both the pending and already-complete paths share it). `profileComplete` is treated as `true` for any account where the field is simply absent (every pre-existing password account, and any LINE account created before this change), so nobody already signed up gets retroactively blocked — only a stored `false` gates entry. A second LINE login from the same account (now `profileComplete: true`) skips the step entirely, confirmed live. One real bug caught and fixed while building this: the pending-user object handed to `showLineProfileStep()`/`completeLineProfile()` was a hand-assembled slim object (`{id, username, firstName}`) rather than the full stored row — since `completeLineProfile()` saves via `dbPut()` (a keyPath `put()`, which replaces the *entire* record, not a partial merge), submitting the form would have silently wiped `salt`/`hash`/`iters`/`linePicture`/`lineAuth`/`createdAt` off the just-created row. Fixed by re-fetching the full row via `dbGet()` immediately after `dbAdd()` instead of reusing the slim object — verified by reading the record straight out of IndexedDB after completing the flow, confirming every original field survived. Also caught mid-build: `applyLang()` (the i18n render pass) previously ran *after* `handleLineLoginRedirect()` in `bootLogin()`, so the new profile screen's `data-i18n` text would never have been localized for a Thai-language user (the English fallback strings happen to be correct, masking the bug for English testing) — reordered `applyLang()` to run first. Also caught and renamed a new i18n key that collided with an unrelated pre-existing key of the same name (`err_name_required`, already used by the Clients/Services "select a name" validation) — renamed the new one to `err_auth_name_required` to avoid one silently overriding the other's text depending on object key order. `APP_VERSION`/`SW_VERSION` bumped 0.9.8 → 0.9.9. Live-verified via a static file server + Playwright, simulating the LINE callback fragment directly (real end-to-end OAuth still needs live credentials): first LINE login shows the profile step prefilled from LINE, blocks an empty-name submit with the right error, completing it logs in and preserves every DB field; a second login with the same LINE account skips straight in; password registration without a name is now blocked, with a name still succeeds; guest login unaffected; `node --check` clean.
- 2026-07-12 · orchestrator · BUILD (branch `account-name-and-labels`) — three changes from one message. (1) The user's own real LINE login had happened *before* the profile-completion step above was merged, so their account's `profileComplete` field is simply absent — treated as already-complete by design, meaning they'd never be re-prompted for a name and had no other way to set one. Added a general **"edit your name"** entry point instead of a one-off backfill: the account row atop Settings ▸ More (avatar + name) is now tappable (guarded off for guest, who has no stored name to edit), opening a small modal (`openAccountNameModal()`/`saveAccountName()`) prefilled with the current name, required non-empty, that updates the `users` row and refreshes the greeting/avatar immediately. Reused the same fetch-full-row-before-`dbPut()` pattern from the LINE profile-completion fix above (`dbGet()` first) to avoid the identical data-loss risk. (2) Renamed the "Pipeline" feature to **"Workflow"** throughout the user-facing UI (bottom nav, screen title, Home's "at a glance"/"Incoming" section titles, the Insights feature-usage and pipeline-activity labels, the stage rail's aria-label) — the internal `stage`/`pipeline` ids, function names, and IndexedDB fields are all unchanged, same rename convention as Booking→Calendar. Caught a naming collision this created: Settings already had its own section literally titled "Workflow" (the stage-reorder config) — renamed that one to **"Stage order"** so the main nav tab and that settings section don't share the same label. (3) Renamed the **"Pitch"** stage to **"Inquiry"** (`STAGE_META.pitch.label`/`.action`/`.done`) — the internal stage id stays `pitch`. `APP_VERSION`/`SW_VERSION` bumped 0.9.9 → 0.9.10. Live-verified via a static file server + Playwright: guest sees no edit affordance and clicking the account row does nothing; a registered user can open the modal, gets blocked on an empty name, and a saved name change persists across reload; bottom nav/screen title read "Workflow", the settings section reads "Stage order" (no longer colliding), the stage rail shows "Inquiry" and never "Pitch"; `node --check` clean.

## Pre-Sidekick History (general "Freelanz" app, M1–M5 — retired 2026-07-11)
*The section below is the full prior content of `project-changelog-handshake.md`, the general multi-persona "Freelanz" app's own handshake file, merged in here verbatim after that app was retired and this squad's fork was promoted to `main` (see the Delta Change Log entries above). That app no longer exists as separate code — this fork's code is now `main`. Kept for historical/engineering-decision record, not as a live status board. A few items it left open were explicitly resolved by the user in the same conversation that triggered this merge, noted inline below rather than silently left stale.*

### System Metadata (as of retirement)
- Target Goal: **Freelanz** — a local-first freelance automation PWA (invoicing, tax, doc-gen, booking, CRM, portfolio) for independent workers, English-only MVP, bilingual (EN/TH) on the roadmap.
- Architecture: local-first PWA — vanilla JS single `app.js`, IndexedDB, Service Worker, no backend, no secrets. Borrows proven scaffolding from DriverLog (`../Driver Log/site/`). One exception as of M-AI: `api/draft-followup.js`, a single opt-in Vercel serverless function that holds the AI Gateway key server-side (never client-side) — everything else remains fully local-first and offline-capable. **(Superseded 2026-07-11: M-AI was dropped entirely per user decision — see below.)**
- Identity: teal `#0F766E`/`#14B8A6` + marigold `#F59E0B` accent, warm surface `#FBFAF7`, dark theme.
- Status at retirement: M1–M5 COMPLETE + browser-verified · v0.5.1 · M-AI code was done and locally testable, with its remaining work (Vercel deploy) explicitly paused since 2026-07-09 — **now formally dropped, not resumed** (2026-07-11 decision).
- GitHub: PR #2 was open against the old `main` (M-AI scaffold + M5 Research library + backlog fixes); moot now that `main` has been replaced by the Sidekick promotion.
- **SECURITY NOTE (still relevant)**: a live AI Gateway token (`vck_...`) was pasted directly in chat on 2026-07-09 and must be treated as compromised — rotate it in the Vercel dashboard if it's ever reused for anything, even though the feature itself is now dropped.
- IndexedDB name at retirement: `freelanz-v2`.

### Milestones (as of retirement)
- [x] **M1 — Foundation**: PWA shell, auth+guest (PBKDF2), IndexedDB, persona onboarding, i18n `t(key@persona)` relabel layer, theming, dashboard, log-job + settings + CSV/JSON export.
- [x] **M1.5 — Refinements**: light-mode-only, job-type dropdown + Custom + Personal gym trainer, Customers module, Services catalog w/ rates + fee-prefill.
- [x] **M2 — Money & docs**: invoicing + PromptPay QR, WHT/VAT tax engine, doc-gen templates.
- [ ] **M-AI — dropped 2026-07-11** (was: paused 2026-07-09). AI-drafted follow-up messages (reason + customer name → a short drafted nudge). `api/draft-followup.js` + client wiring in followups.js were built and unit-tested (16/16) but never deployed live; per explicit user decision this pass, the feature is removed rather than resumed — see the gym-squad Milestones section above for the removal entry.
- [x] **M3 — Booking & CRM**: day view + travel buffers, follow-up queue, portfolio.
- [x] **M4 — Polish**: offline hardening, CSV/JSON export (fixed a real gap — backup silently missed 7 of 9 stores), deploy prep (`.htaccess` + subpath-mount). Actual push-to-Hostinger was a pending user action — **dropped 2026-07-11**, GitHub Pages (where Sidekick now lives) is the canonical host instead.
- [x] **M5 — Research library (monetization scaffold)**: content library (`research.js`), 6 seeded articles (3 Premium-tagged), category filters. Shipped with **no real payment gating** — every article fully readable regardless of `isPremium`. **Superseded 2026-07-11**: real gating is now being built for the gym-squad/Sidekick codebase, scoped to PromptPay QR + manual owner-verification (see gym-squad Milestones).

### Reuse Ledger (borrowed from sibling projects)
| System | Source | Disposition |
|---|---|---|
| IndexedDB layer (`openDB/dbAll/dbPut/dbAdd/dbDel/dbGet`) | DriverLog `site/app.js` | lift + add `clients/invoices/documents` stores |
| Auth + guest (`sha256hex/randomSalt/hashPassword/loginGuest/submitAuth/logout`) | DriverLog | lift verbatim |
| i18n engine `t(key@type)` + `applyLang` | DriverLog | lift engine, re-author dict (EN only, 5 personas) |
| Persona onboarding + presets | DriverLog `applyWorkerType` | adapt to 5-persona taxonomy |
| CSV(BOM, injection-safe) + JSON backup/restore | DriverLog | lift verbatim |
| PWA shell / `sw.js` / manifest / subpath build | DriverLog `site/` | lift + rebrand |
| Deploy discipline (version lockstep, no-cache, versioned URLs, Hostinger MCP) | global CLAUDE.md + memory | apply as process |

**NOT borrowed** (handoff §10 + secrets rule): DriverLog brand/red, AdSense id, AccessTrade affiliate slider, `drivee-orchestrator`, PocketBase sync, any tokens/FTP creds.

### Model / Effort policy (approved 2026-07-08, effective M2 onward)
Right-size per task — Opus on the crux, cheaper tiers on mechanical checks:
| Task kind | Model · Effort |
|---|---|
| Builder (multi-file / migration / new screens) | **Opus · high** |
| Verify — data-loss / DB-migration risk | **Opus · high** |
| Fix — applies edits to shared/crux code | **Opus · high** |
| Verify — i18n / UX / handler-exists (logic trace) | **Sonnet · high** |
| Verify — version-lockstep / grep-mechanical | **Haiku · medium** |
_M1.5 ran pre-policy (all Opus·high). M2 and M3 both applied this matrix via the `Agent` tool's `model` param (builders on Opus; verify lenses split Opus/Sonnet/Haiku by risk tier)._

### Delta Change Logs (M1–M5, general Freelanz app)
- 2026-07-08 · orchestrator · INIT · Handshake created. Wireframes (M0) approved as-is. Scope locked: local-first PWA MVP, English-only. Launching M1 build via ultracode workflow (build → 5-lens adversarial verify → fix).
- 2026-07-08 · ultracode-wf `wf_2c61b339-ade` · BUILD · 7 files written to app/ (index.html, login.html, app.js, styles.css, sw.js, manifest.json, icons/icon.svg). DriverLog spine lifted (IDB/auth/i18n/CSV+JSON export/boot); stripped brand/AdSense/affiliate/PocketBase-sync/driver-fields/secrets. node --check + stubbed-DOM runtime tests 21/21 pass. APP_VERSION 0.1.0 / SW freelanz-v0.1.0.
- 2026-07-08 · verify-panel (5 lenses) · VERIFY · 11 findings, 0 high/critical. Auto-fixer applied none (gated to high/critical).
- 2026-07-08 · orchestrator + fix-agent · FIX · Curated 9 fixes applied: importBackup atomicity (data-loss), SHA-256→PBKDF2(100k)+min-pw-8, visible focus rings, form `for`/labels, auto-theme dark contrast (module-pill + settings-input), SW no longer caches 4xx/5xx, delete_job@persona variants, restore no longer bleeds theme/lang. Deferred (2): guest-persistence (intentional per handoff), iOS PNG apple-touch-icon → M4. node --check green.
- 2026-07-08 · orchestrator · LIVE-CHECK · Drove real Chrome @ localhost:8823: guest → onboarding → Photographer persona relabel (live) → log shoot (Net ฿10,000 computed) → dashboard stats update → Shoots list row → full-reload persistence. 0 console errors. M1 signed off.
- 2026-07-08 · ultracode-wf `wf_973fe4b7-91c` · M1.5 BUILD+VERIFY+FIX · 5 files edited in place; DB_VER 1→2 adds `services` store (guarded, no data loss). Verify (4 lenses): 3 findings, 1 high (customer-intake data-loss) auto-fixed. Model/effort: all Opus·high (ran pre-policy).
- 2026-07-08 · orchestrator · M1.5 FIXES · Applied 2 remaining verify lows (job-type-preserve-on-edit; pre-paint light inline script) + 2 self-found robustness fixes during live check: `openDB` `onblocked`/`onversionchange` (multi-tab upgrade could wedge — real), boot() error-surface instead of silent blank. Bumped v0.2.0→0.2.1 (lockstep). Renamed DB freelanz-v1→v2 (v1 got wedged locally by aggressive debug delete-while-open).
- 2026-07-08 · orchestrator · M1.5 LIVE-CHECK · Clean Chrome run: fresh boot→login→light mode (no toggle) → guest → job-type dropdown → gym persona relabel (Sessions/Member) → services seeded (gym-only, correct) → add-session service prefills fee ฿800 → saves + persists, dashboard ฿800/1. 0 console errors. M1.5 signed off.
- 2026-07-08 · orchestrator · DIRECTION · User adopted ultracode for agents/verify/dynamic-skills; approved per-task model/effort matrix. Vercel = later serverless-proxy AI feature (backend). Next: secrets .gitignore → git init → GitHub via gh.
- 2026-07-09 · ultracode-wf · M2 BUILD+VERIFY+FIX (commit `f082abc`) · Parallel module fan-out: tax.js (WHT/VAT `computeTax`) ∥ invoices.js (quote→invoice, line items, auto-numbered `INV-YYYY-####`) ∥ docgen.js (Contract/NDA/Quote). PromptPay: self-contained EMVCo payload + CRC16-CCITT + QR encoder, fully offline. Adversarial verify found + fixed: PromptPay 0066-mobile misclassification, a11y, print-CSS scoping, version-lockstep. Bumped v0.2.1→0.3.0.
- 2026-07-09 · orchestrator · LIVE-CHECK · Full M2 flow verified end to end (customer → service → invoice → VAT/WHT breakdown → PromptPay QR → Tax screen → Contract doc). 0 console errors. M2 signed off.
- 2026-07-09 · orchestrator · M3 SCAFFOLD · `DB_VER` 2→3 adds `bookings`/`followups`/`portfolio` stores, also retroactively backfilling `invoices`/`documents` for pre-existing v2 databases. Bumped v0.3.0→0.4.0.
- 2026-07-09 · 3 parallel Opus·high builder agents · M3 BUILD · bookings.js (day view, travel-buffer warnings) ∥ followups.js (CRM queue: overdue invoices/unsent drafts/stale customers, snooze/dismiss) ∥ portfolio.js (item grid, image-to-dataURL, Share/Print).
- 2026-07-09 · 3 parallel verify agents · VERIFY · 0 critical/high findings; DB migration backfill confirmed genuine, uid scoping consistent, no cross-account leaks.
- 2026-07-09 · orchestrator · M3 FIX · 6 curated fixes (version-display text, travel-buffer strip rekeyed, overnight "(+1d)" suffix, 0-buffer wording, overdue-with-blank-due-date detection, try/catch guards on render functions).
- 2026-07-09 · orchestrator · M3 LIVE-CHECK · Full booking/follow-up/portfolio flow verified, including overlap warnings and snooze/dismiss/undo. 0 console errors. M3 signed off.
- 2026-07-09 · orchestrator · M4 ASSESS · Found the highest-value gap: JSON backup/restore only round-tripped `jobs`/`expenses`, silently excluding every store added since (clients/services/invoices/documents/bookings/followups/portfolio).
- 2026-07-09 · orchestrator · M4 FIX — export · Generalized export/import over a `BACKUP_STORES` list (all 9 uid-scoped stores), preserving atomicity. Added "Export invoices CSV".
- 2026-07-09 · orchestrator · M4 LIVE-CHECK — export/offline · Full backup roundtrip verified (all 9 stores); offline boot + offline booking creation verified with 0 console errors, no sw.js changes needed.
- 2026-07-09 · orchestrator · M4 FIX + LIVE-CHECK — deploy prep · Added `.htaccess` for Apache/Hostinger-style hosting; verified subpath-mount (`/freelanz/`) end to end. Actual upload needed the user's Hostinger credentials — **never happened, and the plan is now formally dropped (2026-07-11)** in favor of the already-live GitHub Pages deployment.
- 2026-07-09 · orchestrator · DIRECTION · User asked for an M-AI recommendation (AI-drafted follow-up messages); then pasted a live AI Gateway token directly in chat — flagged as compromised immediately, rotate before any real use.
- 2026-07-09 · orchestrator · M-AI BUILD · `api/draft-followup.js` (Vercel serverless proxy) + `vercel.json` + an opt-in "✨ Draft" button in followups.js. **This entire feature was removed 2026-07-11** per user decision (Drop, not resume) — see gym-squad Milestones/Delta log for the removal entry.
- 2026-07-09 · orchestrator · M-AI LIVE-CHECK (partial) · No Vercel account available; verified the graceful-failure path (clear error message, 0 uncaught JS errors) against an endpoint that can't serve the real call.
- 2026-07-09 · orchestrator · BACKLOG SWEEP · Rendered PNG icons from the existing SVG (iOS apple-touch-icon fix); re-confirmed the "jobs list shows client name twice" note was already a non-bug.
- 2026-07-09 · orchestrator · FOLLOW-UPS UN-DISMISS · Added a "Show dismissed (N)" toggle + Undo action to the follow-up queue.
- 2026-07-09 · orchestrator · FULL-APP REGRESSION · Comprehensive live pass across every screen, 0 console errors, backup still correct.
- 2026-07-09 · orchestrator · CROSS-MIDNIGHT BOOKINGS · Travel-buffer/overlap detection extended across the midnight boundary.
- 2026-07-09 · orchestrator · M-AI UNIT TESTS · Standalone Node harness against the real handler + mocked fetch: 16/16 passed.
- 2026-07-09 · orchestrator · M-AI LOCAL DEV SERVER · `dev-server.js` — zero-dependency Node http server routing `POST /api/draft-followup` to the same unmodified handler, for local end-to-end testing without a Vercel account. **Removed along with the rest of M-AI, 2026-07-11.**
- 2026-07-09 · orchestrator · DIRECTION · User: "Skip the vercel-thing until i say again." M-AI paused — **later formally dropped, 2026-07-11.**
- 2026-07-09 · orchestrator · DIRECTION — M5 scope · User asked for a research-topics monetization model; clarified via AskUserQuestion it meant a new content library gated by subscription. User chose to build the UI now and gate it later.
- 2026-07-09 · orchestrator · M5 SCAFFOLD + BUILD · `DB_VER` 3→4 adds `research` store. 3 seed articles (2 Premium-tagged), UI groundwork only, no real payment check.
- 2026-07-09 · orchestrator · M5 LIVE-CHECK · Full CRUD + Subscribe-preview-toast verified; caught and fixed `research` missing from `BACKUP_STORES`.
- 2026-07-09 · orchestrator · GITHUB · Rebased 15 local commits cleanly onto `main` after discovering PR #1 had already merged; opened PR #2.
- 2026-07-09 · orchestrator · LOOP CONTINUED — M5 content + filter · Expanded the research library 3→6 articles, added category filter chips. Bumped v0.5.0→0.5.1.
- 2026-07-13 · orchestrator · DIRECTION — backend migration · User asked to remove the local-first data constraint entirely; risk assessment covered existing-data migration path, the LINE-pilot's single-tenant schema not being reusable as-is, client-side-only auth, offline-capability risk, and PII-centralization obligations. User chose full migration (core app data server-side for every registered account) over lighter alternatives (opt-in cloud sync, or just growing the LINE pilot). Plan scoped to a phased rollout; only Phase 1 (auth + one resource, `clients`, feature-flagged) approved to build now — Phases 2–6 (fan-out, migration flow, read cutover, IndexedDB removal, optional offline sync) are roadmap only.
- 2026-07-13 · orchestrator · BACKEND MIGRATION PHASE 1 BUILD · New `sql/schema-core.sql` (multi-tenant `users`/`clients`, sibling to the LINE pilot's own single-tenant `sql/schema.sql`, left untouched). New `lib/auth.js` (bearer-session sign/verify, reusing `lib/lineLogin.js`'s HMAC pattern; PBKDF2 password hash/verify matching app.js's client-side `hashPassword()` exactly, so a hash computed in the browser today verifies here without ever transmitting a plaintext password during registration). New `lib/crudHandler.js` (shared CRUD factory, strict session-derived row-scoping) + `lib/cors.js` (explicit origin allowlist, reusing `api/line-login-start.js`'s existing GitHub-Pages/Vercel-mirror list). New endpoints: `api/auth-register.js`, `api/auth-login.js`, `api/auth-session.js`, `api/clients.js`, `api/migrate-upload.js`.
  - Design refinement made during the build (deviates from the original plan text in a lower-risk direction): rather than cutting `clients` reads/writes over to the API for registered accounts, Phase 1 ships as a **mirror**, not a replacement — local IndexedDB stays every store's one source of truth for reads and writes, unchanged. Discovered while implementing that every other store (jobs/packages/progressLogs/...) references a client by its local autoincrement `id`, not `cuid`; cutting `clients` over now would have meant abandoning that `id` as the addressing key everywhere it's referenced, a much bigger change than this slice is scoped for. Mirroring by `cuid` (already present on every client record) avoids that entirely — new `app/dataClient.js` (`window.SidekickBackend`) best-effort-mirrors `clients` saves/deletes to the backend alongside the existing local write, fully opt-in via a new "Cloud backup (beta)" row in Settings (`enableCloudBackup()`/`renderCloudBackupSection()`), reusing the already-known local `{salt,hash,iters}` to enable without re-prompting for a password. Guest mode untouched — zero network calls, by design, matching its whole reason to exist.
  - `APP_VERSION`/`SW_VERSION` 0.9.10→0.9.11 (lockstep across app.js/sw.js/index.html/login.html); `dataClient.js` added to `sw.js`'s precached shell.
  - Not yet done (tracked as this phase's own follow-ups, not silently skipped): live end-to-end verification against a real Neon connection (no `DATABASE_URL` available in this environment — verified instead via a Node harness against a hand-rolled in-memory fake of `@neondatabase/serverless`'s dual tagged-template/parameterized-call API, which could not be confirmed against the real package locally); the one-time "back up your existing local data" prompt described in the plan is implemented as the same `enableCloudBackup()` action rather than a separate first-login modal, for scope reasons.
- 2026-07-13 · orchestrator · i18n: STAGE_META THROUGH t() (commit `0c91d5b`) · `STAGE_META`'s `label`/`action`/`done`/`hint` fields (app.js, then ~line 455) were 100% hardcoded English literals, never routed through `t()` at all — the actual stage-chip/action-button text a user sees throughout the whole Task flow board, invisible to the language setting. Changed every field to hold an i18n key instead of raw text (24 new key pairs, en+th), updated every consumer (insights labels, OS notification body, incoming-pipeline rows, pipeline chips/hint/action-button, workflow/stage-order settings rows) to wrap with `t()`. Verified with a dedicated Playwright script (9/9): real Thai text renders app-wide with a language switch, no raw `stage_*` keys or `undefined` leak anywhere including bookings.js's calendar legend (a cross-file consumer of the same object).
- 2026-07-13 · orchestrator · BACKEND MIGRATION PHASE 2: SCHEMA + API FAN-OUT (commit `12f9df9`) · Extended the Phase 1 pattern from `clients` to the other 11 in-scope stores: `jobs`/`services`/`invoices`/`documents`/`app_bookings`/`followups`/`portfolio`/`research`/`packages`/`progress_logs`/`settings` added to `sql/schema-core.sql` (JSONB for embedded arrays — sub-tasks/milestones/time-entries on jobs, line-items/payment-channels on invoices, per-doc-type fields on documents — mirroring the IndexedDB shape as-is rather than normalizing into child tables) and a matching `api/*.js` per resource, all but `settings` a 3-line `createResourceHandler(table, FIELDS)` wrapper around the existing `lib/crudHandler.js`; `settings` got a bespoke handler since its natural key is `(user_cuid, key)` with no `cuid` at all. Server-assigned `created_at` deliberately left off every writable-FIELDS list for the 4 stores with a client-set creation timestamp (app_bookings/followups/portfolio/research), so it's assigned once by the column's own default and can never be backdated by a client. **Schema + API only — this phase deliberately stops short of `app/dataClient.js` mirror wiring** (tracked as its own follow-up below) to avoid touching `app.js`/`bookings.js`/`invoices.js` while the STAGE_META and full-i18n-pass work below was still landing concurrently in those same files. Verified with a standalone Node harness against the hand-rolled fake Neon driver (81/81), including the server-assigned-`created_at`-immutability case.
- 2026-07-13 · orchestrator · i18n: FULL THAI PASS ON bookings.js / invoices.js (commit `9cb3fe4`) · Both files called `t()` extensively already but their own header comments still claimed "English-only" — real hardcoded English remained in form labels, placeholders, empty states, and validation errors (154 new i18n key pairs added, en+th). Caught and fixed a real bug found along the way: `invoices.js`'s `recalcTotals()` had a local `const t = window.computeTax(...)` silently shadowing the global `t()` translation function, breaking every translation call downstream of it in that function (renamed to `taxRes`). Also caught, on a second pass, a duplicate hardcoded-English invoice print/PDF rendering path ("Client pays"/"You receive"/"Bill to") that a first pass had missed. Verified with a dedicated Playwright script (144/144) plus the full existing regression suite re-run (broad-smoke 18/18, regression_full 14/14, regression_full2 11/11, service-fields 12/12), 0 console errors.
- 2026-07-13 · orchestrator · INVOICE/MILESTONE/UNBILLED-TIME AUTO-LINKING · Resolves the oldest open backlog item below. `draftMilestoneInvoice()`/`convertUnbilledToInvoice()` (app.js) now pass a `linkMeta` object through `openInvoiceForm()`'s existing `prefillQuote` parameter (`invoices.js`); on **actual save only** (never on cancel), a new `window.onMilestoneInvoiceCreated`/`window.onUnbilledTimeInvoiceCreated` hook writes the invoiceId back — `m.invoiceId` on the milestone, `e.invoiced = true` + `e.invoiceId` on exactly the time entries that were unbilled at draft-time (a fresh snapshot, not whatever happens to be unbilled by the time save occurs, so an entry logged while the invoice form is still open is never swept in by mistake). Deliberately kept separate from the pre-existing `onEngagementInvoiceCreated`/`fromJobId` engagement-linking hook, which also advances the Pipeline stage — milestone/unbilled-time invoices must not do that, since a job can have several milestones before it's actually done. `renderMilestones()` now shows an "Invoiced" chip (reusing the existing `time_invoiced_label` i18n key) instead of the "Draft invoice" button once a milestone is linked, preventing double-invoicing the same milestone. `APP_VERSION`/`SW_VERSION` 0.9.11→0.9.12. Verified with a dedicated Playwright script (15/15, including both cancel-leaves-nothing-linked cases and the entry-logged-after-the-snapshot-is-never-swept-in case) plus the full existing regression suite re-run (broad-smoke 18/18, regression_full 14/14, regression_full2 11/11, service-fields 12/12, subtask-ux 8/8, expiry-fastpath 16/16, package-delivery 16/16), 0 console errors.

- 2026-07-13 · orchestrator · BACKEND MIGRATION PHASE 2b: MIRROR WIRING · Extends `app/dataClient.js`'s Phase 1 clients-mirror pattern to the 11 stores Phase 2 fanned `crudHandler` out to server-side (schema + API only, until now). A new generic `createMirror(apiPath, toPayload)` factory produces a create-then-409-fallback-to-update pair per store — `mirrorJobSave/Delete`, `mirrorServiceSave/Delete`, `mirrorInvoiceSave/Delete`, `mirrorDocumentSave/Delete`, `mirrorBookingSave/Delete` (POSTs to `/api/app-bookings`, not `/api/bookings` — the local IndexedDB store is still named `bookings`, only the backend table/route differ to avoid colliding with the LINE pilot's own table), `mirrorFollowupSave` (save-only — followups are never deleted locally), `mirrorPortfolioSave/Delete`, `mirrorResearchSave/Delete`, `mirrorPackageSave` (save-only — nothing currently deletes a package locally either), `mirrorProgressLogSave/Delete`, plus a bespoke `mirrorSettingSave` (matches `api/settings.js`'s own bespoke non-`createResourceHandler` shape — strips the local `uid:`-prefix from a settings key before sending, since the server row is already scoped per-account by the bearer session). Wired into each store's main Save/Delete action only (`saveJob`/`deleteJob` in app.js, `saveInvoice`/`deleteInvoice` in invoices.js, `saveBooking`/`deleteBooking` in bookings.js — including every extra row a recurring-booking series creates, `saveItem`/`deleteItem` in portfolio.js, `saveArticle`/`deleteArticle` in research.js, `saveDocumentFromForm`/`deleteSavedDocument` in docgen.js, `applyDecision` in followups.js, `savePackage`/`saveService`/`deleteService`/`saveProgressEntry`/`deleteProgressEntry`/`saveSetting` in app.js) — deliberately NOT into every granular in-place mutation a record can go through afterward (a job's Pipeline stage move, a milestone/sub-task/timer edit, a persona-tracker field tweak on a client), matching the exact same scope boundary Phase 1 already drew for `clients` (its persona-tracker helpers built in this same session don't mirror in-place edits either). One real gap found and fixed along the way: `followups` records predate the cuid convention entirely (this store was never part of the original migration slice) — `applyDecision()` now mints one lazily on first save rather than requiring a separate backfill migration. `APP_VERSION`/`SW_VERSION` 0.9.18→0.9.19. Verified with a Node harness (19/19 — field-mapping correctness for every store, the 409-fallback-to-PUT path, and the strict no-op-when-disabled guarantee) plus a dedicated Playwright wiring test (8/8 — each call site actually fires at the right moment, guest mode makes zero `/api/*` calls even with a token present) plus the full existing regression suite re-run (14 suites, 0 console errors).
- 2026-07-13 · orchestrator · GARAGE MULTI-VEHICLE SUPPORT · First of the four persona-tracker-depth items below. Garage clients previously had one flat `vehiclePlate`/`vehicleMileage`/`nextServiceDate` per client (no second vehicle possible) plus a client-level `serviceHistory` log with no notion of which vehicle a service applied to. Replaced with a `client.vehicles[]` array (plate/mileage/nextServiceDate per vehicle, add/edit/delete each independently), and `serviceHistory` entries now carry an optional `vehicleId` — a service entry can be tied to a specific vehicle or left general/unassigned. Existing single-vehicle records migrate automatically and non-destructively the first time their tracker renders (`migrateGarageVehiclesIfNeeded()`): the old flat fields fold into `vehicles[0]`, then get deleted so they can't drift out of sync with the new array. New generic `saveClientListItemField()` helper (in-place edit of one field on one array item) joins the existing `addClientListItem`/`deleteClientListItem`/`listRowsHtml` trio already shared by every persona tracker's repeatable rows — reused directly for vehicles rather than one-off vehicle-specific functions. `APP_VERSION`/`SW_VERSION` 0.9.14→0.9.15. Verified with a dedicated Playwright script (13/13: legacy flat-field migration, independent multi-vehicle edits, vehicle-tied vs. unassigned service-history entries, deleting one vehicle leaves others and their service history intact, a brand-new garage client with zero vehicles renders cleanly) plus the full existing regression suite re-run, 0 console errors.
- 2026-07-13 · orchestrator · LAUNDRY ORDER HISTORY · Second of the four persona-tracker-depth items below. Laundry clients previously had one live `orderStatus` scalar (received/washing/drying/ready) with no order history at all — starting a new order silently overwrote whatever the last one's status was. Replaced with a `client.orders[]` array: each order has its own `date`/`kg`/`status`/`notes`. The tracker now shows a "Current order" editor for whichever order isn't yet `completed`, a "Mark picked up" button that closes it into read-only history (new `completeLaundryOrder()` — the one order-lifecycle action that needs an explicit re-render, since the editor has to disappear and the order has to reappear in the history list below it), and a "+ Start new order" button once there's no active order. Existing flat-`orderStatus` records migrate automatically and non-destructively the first time their tracker renders (`migrateLaundryOrdersIfNeeded()`, mirroring the same pattern `migrateGarageVehiclesIfNeeded()` just established), folding the old scalar into a single active order and dropping the flat field. `monthlyKgPlan`/`preferences` stay flat client-level settings (not order-specific, out of scope). `APP_VERSION`/`SW_VERSION` 0.9.15→0.9.16. Verified with a dedicated Playwright script (11/11: legacy scalar migration, independent order-field edits, marking picked up moves an order into history and clears the active editor, starting a new order after completion doesn't disturb the completed one, a brand-new laundry client with zero orders renders cleanly) plus the full existing regression suite re-run, 0 console errors.
- 2026-07-13 · orchestrator · INSURANCE MULTIPLE POLICIES · Third of the four persona-tracker-depth items below. Insurance clients previously had one flat `policyName`/`policyRenewalDate` per client — a client with health + motor + life policies could only track one of them, and adding a second silently overwrote the first. Replaced with a `client.policies[]` array: each policy has its own `name`/`renewalDate`, add/edit/delete independently, each rendering its own overdue/until-renewal countdown (previously computed once for the single flat date, now per-policy). `birthday`/`referredBy` stay flat client-level fields (not policy-specific, out of scope). Existing single-policy records migrate automatically and non-destructively the first time their tracker renders (`migrateInsurancePoliciesIfNeeded()`, same pattern as the garage/laundry migrations above): the old flat fields fold into `policies[0]`, then get deleted. `APP_VERSION`/`SW_VERSION` 0.9.16→0.9.17. Verified with a dedicated Playwright script (10/10: legacy flat-field migration, independent multi-policy edits, each policy renders its own countdown, deleting one policy leaves the other intact, a brand-new insurance client with zero policies renders cleanly) plus the full existing regression suite re-run, 0 console errors.
- 2026-07-13 · orchestrator · REAL ESTATE DEAL-STAGE PIPELINE · Last of the four persona-tracker-depth items, and the biggest structural change of the four. Real estate clients previously had one flat `searchBrief`/`offerStatus`/`estCommission` per client plus a single undifferentiated `viewings[]` log (date/property/verdict) with no notion of which property a viewing was for or where any given deal actually stood. Replaced with a real `client.deals[]` array: each deal is one property pursuit with its own `property`/`stage`/`commission`/`notes`, moving through a genuine 6-stage pipeline (`DEAL_STAGES = searching → viewing → offer → negotiating → closing → closed`) instead of a flat activity log — this was flagged at design time as needing "a real architecture shift," and is the one persona among the four that actually required it. Viewings are now nested *inside* each deal (`deal.viewings[]`, date/verdict — the `property` field moved up to the deal itself, since one deal already means one property) via two new deal-scoped helpers, `addDealViewing()`/`deleteDealViewing()`, mirroring `addClientListItem()`/`deleteClientListItem()`'s shape but reaching into a nested array rather than a top-level client field. `searchBrief` stays a flat client-level field (the client's general search criteria, not any one deal's). Existing records migrate automatically and non-destructively the first time their tracker renders (`migrateRealEstateDealsIfNeeded()`): `offerStatus` (always free text, e.g. "Offer submitted, awaiting reply" — never a stage enum, so it can't map onto `DEAL_STAGES` automatically) becomes the migrated deal's notes, `estCommission` becomes its commission, and every existing flat viewing carries over into that same deal's nested `viewings[]` as-is. `APP_VERSION`/`SW_VERSION` 0.9.17→0.9.18. Verified with a dedicated Playwright script (14/14: legacy migration incl. the nested-viewing carry-over, searchBrief staying flat, independent multi-deal edits, deal-scoped viewing add/delete not leaking across deals, deleting a deal removes its nested viewings with it, a brand-new client with zero deals renders cleanly) plus the full existing regression suite re-run, 0 console errors.
- 2026-07-13 · orchestrator · CLOUD-BACKUP FIRST-LOGIN MODAL · Resolves the Phase 1 follow-up below: the migration plan's "back up your existing data" first-login prompt was shipped in Phase 1 as an opt-in Settings row only (`renderCloudBackupSection()`/`enableCloudBackup()`), which a user had to notice and tap rather than being prompted. New `maybeShowCloudBackupModal()`, called once per `enterApp()` boot right after `switchScreen('home')`: for a non-guest account that hasn't enabled cloud backup yet and hasn't seen this prompt before (a per-account `localStorage` flag — not the server's `users.migrated_at`, since this account may not even be registered server-side yet), shows a modal offering "Enable cloud backup" (routes straight into the existing `enableCloudBackup()` flow) or "Not now"; either choice dismisses it permanently, and Settings still carries the same row for anyone who changes their mind later. LINE-only accounts (`hash === null` locally — Phase 1's register endpoint requires a password hash) are deliberately skipped rather than shown a button guaranteed to fail. `APP_VERSION`/`SW_VERSION` 0.9.13→0.9.14. Verified with a dedicated Playwright script (8/8: modal appears on a fresh registered account's first boot, "Not now" dismisses it for good, guest mode never sees it, a LINE-shaped account never sees it, "Enable" closes the modal immediately regardless of the (here, unreachable) backend's response) plus the full existing regression suite re-run, 0 console errors.
- 2026-07-13 · orchestrator · GUEST START-FRESH/RESUME PROMPT · Resolves the oldest "low" item below. Guest data lives under one fixed `uid: 'guest'` per device by design, so a second person picking "Continue as guest" on a shared device used to silently land in the first guest's data. `loginGuest()` now checks `guestDataExists()` (any row across `BACKUP_STORES` with `uid === 'guest'`) first — a brand-new guest on a fresh device skips straight through unchanged (no added friction for the common case); an existing guest sees a new `s-guest-choice` screen (`login.html`, styled like the existing `s-line-profile` screen) offering "Resume as `<existing guest name>`" or "Start fresh" (native `confirm()`-gated, since it's irreversible). `wipeGuestData()` deletes every guest-uid row across `BACKUP_STORES` plus `guest:`-prefixed settings plus guest-uid `usageEvents` rows (excluded from `BACKUP_STORES` since backups never carry it, but still this guest's data on-device), then drops the remembered `sidekick_guest_username` so the next guest gets a genuinely new label instead of the erased one's. `APP_VERSION`/`SW_VERSION` 0.9.12→0.9.13. Verified with a dedicated Playwright script (9/9: fresh-device skip-through, choice-screen appears with existing name, Cancel is a no-op, Resume preserves data, Start-fresh wipes it and mints a new name, dismissing the confirm leaves data untouched) plus the full existing regression suite re-run, 0 console errors.
- 2026-07-13 · orchestrator + user · LIVE NEON SMOKE TEST · Resolves the last item explicitly gated behind a real-database check-in in the original backend-migration plan — the Phase 1/2 API had only ever been verified against a hand-rolled in-memory fake of `@neondatabase/serverless`'s calling convention (no `DATABASE_URL` reachable from the build/agent sandbox). Ran a real end-to-end pass against the actual deployed production API (`https://sidekickz.vercel.app`) and its live Neon database: register/login/session, full `clients` CRUD (create/list/update/delete), row-isolation between two independent accounts (a second account sees zero of the first's data and gets a clean 404 — not a leak — trying to read/update/delete it), duplicate-username rejection, unauthenticated-request rejection, and cleanup deletion. **15/15 passed.** Root cause of several earlier failed attempts, for the record: **two separate Neon databases were connected to the same Vercel project** (`neon-emerald-field` and `neon-cordovan-marble`), and `sql/schema-core.sql` had only been run against the one `DATABASE_URL` *doesn't* actually point to — every DB-touching endpoint returned a generic 502 whose real server-side cause (`relation "users" does not exist`, visible only in Vercel's own Runtime Logs, deliberately never sent to the client) took several rounds to track down since a Vercel-dashboard "which branch is this" click-through kept landing on the wrong one. Resolved for good by running the schema directly against the literal `DATABASE_URL` connection string via `psql "$DATABASE_URL" -f sql/schema-core.sql` — removes any ambiguity about which project/branch/database is actually targeted, since it's the exact string the app itself uses. (The agent's own sandbox can't reach `sidekickz.vercel.app`, `console.neon.tech`, or `api.neon.tech` at all — blocked by this environment's own egress allowlist — so the test itself ran from a temporary, immediately-removed GitHub Actions `workflow_dispatch` job on a GitHub-hosted runner instead, which has normal internet access.)

### Known polish backlog (as of retirement — resolved status noted)
- ~~Not yet done: pushing the built app to the live Hostinger account~~ — **dropped 2026-07-11**, GitHub Pages is canonical.
- ~~Not yet done: the actual Vercel project for M-AI~~ — **moot, M-AI dropped entirely 2026-07-11.**
- ~~Freelanz hosted from two possible targets (Hostinger/Vercel), decide which is canonical~~ — **resolved 2026-07-11**: GitHub Pages, neither of the other two.
- ~~M5's "Premium" content gating is UI-only~~ — **superseded 2026-07-11**: real gating now being built for the gym-squad/Sidekick codebase (PromptPay QR + manual verify), see gym-squad Milestones.
- M5's content ("research" articles) is per-uid/local-only — this design question (server-fetched shared content vs. local-only with a payment check) still applies to whatever real gating gets built next.
- ~~iOS PNG apple-touch-icon~~ — fixed.
- ~~Shoots/jobs list shows client name twice~~ — verified not a live bug.
- ~~Guest data persists on shared device by design; consider an explicit "start fresh" vs "resume" prompt later~~ — **resolved 2026-07-13**, see the GUEST START-FRESH/RESUME PROMPT delta entry below.
- ~~`DB_VER` stayed at 2 when M2 added stores~~ — fixed in M3.
- ~~Follow-up queue items have no "un-dismiss"~~ — fixed.
- ~~bookings.js only compares within the same calendar day~~ — fixed.
- bookings.js/portfolio.js delete actions swallow a failed `dbDel` and toast success unconditionally — deliberately left alone (theoretical, `dbDel` never actually rejects today). (low, still open)

**Reconciled 2026-07-13** (checked against the actual code rather than assumed; see the backend-migration session's research pass):
- ~~Invoice/milestone/unbilled-time auto-linking to the created invoice~~ — **resolved 2026-07-13**, see the INVOICE/MILESTONE/UNBILLED-TIME AUTO-LINKING delta entry above.
- ~~i18n gaps (STAGE_META never routed through `t()`; bookings.js/invoices.js hardcoded English despite calling `t()` elsewhere)~~ — **resolved 2026-07-13**, see the two i18n delta entries above.
- ~~Persona-tracker depth (garage multi-vehicle / laundry recurring order history / insurance multiple policies / real estate deal timeline)~~ — **resolved 2026-07-13, all four done**, see the GARAGE MULTI-VEHICLE SUPPORT, LAUNDRY ORDER HISTORY, INSURANCE MULTIPLE POLICIES, and REAL ESTATE DEAL-STAGE PIPELINE delta entries below.

**Added 2026-07-13** (backend-migration Phase 1 follow-ups — see the BACKEND MIGRATION PHASE 1 BUILD delta entry above for full context):
- ~~Live verification against a real Neon connection~~ — **resolved 2026-07-13**, see the LIVE NEON SMOKE TEST delta entry below: full 15/15 pass against the real deployed production API and database.
- ~~The one-time "back up your existing local data" first-login prompt described in the migration plan doesn't exist yet~~ — **resolved 2026-07-13**, see the CLOUD-BACKUP FIRST-LOGIN MODAL delta entry below.
- Backend migration Phase 2 (schema + API) and ~~Phase 2b (`app/dataClient.js` mirror wiring for the 11 new stores)~~ are **both done** — see the BACKEND MIGRATION PHASE 2 and PHASE 2b delta entries. Broad rollout of the local→server migration-upload flow beyond `clients` (the other 11 stores' data never gets bulk-uploaded for an existing account the way `clients` does via `enableCloudBackup()`), cutting reads/writes over to the API for good (still mirror-only, IndexedDB stays authoritative), removing the now-dead local-storage code paths, and optional real offline sync via the dormant `meta`/`outbox` stores (only if a concrete offline-writes need ever materializes — not assumed) all remain roadmap only, not built. (large, still open, intentionally not attempted yet)

## Subscription Pricing (Basic / Pro / Team)

- 2026-07-14 · orchestrator · ASSESS + DIRECTION — Subscription pricing tiers (Basic/Pro/Team) · Per user request to assess a 3-tier discriminating pricing model (**Basic** w/ 15-day free trial, **Pro**, **Team**) and scope the feature roadmap needed to support it. Researched current state first, no assumptions: the app's only existing monetization is `research.js`'s one-time ฿99 self-attested "Premium" unlock (PromptPay QR + honor-system "I've paid" button, `settings.premiumUnlocked` — a per-device local flag, not a server-verified entitlement). There is no `plan`/`tier`/`subscription_status`/`trial_*` column anywhere in `sql/schema-core.sql`'s `users` table, no payment-gateway SDK in `package.json` (only dependency is `@neondatabase/serverless`), and — the critical finding — **no team/organization/multi-user data model at all**: every one of the 12 resource tables is scoped to exactly one `user_cuid`, `lib/crudHandler.js` hard-scopes every query to the authenticated caller's own row, and there is no membership/role/invite concept anywhere in the codebase. A **Team tier is therefore not a pricing/UI decision today — it needs new infrastructure**, not a config toggle.

  **Recommended tiers** (directional, anchored against FlowAccount ~฿165+/mo as the closest Thai comparable and HoneyBook/Bonsai $9–129/mo as global freelancer-CRM comparables — not validated against Sidekick's own costs/margins/target market, flagged as needing real testing before committing):
  - **Basic — ฿149/mo, 15-day free trial**: core toolkit (Workflow/Pipeline, Clients capped ~15, Calendar, Invoicing + PromptPay, Quote/Receipt/Contract docs, Tax calculator), local-device storage only (no cross-device cloud sync), no LINE booking, no Research premium content.
  - **Pro — ฿349/mo**: unlimited clients, cross-device cloud sync (via the already-in-progress backend migration), recurring bookings, LINE Login + LINE OA self-service booking page, Research premium bundled in (replacing the old one-time unlock), unlimited session packages/progress logs, invoice/quote branding (logo — net-new feature), priority notifications + support.
  - **Team — ฿349/seat/mo, minimum 2 seats**: everything in Pro, plus multiple staff seats sharing one client base/pipeline/calendar, roles (Owner/Admin/Staff), centralized billing, team-wide reporting.

  **Roadmap to make this real, phased:**
  - **Phase 0 — foundation, blocks all three tiers.** Add `users.plan` / `subscription_status` / `trial_started_at` / `trial_ends_at` / `current_period_end` columns; build a real entitlement-check layer (client + server) replacing the single hardcoded `premiumUnlocked` flag; **billing-gateway decision flagged back to the user** — the existing honor-system PromptPay unlock doesn't scale to recurring billing (no auto-charge, no dunning, trusts the user to come back and re-pay monthly), recommend a real Thai-capable processor (Opn Payments/2C2P, or Stripe) for Pro/Team, which reverses the 2026-07-11 "stay backend-free, no Stripe/Omise" decision now that the backend migration is already underway — needs explicit user sign-off before building, not assumed; trial countdown + auto-downgrade-on-expiry + trial-ending reminder (reusing the existing `computeNotificationConditions()` notification engine); a Settings ▸ Subscription screen (plan, trial days left, upgrade, receipts).
  - **Phase 1 — Basic/Pro gating, moderate lift.** Client-count cap enforcement + upgrade prompts; gate LINE booking / recurring bookings / Research premium behind Pro; move the Premium entitlement server-side (account-based, not per-device, so it's consistent across devices); build invoice/quote/receipt logo branding (net-new, doesn't exist today); finish gating cloud sync as the Basic/Pro dividing line.
  - **Phase 2 — Team tier, major/new architecture, not started this pass.** New `organizations`/`org_members` tables with roles; `org_cuid`-scoping added across every resource table plus a `lib/crudHandler.js` rework to support it; invite flow; seat-based billing; staff-assignment fields on jobs/bookings; shared-calendar conflict-checking across staff; team-wide reporting (extending the current dev-only local Insights concept into a real cross-user view). This also forces every team member off pure local-first into backend-mode — a real architectural shift, called out explicitly rather than folded silently into "add a Team plan." **Built 2026-07-15 — see the "Team tier (organizations / seats / roles)" section below.** Built with the lighter shared-single-identity model instead of the `org_cuid`-scoping sketched here (a genuine architectural fork, checked with the user first); invite flow, seat-based billing, and roles all shipped as scoped. Staff-assignment fields on jobs/bookings, shared-calendar conflict-checking, and team-wide reporting were **not** part of that pass and remain open follow-ups.

  Full assessment (with market-comparable sourcing) delivered as a chat response; recorded here per the user's request to track it alongside the rest of the product-decision history, matching this file's own convention. **Assessment only — nothing built this entry.** User then asked to start building Phase 0/1, with an explicit instruction to verify the plan with them before starting the engineering-loop.

- 2026-07-14 · orchestrator · CLARIFY + RESEARCH — billing-gateway comparison · Per the Phase 0 checkpoint above, presented two AskUserQuestion decisions before touching code: (1) billing method — user chose "let me decide after seeing a fuller comparison" rather than picking blind; (2) trial-expiry behavior — user chose "downgrade to locked/read-only" (existing data stays visible, new-record creation blocked until subscribed). Researched Opn Payments (Omise), 2C2P, and Stripe head-to-head on recurring-subscription support, PromptPay support, Thai onboarding requirements, Node SDK/webhook fit for Vercel serverless, and pricing. Finding that applies regardless of which is picked: **none of the three can auto-charge PromptPay on a recurring schedule** — PromptPay has no stored-credential/mandate mechanism, so true unattended monthly billing requires card-on-file on any of them; PromptPay can only ever be a one-time/manual payment leg. Recommendation: **Opn Payments (Omise)** — self-serve sole-proprietor onboarding, official actively-maintained Node SDK with clean HMAC webhook verification, transparent public 2026 pricing (cards 3.65%, PromptPay 1.65%, no monthly/setup fees), Thai-bank settlement. Stripe is a credible #2 (near-identical pricing, more polished Billing UX, slightly thinner Thailand track record); 2C2P ruled out (no official Node SDK, unpublished pricing, enterprise/sales-led onboarding — poor fit for this project's size). Billing-gateway pick still awaiting final user sign-off before Phase 0's payment/webhook pieces are built.

- 2026-07-14 · orchestrator · DIRECTION — billing gateway: Stripe · User picked **Stripe** over Opn Payments/2C2P (see the comparison above). Concrete Stripe-shaped Phase 0 plan drafted (schema additions, `lib/stripe.js`, `lib/entitlements.js`, `api/billing-checkout.js`/`api/stripe-webhook.js`/`api/billing-portal.js`, Settings ▸ Subscription screen) and presented back to the user for the standing "verify before the engineering-loop starts" checkpoint, along with two remaining sub-decisions (card-required-at-trial-start vs. not; whether pre-existing accounts get grandfathered or a fresh trial). User picked **no card upfront** (trial tracked as our own timestamp, Stripe only enters the picture at checkout) and **grandfather existing accounts as already active, no trial clock** — both the recommended options.

- 2026-07-14 · orchestrator · BUILD — Phase 0: real Stripe subscription billing (branch `claude/subscription-pricing-features-t8131t`) · Implements the plan above.
  1. **Schema** (`sql/schema-core.sql`) — `users` gains `plan` ('basic'|'pro'|'team'), `subscription_status` ('trialing'|'active'|'past_due'|'canceled'), `trial_ends_at`, `stripe_customer_id`, `stripe_subscription_id`, `current_period_end`. Both a fresh `create table` definition (new deployments) and an idempotent `alter table ... add column if not exists` block (the already-live production table) are included — no migration runner exists in this project, both are applied by hand like everything else in this file. `subscription_status` defaults to `'active'` with no trial clock specifically so the ALTER grandfathers every pre-existing row per the user's decision; `api/auth-register.js` explicitly overrides both fields to start a real 15-day trial for every *new* registration instead of relying on that default.
  2. **`lib/entitlements.js`** (new) — the single source of truth for "what can this account do right now," reading straight off a `users` row: `isLocked()` (computed live from `subscription_status`/`trial_ends_at`, no background job needed — the only state that needs a clock check is `'trialing'`, since this app never creates a card-backed Stripe subscription at signup), `hasFeature(user, key)` against a `PLAN_FEATURES` map (`clientCap`/`cloudSync`/`lineBooking`/`recurringBookings`/`researchPremium`/`docBranding`, each keyed off plan tier — a locked account gets none of them regardless of tier), `canWrite()`, `trialDaysLeft()`. Deliberately pure/dependency-free so it's unit-testable without a live Neon or Stripe connection.
  3. **`lib/stripe.js`** (new) — shared Stripe client (`STRIPE_SECRET_KEY`), plus `verifyStripeWebhook()` using Stripe's `createSubtleCryptoProvider()` (Web-Crypto-backed, not Node's `crypto` module) so webhook signature verification works from an edge-runtime function, same reasoning `lib/line.js`/`lib/lineLogin.js` already established by hand for their own HMAC checks.
  4. **`lib/crudHandler.js`** — every write (POST/PUT/DELETE, across all 12 resource endpoints that share this factory) now does one extra lookup (`select plan, subscription_status, trial_ends_at from users where cuid = $1`) and returns `402 {code:'locked'}` if `canWrite()` is false. GET is untouched — reads stay open on a locked account, matching the "downgrade to read-only, not full lockout" decision. Deliberately not folded into the 30-day session token, since a Stripe webhook can flip `subscription_status` between requests and a long-lived token would otherwise go stale.
  5. **`api/billing-checkout.js`** (new) — creates/reuses a Stripe Customer for the caller, then a Stripe-hosted Checkout Session (`mode:'subscription'`) for `'basic'`|`'pro'` (`'team'` rejected — seat billing is Phase 2, not built), reading Price IDs from `STRIPE_PRICE_BASIC`/`STRIPE_PRICE_PRO` env vars that must be created by hand in the Stripe Dashboard (documented in `.env.example`, same "set up once by hand" convention as `sql/schema-core.sql` and the LINE channel setup). `success_url`/`cancel_url` are built from the request's `Origin` header validated against the existing CORS allowlist (`lib/cors.js`'s new `resolveOrigin()` export) rather than trusting a client-supplied redirect target.
  6. **`api/stripe-webhook.js`** (new, edge runtime) — same raw-body-signature-verification shape as `api/line-webhook.js`. Listens only to `customer.subscription.created/updated/deleted` (Checkout in subscription mode always fires `.created` right after, and Stripe's own `subscription.status` already reflects active/past_due/canceled/trialing accurately, so one mapping function off Stripe's canonical state beats juggling several overlapping event types). Matches rows by `stripe_customer_id` (robust to a subscription later hand-edited in the Stripe dashboard, which wouldn't carry this app's metadata) and only overwrites `plan` when the event actually carries `metadata.plan` (set at Checkout-creation time), so a dashboard-initiated change with no metadata never silently nulls out the plan column.
  7. **`api/billing-portal.js`** (new) — hands back a Stripe-hosted Billing Portal URL (view invoices/update card/cancel) — no custom cancel-subscription UI built, same defer-to-Stripe's-hosted-flow reasoning as checkout.
  8. **Client** — `app/dataClient.js` gains `SidekickBackend.billingCheckout(plan)`/`billingPortal()`; `api/auth-session.js` ("whoami") now also returns `plan`/`subscriptionStatus`/`trialEndsAt`/`currentPeriodEnd`/`trialDaysLeft`/`locked`/`hasStripeCustomer`, computed server-side via `lib/entitlements.js` so the client stays dumb (no duplicated lock-state logic). New Settings ▸ **Subscription** section (`renderSubscriptionSection()`, `#subscription-body`) shows plan + status (trial countdown / active / payment-failed / canceled), a red locked banner + copy when `locked`, "Upgrade to Pro"/"Subscribe to Basic" buttons (via `startSubscriptionCheckout()`) and "Manage billing" (via `openBillingPortal()`) — both just redirect to the Stripe-hosted URL the corresponding endpoint returns. Renders nothing beyond a one-line hint for guest mode or an account that hasn't enabled cloud backup yet (see `renderCloudBackupSection()`) — there is no backend `users` row to have a subscription against until that happens, since `enableCloudBackup()` is what actually creates one for a pre-existing local account (only fresh registrations start a trial automatically today).
  9. **`package.json`** gained its second dependency, `stripe`. **`.env.example`** documents the three new required vars (`STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_BASIC`, `STRIPE_PRICE_PRO`).

  **Explicitly out of scope this pass, not silently dropped:** the rest of Phase 1's feature-specific gates (gating LINE booking / recurring bookings / Research-premium-as-a-real-entitlement / invoice-quote-receipt logo branding behind Pro, and actually *enforcing* the `clientCap` value `lib/entitlements.js` already defines — nothing currently stops a Basic account from adding a 16th client) are none of them wired up yet; `research.js`'s original one-time ฿99 manual PromptPay unlock is untouched and still the only gate on Research content. All of Phase 2 (Team seats/orgs) remains unbuilt, as planned. Also unverified: end-to-end behavior against a live Neon connection and a real Stripe test-mode account (no live credentials in this session, same standing limitation every prior backend-migration entry in this file has flagged) — `checkout.session`/webhook DB-update logic is exercised only by the unit/signature tests below, not a real Stripe event delivery.

  `APP_VERSION`/`SW_VERSION` bumped 0.9.20 → 0.9.21. Verified: a pure-function Node harness for `lib/entitlements.js` (21/21 — lock-state transitions including the grandfathered-`trial_ends_at`-null edge case, `hasFeature` correctly zeroing out on a locked account regardless of plan tier, `clientCapFor`/`trialDaysLeft` rounding/floor behavior); a Node harness for `lib/stripe.js`'s webhook verification using Stripe's own `generateTestHeaderString()` test helper against a real signed payload (4/4 — valid signature accepted, wrong secret/tampered payload/malformed header all correctly rejected); a Playwright script driving the real Settings ▸ Subscription UI against three stubbed `SidekickBackend.session()` responses — trialing/active-pro/locked (10/10 — correct status text, locked banner only when locked, upgrade/manage-billing button visibility per state, the no-account hint) — plus the two onboarding suites from the previous entry re-run unchanged (16/16), 0 console errors across all three.

- 2026-07-14 · orchestrator · BUILD — Phase 1: wire the remaining feature gates (client cap, recurring bookings, Research premium, document branding) · Continues directly off Phase 0, per user instruction to keep going down the list flagged as deliberately out of scope there.
  1. **Client-side entitlement cache** (`app/app.js`) — `refreshEntitlements()`/`__entitlements`, populated at boot (`finishAppBoot()`, fire-and-forget) and refreshed whenever Settings ▸ Subscription renders. `planHasFeature(key)`/`planClientCap()` read it synchronously so a "+ Add client"/booking-save tap never has to await a network round trip — a few minutes of staleness on a plan change is an accepted trade, same bar this app already applies to its backend mirror writes. `null`/no tracked account (guest, or registered but never enabled cloud backup) reads as **unrestricted** on every gate below, matching Phase 0's own framing: the paywall only ever applies once an account opts into the backend/subscription system at all. `api/auth-session.js` now also returns a computed `features` map (iterating `lib/entitlements.js`'s new `FEATURE_KEYS` export) and `clientCap` (`null` = unlimited) — the client never duplicates the plan→feature mapping, it only reads server-computed booleans.
  2. **Client cap** — `saveCustomer()` blocks creating a *new* client (never blocks editing an existing one, so nobody's existing data becomes unreachable just from going over a cap after the fact) once `customers.length >= planClientCap()`, with a toast pointing at Settings ▸ Subscription.
  3. **Recurring bookings** — `bookings.js`'s "Repeat" form section renders a disabled, options-stripped `<select>` plus an upgrade note when `!planHasFeature('recurringBookings')`, instead of the real weekly/biweekly picker. `saveBooking()` also independently re-checks the same flag before reading `#bk-repeat`'s value (defense in depth — a stale render or DOM tampering still can't produce extra rows).
  4. **Research premium** — new `researchPremiumUnlocked()` in `app/research.js` is true via *either* path: the original per-device honor-system unlock (`settings.premiumUnlocked`, completely unchanged — still the only option for guest/local-only accounts with nothing to check a subscription against) OR `planHasFeature('researchPremium')`. Deliberately additive, not a replacement: an account that already paid the one-time ฿99 keeps that unlock regardless of plan; a Pro subscriber gets Premium content automatically with no separate purchase. Both the article-list "Subscribe" banner and the per-article lock check now read this instead of the raw setting.
  5. **Document branding** — genuinely new feature (didn't exist in any form before this), not just a gate on existing capability. Settings ▸ Business info gained a `#seller-logo-body` section (`renderSellerLogoSection()`) — reuses `portfolio.js`'s exact `FileReader.readAsDataURL()`/2MB-cap image-upload pattern, storing one logo per account as `settings.sellerLogoDataUrl`. Shows the real upload+preview UI only when `planHasFeature('docBranding')`; otherwise a locked note, no upload control at all. `sellerLogoDataUrl()` (the read side, used by `docgen.js`'s new `sellerLogoHtml()` and `invoices.js`'s `printInvoice()`) independently re-checks the same gate *at render time*, not just at upload time — a downgrade quietly stops the logo from appearing on new documents without deleting the stored image, so re-upgrading brings it straight back rather than requiring a re-upload.
  6. **LINE booking — assessed, explicitly not gated, and why.** The original Phase 1 roadmap bundled "LINE booking" in as a Pro perk, but the actual LINE integration splits into two unrelated things: LINE Login (`api/line-login-*.js`, a general sign-in method available to every account — gating *how someone logs in* behind a subscription they can't start until they're logged in makes no sense, so this was never a candidate) and the LINE OA self-service booking pilot (`api/line-webhook.js`/`api/booking-*.js`), which is **explicitly single-tenant** per its own `sql/schema.sql` header comment — one hardcoded Channel via env vars, for one specific pilot freelancer, with no per-account concept at all. There is structurally nothing to gate per-plan yet; building a real per-user "connect your own LINE OA" flow was already noted as separate, larger, unbuilt work back when the pilot shipped, and remains so. Left untouched rather than forcing an incoherent gate onto a feature that isn't multi-tenant.
  `APP_VERSION`/`SW_VERSION` bumped 0.9.21 → 0.9.22. Verified with a new Playwright script exercising all four real gates end-to-end against a registered (non-guest) account with stubbed `SidekickBackend` responses (13/13 — Basic-at-cap blocks a 2nd new client but still allows editing the 1st, upgrading to Pro immediately allows it; the repeat `<select>` is disabled with only 1 option on Basic vs. all 3 on Pro; the Research "Premium unlocked" banner appears on Pro with zero local `premiumUnlocked` setting; the logo upload control is absent on Basic and present on Pro) plus all three prior suites re-run unchanged (30/30: onboarding ×2, Subscription UI), 0 console errors throughout.

- 2026-07-14 · orchestrator · BUILD — light theme + Thai language defaults, first-run persona onboarding (branch `claude/subscription-pricing-features-t8131t`) · Three related changes from one user request, landed ahead of the subscription-tier build (independent of the billing-gateway decision above, which stays open):
  1. **Light theme by default** — reverses the 2026 rebrand's "dark by default" decision (`add-dark-mode` milestone) on explicit instruction. `applyTheme()`'s fallback changed from `'dark'` to `'light'`; the now-obsolete one-time `sidekick_dark_default_migrated_v1` migration flag (which used to force-flip a pre-rebrand `'light'` value to `'dark'`) removed entirely, along with its mirrored logic in both `index.html`'s and `login.html`'s pre-paint inline scripts. `theme-color` meta and `manifest.json`'s `background_color`/`theme_color` updated from the dark ink `#121715` to the light `#F7F6F2` to match. An explicit user choice (e.g. picking Dark in Settings) still persists and is respected normally — only the no-choice-made fallback changed.
  2. **Thai language by default** — `curLang()`'s trailing fallback changed `'en'` → `'th'`; found and fixed a second, easy-to-miss hardcoded `'en'` default in the top-level `let settings = {lang:'en', ...}` state declaration (line ~443) that was overriding `curLang()`'s fallback chain entirely for any account with no stored `lang` setting — a first Playwright run caught this (tagline/login-tab still rendered English) before it shipped. `manifest.json`'s `lang` field also updated `'en'` → `'th'`. An explicit language choice via Settings still persists as before.
  3. **First-run persona onboarding, replacing the silent `'trainer'` default** — `enterApp()`'s old `if (!settings.businessType) await saveSetting('businessType', 'trainer')` (silent, no prompt) replaced with a blocking, non-dismissible modal (`#modal-persona-onboard`, index.html) shown only when `businessType` has never been set — existing installs already have it set from the earlier silent-default migration, so nobody already using the app sees this appear retroactively. Offers the 5 existing business types (Personal trainer / Real estate agent / Laundry service / Insurance agent / Car garage) plus a new 6th **"Other"** (`custom`) type for anything that doesn't fit — added to `BUSINESS_TYPES`/`PACKAGE_UNIT_DEFAULTS`/the Settings ▸ Business type dropdown, with empty `seedServices` (no preset catalog) and its client-detail persona-tracker section hidden entirely (`PERSONA_TRACKER_TITLES` has no `custom` entry) rather than rendered empty. `enterApp()` split at the businessType check into `finishAppBoot()` (the member-ID migration, `seedServicesIfEmpty()`, `switchScreen('home')`, cloud-backup modal, notification interval — everything that needs a known business type) so the picker's `choosePersonaOnboard(v)` handler can resume boot after a choice is made; auto-seeding existing services-per-persona (`seedServicesIfEmpty()`) already existed and needed no changes — it now just fires after an explicit choice instead of after a silent default.
  `APP_VERSION`/`SW_VERSION` bumped 0.9.19 → 0.9.20 (lockstep across app.js/sw.js/index.html/login.html). Verified with two dedicated Playwright scripts against a local static server (16/16: light theme by default, Thai text renders by default on the login screen, onboarding modal blocks Home until a persona is chosen, all 6 choices present, choosing "Car garage" auto-seeds its services and reflects in Settings, the modal never reappears on reload once set, choosing "Other" hides the persona-tracker section on a client, explicit Dark theme choice persists across reload), 0 console errors. No committed regression-suite scripts exist in this checkout to re-run (prior sessions' Playwright scripts were run ad hoc and not checked in), so only the two scripts written for this change were run — broader regression is a known gap for future sessions to close by committing a real test suite.

## Generic LINE Business Connection (retiring the single-tenant pilot)

- 2026-07-14 · orchestrator · BUILD — every account can now connect its own LINE Official Account (branch `claude/subscription-pricing-features-t8131t`) · Per direct user instruction, following up on the LINE self-service-booking pilot's own long-standing "generic per-freelancer 'connect your own LINE OA' flow — deliberately out of scope, noted but not built" note (see the Phase 1 entry above, item 6). Turns the single hardcoded-Channel pilot into a real per-account, Pro-gated feature.
  1. **Found and fixed a real pre-existing bug while consolidating the schema**: `sql/schema.sql` (the pilot's own file) and `sql/schema-core.sql` both defined a bare `create table if not exists services (...)` — a genuine SQL table-name collision `schema-core.sql`'s own header comment claimed (in the past tense, as if already handled) was avoided, when it never actually was. Whichever file ran first against a given database would silently "win"; the loser's `create table if not exists` would just no-op. Since the pilot schema was still only "tracked, not started" (no Vercel deploy access ever available to run it), nothing live depended on its shape — resolved by retiring `sql/schema.sql` entirely (deleted) and folding its tables into `sql/schema-core.sql` as real multi-tenant tables, reusing schema-core's own already-existing, already-live `services` table (the account's real Service catalog — rate/name) instead of maintaining a second, narrower one just for booking.
  2. **Schema** (`sql/schema-core.sql`, new "LINE INTEGRATION" section) — `line_channels` (one connected Messaging API channel per account, 1:1: `channel_id`, `channel_secret`, `bot_user_id` resolved once at connect time via `GET /v2/bot/info`, optional self-reported `freelancer_line_user_id` for push alerts), `availability_slots` and `bookings` both gained `user_cuid`, and `bookings.service_cuid` now points at the shared `services` table instead of a separate one. `idx_line_channels_bot_user` is what makes routing an inbound webhook to the right account an indexed lookup, not a table scan.
  3. **`lib/line.js`** — every function already took credentials as parameters (no rewrite needed there), but `getLineAccessToken()`'s cache was one unkeyed module-level variable, silently wrong the moment a second account connected its own channel — changed to a `Map` keyed by `channelId`. New `getLineBotUserId(accessToken)` (calls LINE's `GET /v2/bot/info`) is what makes routing possible at all.
  4. **`api/line-webhook.js` rewritten** — ONE shared webhook URL now serves every connected account. LINE's webhook payload always carries a `destination` field (the receiving bot's own LINE userId); matched against `line_channels.bot_user_id` to find the account *before* verifying the signature — safe to do in that order because destination only ever selects *whose* secret to check the signature against, never trusted on its own (a forged destination still fails the signature check right after, since the attacker doesn't have that account's real `channel_secret`). No match → 200 ack, do nothing (never a 401/404 that would tell a prober which destinations are connected).
  5. **`api/booking-availability.js`/`api/booking-request.js` rewritten** — both now require `?u=<user_cuid>` / `userCuid` in the body and scope every query by it; `booking-request.js` looks up that tenant's own `line_channels` row for the optional new-booking push alert instead of reading `LINE_FREELANCER_USER_ID` from env. Both gained real CORS (`lib/cors.js`) — actually required, not just future-proofing: the public booking page is served from GitHub Pages while the API lives on Vercel, a genuinely cross-origin call.
  6. **`api/line-channel-connect.js`** (new) — authenticated GET (status)/POST (connect — validates the pasted Channel ID/secret against LINE itself before storing anything, via a real token exchange + bot-info call, so a typo'd secret fails loudly at connect time instead of silently inside a future webhook delivery nobody's watching)/DELETE (disconnect). **`api/booking-slots.js`** (new) — authenticated CRUD for an account's own offered time windows, separate from the public read-only availability endpoint.
  7. **Settings ▸ LINE booking** (new, `app/index.html`/`app/app.js`) — gated behind `planHasFeature('lineBooking')` (Pro+) same pattern as document branding; shows a connect form (Channel ID/secret/optional alert user ID) when not connected, or the shared webhook URL + this account's own booking-page URL (both copyable) + Disconnect once connected; a slot list below it (add/delete simple start/end time windows) — no recurring-availability-rules engine, deliberately kept to the minimum needed for the feature to be end-to-end usable at all, not a full Calendly-style scheduler.
  8. **`app/book.html`** (new) — the actual public, anonymous booking page that never existed for the pilot (it had the backend endpoints but nothing calling them). Reads `?u=`, fetches services+slots, lets a stranger pick a service/slot/name and submit, shows a confirmation or a clean "that slot's gone, pick another" on a 409. Deliberately minimal — functional, not a full design pass — and deliberately does NOT integrate LINE's LIFF in-chat browser SDK (would auto-fill `clientLineUserId` for someone arriving via the LINE rich menu specifically) — the page works as a plain shareable link either way, `clientLineUserId` was already nullable/optional in the original pilot design.
  9. **`.env.example`** — the four pilot-only vars (`LINE_CHANNEL_ID`/`LINE_CHANNEL_SECRET`/`LINE_BOOKING_URL`/`LINE_FREELANCER_USER_ID`) are gone; documents the new optional `API_ORIGIN`/`APP_ORIGIN` overrides instead (both already default sensibly).

  **Deliberately out of scope, flagged not dropped**: no recurring/repeating-availability rules (each slot is added one at a time); no LIFF integration; one connected LINE channel per account only (no multi-channel); booking confirmation is still manual (a freelancer marks a `requested` booking `confirmed`/`declined` — that management UI doesn't exist yet either, tracked as the natural next slice); end-to-end behavior against a live Neon connection and a real LINE channel is unverified (no live credentials in this session, same standing limitation flagged on every backend piece this session).

  `APP_VERSION`/`SW_VERSION` bumped 0.9.22 → 0.9.23. Verified: a Node harness proving `lib/line.js`'s token cache is genuinely per-channel (two distinct mocked channels never cross-contaminate a cached token or resolve to the same bot userId) plus signature verification still correctly rejects a valid signature checked against the *wrong* tenant's secret — the exact case that makes destination-based routing safe (11/11); a new Playwright suite driving the real Settings ▸ LINE booking UI end-to-end against a stubbed backend — locked on Basic, connect form → connected status view with both URLs shown → slot add/delete (including the end-before-start client-side rejection) → disconnect (13/13); a new Playwright suite driving the real `app/book.html` against intercepted network responses — missing `?u=` handled cleanly, services/slots render, empty-selection submit blocked with guidance, full happy-path booking to confirmation, and a second visitor correctly no longer seeing an already-held slot on reload (8/8) — plus all five prior suites and both earlier Node harnesses re-run unchanged (66/66 + 25/25), 0 console errors throughout.

## Demo data for sales pitches

- 2026-07-15 · orchestrator · BUILD — "Try a demo" mode with realistic per-persona seed data (branch `claude/subscription-pricing-features-t8131t`) · Per direct user request for a way to demo the app to a prospect without an empty state. Guest-mode-based by design — no server/account dependency, matching this session's own no-live-database-access limitation, and reuses guest mode's existing "Start fresh" reset story rather than needing a new one.
  1. **`app/login.html`** gained a third entry-point button, "Try a demo", alongside Log in/Continue as guest. `startDemo()` (`app/app.js`) force-wipes any existing guest data on the device first (reusing the existing `guestDataExists()`/`wipeGuestData()` pair, with a confirm prompt so it never silently destroys a prior demo/guest session someone meant to keep), sets a `sessionStorage` flag, then enters guest mode as normal.
  2. **`choosePersonaOnboard()`** (the first-run persona picker every fresh account already goes through) checks that flag after `finishAppBoot()` — picking a persona both sets `businessType` as usual AND calls the new `seedDemoData(persona)`, so a demo is just the existing onboarding flow with one extra step, not a parallel code path to keep in sync.
  3. **`seedDemoData()`** writes directly to IndexedDB (`dbAdd`/`dbPut`) rather than driving the DOM-coupled `saveCustomer()`/`saveJob()`/etc. form-submit functions, matching each store's exact real record shape (confirmed against the actual save functions, not guessed) — `clients` (with persona-specific tracker sub-arrays: `vehicles[]`+`serviceHistory[]` for garage, `orders[]` for laundry, `policies[]` for insurance, `deals[]`+nested `viewings[]` for real estate, `mealPlan[]` for trainer), `jobs` (pipeline engagements spread across all 6 stages so the board reads as a real, active pipeline rather than one column), `invoices` (draft/sent/paid statuses), `bookings`, and for trainer specifically `packages`+`progressLogs`. Every date is authored as a day-offset from "today" and resolved at seed time (`relDate()`), not a literal string, so a demo run months from now still looks current instead of stale. Reuses `finishAppBoot()`'s own `seedServicesIfEmpty()` output (services are seeded first) so demo jobs/invoices reference the persona's real, already-existing service catalog by name rather than inventing a parallel one.
  4. **`DEMO_PERSONA_DATA`** — hand-authored realistic content for the 5 named personas (trainer/realestate/laundry/insurance/garage — 5 clients, 6 pipeline jobs, 3 invoices, 2–3 bookings each, plus persona-specific extras), reflecting plausible Thai small-business scenarios (e.g. a garage's fleet client with two vehicles on different service-due dates, a real-estate deal moving through negotiation with a logged viewing history, a laundry order mid-wash). `custom` deliberately has no entry — there's no generic "custom" business content to fabricate, same reasoning `BUSINESS_TYPES.custom` already has an empty `seedServices` list.
  5. A demo-seeded toast ("this is a preview account, nothing here is real") confirms the load. Resetting for the next pitch is just clicking "Try a demo" again — the wipe-confirm it already shows when guest data exists is the one reset mechanism, no separate "clear demo" control was added.

  **Deliberately out of scope**: persistent/server-side demo accounts that would survive across devices without re-seeding — not buildable without live database access this session doesn't have; flagged as a natural follow-up once deployed, either by seeding real registered accounts server-side or by extending this same seeder to run against a backend-enabled account.

  `APP_VERSION`/`SW_VERSION` bumped 0.9.23 → 0.9.24. Verified with a new Playwright suite (22/22): a smoke pass seeding all 5 personas fresh with zero console errors and a non-zero Home dashboard each time, plus a deep pass on garage (heaviest nested data) confirming 5 clients list correctly, a real seeded vehicle plate renders in the persona tracker, the pipeline board and 3 invoices populate, and re-running the demo wipes the prior run cleanly (exactly 6 jobs after reseeding, not 12) — plus all six prior suites re-run unchanged (74/74), 0 console errors throughout.

## Team tier (organizations / seats / roles)

- 2026-07-15 · orchestrator · BUILD — Team plan: shared-data orgs with owner/admin/staff roles and Stripe per-seat billing (branch `claude/subscription-pricing-features-t8131t`) · Per direct user instruction — "go with Team tier (orgs/seats/roles) — ask me if you need to verify" — continuing the pricing-tier backlog started with Basic/Pro. One architectural fork genuinely needed a check-in and was asked via `AskUserQuestion` before any code was written: **real org-scoped tables** (add `org_cuid` to all 12 existing resource tables, every query filtered by it) vs. **shared single identity** (one `team_members` mapping table; all of a team's data stays owned by a single underlying `users` row, staff/admins just get their own login credentials that resolve to it). The user picked shared single identity. This decision shaped everything below — it meant the entire feature could be built as one additive resolution layer instead of a rewrite of every existing table and query.
  1. **`sql/schema-core.sql`** — one new table, `team_members(cuid, org_owner_cuid, member_cuid unique, role check(admin|staff), joined_at)`. The org owner is never a row here (implicit: "not in team_members" == "I'm an owner, of at least myself"); `member_cuid` is `unique` so an account can only ever belong to one org at a time; there are no pending-invite rows because invite tokens are stateless (signed, not stored — see below). `users` gained `team_seats integer`, synced from the Stripe subscription's line-item quantity (owner included in the count).
  2. **`lib/teams.js`** (new) — the whole feature's single seam. `resolveDataOwner(sql, callerCuid)` looks up `team_members` and returns the org owner's cuid if the caller is a member, else the caller's own cuid unchanged — this is the one function every data-touching endpoint now calls before running any query. `isAccountOwner()` gates billing to the owner only. `getMembership()` backs the Settings ▸ Team UI and the invite/remove endpoints' role checks. Invite tokens (`signInviteToken`/`verifyInviteToken`) reuse this codebase's existing HMAC-signed-stateless-token convention (same shape as session tokens in `lib/auth.js` and OAuth state in `lib/lineLogin.js`) rather than a new invites table — `base64url(json).base64url(hmac)`, 7-day expiry baked into the payload, verified with the existing `constantTimeEqual` from `lib/lineLogin.js`. Deliberately reuses `SESSION_SECRET` rather than minting a second dedicated secret: a leaked `SESSION_SECRET` already allows full session forgery, a strictly worse outcome than forged invites, so a second required env var wouldn't close any real gap.
  3. **The resolution seam, wired in exactly two places**: `lib/crudHandler.js` (the generic factory behind every one of the 12 existing resource endpoints — clients/jobs/services/invoices/documents/bookings/followups/portfolio/research/packages/progressLogs/settings-adjacent) now calls `resolveDataOwner(sql, session.userCuid)` immediately after authenticating, before any query runs — so a staff/admin login transparently reads and writes the owner's data with zero per-table changes. `api/settings.js` is hand-rolled (not on the generic factory) and got the same one-line change directly; fixing it surfaced a **pre-existing bug** — it was missing the `canWrite()` plan-lock gate that every other resource endpoint already enforces, silently allowing writes on a locked/expired account. Fixed as part of this pass (GET stays open, PUT/DELETE now gated).
  4. **`api/team-invite.js`** (new, POST) — owner or admin only; validates `role` is `admin`/`staff`, confirms the org is actually on the Team plan, checks live seat capacity (`team_seats` vs. current member count + 1 for the owner) before minting a token, returns a shareable `login.html?teamInvite=<token>` link. No email-sending subsystem exists in this app, so redemption is manual link-sharing (LINE, WhatsApp, etc.) by design, not an oversight.
  5. **`api/team-join.js`** (new, POST) — verifies the token, rejects self-invites and re-joins, re-checks seat capacity authoritatively at redemption time (not just at invite-mint time, closing the obvious race), and rejects an invitee who is themselves a Team-plan account owner (joining someone else's org while owning your own doesn't make sense under shared-single-identity). **Known, deliberately unsolved gap**, flagged in the file's own header comment: an invitee's pre-existing solo data becomes unreachable the moment they join (no merge/import tool) — real freelancers with existing clients need this eventually, out of scope for this pass, tracked as a follow-up.
  6. **`api/team-members.js`** (new, GET/DELETE) — GET returns the roster (owner name, caller's own role, member list) to anyone on the team, owner or member alike. DELETE is role-gated: owner can remove anyone but themselves ("cancel the subscription instead"), admin can remove staff only (not other admins, not the owner), staff can't call it. Removing a member just deletes the `team_members` row — `resolveDataOwner()` then naturally falls back to their own cuid on their very next request, handing back their own unchanged solo account rather than destroying anything.
  7. **`api/billing-checkout.js`/`api/billing-portal.js`** — both gated to `isAccountOwner()` only, since a member's data belongs to someone else's subscription. Checkout accepts a `seats` integer (≥2, owner included) for the `team` plan, sets Stripe line-item `quantity` accordingly; `STRIPE_PRICE_TEAM` joins `STRIPE_PRICE_BASIC`/`STRIPE_PRICE_PRO` as a required env var. Seat-count *changes* after the fact deliberately route through Stripe's own Billing Portal (enabling "customers can update quantities" in the Stripe Dashboard's Customer Portal settings — a one-time manual config step, not app code) rather than a bespoke in-app seat-editor, since `api/stripe-webhook.js` already re-syncs `team_seats` from the subscription's line-item quantity on every `customer.subscription.updated` event regardless of which side initiated the change.
  8. **Client (`app/dataClient.js`, `app/app.js`, `app/index.html`)** — new Settings ▸ Team section (`renderTeamSection()`), shown once entitled (Team plan or already a member): owner/self card with seat usage ("N of M seats used"), member roster with role-gated remove buttons, invite-staff/invite-admin buttons (admin invite is owner-only), and a copyable invite-link panel reusing the same `urlRow`-style markup and `copyLineUrl()` helper as the LINE settings section. The Subscription section gained a "Upgrade to Team" button (prompts for seat count, minimum 2) and, for a non-owner team member, drops all billing controls entirely and shows "part of {owner}'s team" instead — billing is the owner's business, not theirs. `login.html`/`bootLogin()` picks up `?teamInvite=<token>` into `sessionStorage` (survives whichever auth path — login, register, or LINE — the invitee takes) and `finishAppBoot()` redeems it once a real (non-guest) backend-enabled account exists, bridging through the existing `enableCloudBackup()` flow if the invitee was still local-only.
  9. **~25 new i18n keys**, English + Thai (Thai is this app's default language), covering the whole Team section, invite/remove confirmations, and the seat-prompt/validation copy.

  **Deliberately out of scope**: real per-row org scoping (rejected in favor of shared-single-identity, see above); merging an invitee's pre-existing solo data into the org on join (`api/team-join.js` header comment); an in-app seat-quantity editor (Stripe Billing Portal's native control does this instead); email delivery of invite links (no email subsystem exists in this app at all, not just for teams).

## First live-deployment smoke test: two real bugs found, plus LINE billing gap closed

- 2026-07-16 · orchestrator · FIX + BUILD — first-ever live smoke test against a real Vercel deployment (branch `claude/subscription-pricing-features-t8131t`), walking through Neon connection, schema migration, and Stripe test-mode setup interactively with the user. This is the first pass all session with genuine live infrastructure to test against — every prior "not verified against a live deployment" caveat in this file was exactly this gap. Two real, previously-invisible bugs surfaced immediately, plus one gap the user asked to close on the spot.

  **Bug 1 — Stripe endpoints crashed on Vercel's Edge Runtime.** `api/billing-checkout.js`, `api/billing-portal.js`, and `api/stripe-webhook.js` all import `lib/stripe.js`, which imports the `stripe` npm package — its Node SDK relies on Node core modules Edge Runtime doesn't support. Importing it there crashes at module-load time, before the handler function ever runs, producing a bare platform `500` with **zero outgoing requests logged** — not this codebase's own try/catch's `502` JSON body. Live-tested: clicking "Upgrade to Pro" hit exactly this. Every other `api/*.js` endpoint stays safely on `edge` because none of them import `lib/stripe.js` — only `lib/db.js` (the HTTP-based Neon driver, edge-safe) and Web Crypto (edge-safe). **Fix**: switched those 3 files' `config.runtime` from `'edge'` to `'nodejs'` — Vercel's own documented fix for using the full Stripe SDK. No handler logic changed; all three already use the Web-standard `Request`/`Response` handler shape, which Vercel's Node.js runtime supports identically, so `stripe-webhook.js`'s raw-body signature verification (`request.text()`) is unaffected. **Why no prior test caught this**: `test-stripe-webhook.mjs` runs in plain local Node.js, never Vercel's actual Edge Runtime — this whole class of bug (a runtime/bundler incompatibility) is only observable against a real deployment, which no pass before this one had access to.

  **Bug 2 — `enableCloudBackup()` had no guard for LINE-authenticated accounts, and would 400.** Live-tested: logging in via "Continue with LINE" and clicking Settings ▸ "Enable cloud backup" failed with `400 Missing password hash fields`. Root cause: LINE accounts are `salt: null, hash: null, iters: null` by design (no password at all — `app/app.js`'s `handleLineLoginRedirect()`), but `enableCloudBackup()` unconditionally sent those null fields to `api/auth-register.js`, which requires them. The bug had a sibling that was already correctly guarded: `maybeShowCloudBackupModal()` (the auto-popup) already skipped LINE accounts entirely with a comment explaining exactly why an unconditional "Enable" button would be "guaranteed to fail" — but that guard only covered the popup, not the manual Settings button, which is what the user actually clicked.

  **The user then asked directly: does Stripe billing even work for LINE-login users at all?** It didn't — LINE login (`api/line-login-callback.js`) has always been deliberately local-only, "no server-side user record to create, matching the app's local-first model" (its own header comment, written before subscriptions existed). With no server account, there's nothing for billing to attach to, and no fix to bug 2 alone could close that — a LINE user had **no path to a paid plan whatsoever**. Asked the user whether to fix this now or backlog it; they chose fix now.

  **The fix, built this pass**: rather than force a second OAuth round trip every time a LINE user wants to enable cloud backup (bad UX, and unnecessary — `api/line-login-callback.js` already does the one real verification against LINE), that same callback now also mints a signed, long-lived (365-day) proof of the identity it just verified — `signLineIdentity()`/`verifyLineIdentity()`, new in `lib/lineLogin.js`, same HMAC-signed-stateless-token shape as this file's existing `state` param and every other signed token in this codebase, reusing `LINE_LOGIN_STATE_SECRET` rather than minting a new env var (same "an existing secret already covers this risk" call this session already made for team invite tokens). The client (`handleLineLoginRedirect()`) stores this token on the local account record, refreshing it on every re-login so an account created before this shipped self-heals the next time its owner logs in via LINE. A new endpoint, `api/auth-register-line.js`, verifies that token and creates-or-looks-up a `users` row keyed by `line_sub` (a column that already existed in the schema but nothing wrote to until now) — unlike `api/auth-register.js`, it's safe to treat "row already exists" as a normal re-login rather than a 409, since the signed token already proves identity as strongly as a password would; no separate login step is needed. `enableCloudBackup()` now branches: password accounts keep the exact existing flow, LINE accounts call the new `SidekickBackend.registerLine()`, and a LINE account with no stored token yet (the pre-fix edge case) gets a clear "log out and back in with LINE" toast instead of a silent failure. `maybeShowCloudBackupModal()`'s guard was loosened to match — it now shows the popup for any LINE account that DOES have a usable token, not none of them.

  **Also fixed, unrelated but found in the same conversation**: the registration form's field order in `login.html` read Email → Password → Name → Confirm-password (Name and Confirm were just made visible in place on the Register tab, not reordered) — reordered to Email → Name → Password → Confirm-password, grouping the two password fields together as any normal signup form would.

  `APP_VERSION`/`SW_VERSION` bumped 0.9.25 → 0.9.26. Verified with a new Node harness for `lib/lineLogin.js`'s new token functions (10/10 — valid roundtrip, empty-field normalization, wrong-secret/tampered/malformed/null rejection, missing-`sub` rejection, 365-day expiry boundary in both directions) and a new Playwright suite simulating the full LINE-redirect-to-cloud-backup flow (8/8 — a fresh LINE login stores the right account shape and token, `enableCloudBackup()` correctly routes to `registerLine()` and never touches the password-based `register()` for a LINE account, and a token-less LINE account gets the guidance toast instead of a crash) — plus the full existing regression suite re-run unchanged (Node: entitlements 21/21, Stripe webhook signature 4/4, LINE multi-tenant 11/11, teams 16/16; Playwright: onboarding 12/12 + 4/4, subscription UI 10/10, Phase 1 gates 13/13, LINE settings 13/13, booking page 8/8, demo data 22/22, team UI 22/22), 0 console errors throughout. The Stripe-runtime fix itself could only be confirmed against the live deployment (Vercel logs showing "0 outgoing requests" for the crash, matched to the exact import path) — the user's own next click-through is what will confirm it end-to-end; the LINE-billing fix is unit/integration-tested locally but, like everything else in this session, not yet exercised against a live LINE Login channel + Stripe test-mode account together.

  A genuine UI bug surfaced and fixed during testing, unrelated to the architecture: the new invite-staff/invite-admin buttons were missing `style="width:100%"`, so `.qc-btn`'s default fixed 30×30px icon-button sizing (meant for small square icon buttons elsewhere, e.g. the roster's "✕" remove button) clipped them to unreadable, effectively unusable squares — caught because Playwright's real synthetic clicks (unlike a raw DOM `.click()`) hit-test by screen coordinate and kept landing on the wrong control. Fixed by matching the same `style="width:100%"` pattern every other full-width settings button in this app already uses.

  `APP_VERSION`/`SW_VERSION` bumped 0.9.24 → 0.9.25. Verified with a new Node harness for `lib/teams.js` (16/16 — resolution for member/solo/unknown cuids, owner-vs-member checks, invite token sign/verify/tamper/wrong-secret/forged-role/7-day-expiry) and a new Playwright suite for the Settings ▸ Team UI and `?teamInvite=` redemption flow (22/22 — locked hint on Pro with no team, owner seat usage + full roster + both invite buttons, invite-link rendering, member removal, admin's narrower invite/remove permissions, staff's fully read-only view, the seat-count checkout prompt and its ≥2 validation, and `maybeRedeemTeamInvite()`'s token consumption) — plus every prior suite re-run unchanged (Node: entitlements 21/21, Stripe webhook signature 4/4, LINE multi-tenant 11/11; Playwright: onboarding 12/12 + 4/4, subscription UI 10/10, Phase 1 gates 13/13, LINE settings 13/13, booking page 8/8, demo data 22/22), 0 console errors throughout. As with every feature this session, none of this was exercised against a live database, Stripe account, or deployed app — this session has no `DATABASE_URL` or live Stripe/LINE credentials, so the schema migration, `STRIPE_PRICE_TEAM` env var, and the Stripe Dashboard "update quantities" toggle all remain manual pre-deploy steps, same as every other pricing-tier change this session.

## Dated steps, stage-gate booking, repeat steps, and pipeline timeline (4-layer orchestrated build)

- 2026-07-16 · orchestrator · BUILD — appointment-aware pipeline: dated sub-tasks (exact / due-by), stage-gate booking prompt, repeatable steps, timeline/Gantt view, calendar-booking links (branch `claude/subscription-pricing-features-t8131t`) · Per direct user request framed on the insurance-broker use case (inquiry 01 Jun → health check-up 07 Jun as a dated sub-task → hear result 14 Jun → insurer premium quote "within 30 Jun" as a deadline-type step → quotation to client with deadline), with three named challenges: two appointment-date types (exact vs within-deadline), "cannot just ship and move card" (a forward stage move must prompt for the next appointment), and a Gantt/timeline view for users who think in timelines. Built via an explicit 4-layer multi-agent orchestration the user requested: Layer 1 two parallel code-assess agents (exact line-ref maps of pipeline/sub-task/booking/mirror internals), Layer 2 two design lenses (user-flow correctness vs minimal-diff) merged by a synthesis agent — which caught a real design-breaking fact (stage advances happen inline at 5 separate commit points, `advanceJobStage`/`cashJobPath`/`markJobPaid`/`onEngagementInvoiceCreated`/`onEngagementQuoteCreated`, not via one shared function; a 3-hook design would have silently missed the quote/invoice/paid paths) — Layer 3 two sequential implementation agents (same-file conflict avoidance in the one big app.js), Layer 4 parallel verify (durable acceptance-test writer + correctness reviewer + conventions reviewer).
  1. **Data model (no new stores, no DB_VER bump)**: subTask items gain `dateType('exact'|'by')/date/startTime/bookingCuid/stage/repeatOfId` — falsy `dateType` = legacy undated row, rendered byte-identically (verified in tests), no migration pass. Jobs gain one field, `pendingGateStage`, and `saveJob`'s edit-preserve block keeps it across form re-saves. Bookings gain `jobCuid` (nullable link to the originating job) — carried through the mirror end to end: `dataClient.js` bookingsMirror mapping + `api/app-bookings.js` FIELDS + idempotent `alter table app_bookings add column if not exists job_cuid` in `sql/schema-core.sql` (one more manual live-DB re-run of the schema file needed at deploy time, same standing convention).
  2. **Stage-gate ("never a silent move")**: all 5 forward-commit points set `pendingGateStage` in the same `dbPut` as the move (crash-safe: reloading mid-prompt can't lose it), then open a shared appointment modal — step name, exact-vs-deadline segmented toggle, date (+time for exact), or an explicit "no appointment needed" resolution (logged via the existing `logEvent`). Gate mode locks overlay-dismissal — the only exits are booking or explicitly declining. A pending card shows an amber "Book next step" banner and its advance action re-opens the prompt instead of advancing (re-entry guard in `pipelineAction`). Backward moves (`moveJobStageBack`) and stage-finish never gate and clear the flag; terminal advance clears it too.
  3. **Booking link**: an exact-date step writes a real `bookings` store row (status `scheduled`, titled "step — client", linked `jobCuid`, back-linked `bookingCuid` on the step) through the store directly — `saveBooking` is IIFE-private and DOM-coupled, so the create path mirrors its record shape instead of calling it — and it appears on the existing booking calendar with zero renderer changes. Form edits preserve `jobCuid`; form-created bookings get `null`. Deliberately create-time-link-only: no render-time reconciliation or cascade-delete (no requirement needed it; dangling refs are harmless).
  4. **Repeat steps**: dated rows get a `↻` that opens the same modal in repeat mode (name/type prefilled, date empty) — one tap + one date = a new occurrence carrying `repeatOfId`, source row untouched. Matches the "customer needs a second check-up" reality of the broker flow.
  5. **Timeline/Gantt**: Board/Timeline segmented toggle on the Pipeline screen (persisted as `plViewMode` via the existing settings path, mirroring `calViewMode`); `renderPipelineTimeline()` renders active jobs with dated steps as rows on a 28px/day axis (data-range ±3d, clamped to include today), exact steps as dots (done → dimmed, overdue → danger), due-by steps as hatched bars running today→deadline with a hard right border at the deadline (a missed deadline collapses to a danger flag ⚑ at the missed date), a full-height today rule, sticky row labels opening the job editor, and all horizontal scrolling confined to the chart's own `.tl-scroll` container (asserted at a 320px viewport: `body.scrollWidth === clientWidth`). Pure CSS/JS on existing color tokens, light/dark safe, no libraries. One real bug was found and fixed by the implementer's own smoke test: boot renders the pipeline while its screen is `display:none`, where `scrollLeft` assignment silently no-ops — scroll-to-today now waits for the first *visible* render and preserves the user's scroll position on re-renders after that.
  6. **i18n**: 26 new keys (all `appt_*`, `pl_view_*`, `tl_*`) in both dicts, natural Thai (default language) written to match neighboring tone.
  7. **Quotation-with-deadline**: covered by the deadline-type step (the gate fires on the Quote-stage advance, where "send quotation by {date}" is exactly a 'by' step) plus the existing quote-doc flow — no separate quote-deadline field added; noted as the deliberate reading of that requirement.

  `APP_VERSION`/`SW_VERSION` bumped 0.9.26 → 0.9.27. Verified during implementation with two Playwright smoke suites (27/27 gate/booking-link/compat + 30/30 repeat/timeline/geometry, both re-run green after the second chunk); Layer 4's consolidated 20-point acceptance suite + two adversarial review agents ran against the diff, with confirmed findings applied as a follow-up commit (see the next delta entry if any were found). Not exercised live: the mirrored `job_cuid` against a real Neon database (needs the schema re-run at deploy time, as flagged above).

  **Layer 4 completion note (2026-07-16, follow-up)**: the three verify agents hit the session's usage limit mid-run, so the orchestrator completed Layer 4 inline. The half-written consolidated acceptance suite (`check-scheduling.js`) had one test-infra bug — its in-page `__mkJob` helper died on `page.reload()` (window props don't survive navigation); fixed by making helper installation re-runnable after each reload — after which **all 67/67 acceptance assertions covering the full 20-point checklist pass, 0 console errors**. Inline adversarial review confirmed: every user-input render path escaped (`attrEsc` on title/aria/value attributes incl. timeline tooltips and modal prefill, `htmlEsc` on chip/label text, `cuid()`-generated ids in onclick literals), all 26 i18n keys present in exactly both dicts, version lockstep clean with no stale 0.9.26 references, the `job_cuid` triple spelled identically across dataClient.js/api/app-bookings.js/schema-core.sql with an idempotent alter, and the gate flag's full lifecycle (set at 5 forward points, cleared on resolve/decline/back/finish/terminal, preserved through `saveJob` re-saves) verified against the committed code. Full regression re-run green: Playwright 112/112 across 9 prior suites + Node harnesses 62/62 across 5. **No app-code findings — no follow-up code commit needed.**

## Options compared (multi-property deals) + Mark-as-lost + edit-save data-loss fix

- 2026-07-16 · orchestrator · BUILD + FIX — per the realestate-agent gap assessment (client weighing many buildings, quote back-and-forth, deals that just die) and the user's direct proposal "add building lists inside pitch card" (branch `claude/subscription-pricing-features-t8131t`).
  1. **`job.options[]`** — persona-generic "options compared" list on the job (labeled **"Buildings"/"อสังหาฯ ที่ดู"** for the realestate persona, generic "Options compared" elsewhere — same reasoning as the persona tracker system: an insurance broker comparing insurers' quotes or a garage comparing repair approaches is the same shape, so one feature, not five). Lives on the JOB deliberately (deal-scoped — the same client can run a fresh search next year), not the client record, whose realestate `deals[]` tracker remains the long-term relationship view. Each option: editable name, a 7-state status select (considering / viewing booked / interested / passed / quoted / chosen / dropped), 📅 book-viewing, ✕ delete. Picking **'chosen'** is the one status with mechanics: every other still-live option flips to 'dropped' (choosing one IS dropping the rest), with a toast. Rendered in the job edit modal above Sub-tasks; the pipeline card face gets a compact "{n} buildings · {m} interested" line whenever options exist. Rides the jobs JSONB mirror — zero schema change.
  2. **📅 Book viewing** opens the shared appointment modal (new `ctx.prefillText` + `ctx.optionId` support) prefilled "Book viewing · {name}" — an exact date there becomes a dated sub-task + real calendar booking via the whole existing scheduling machinery, and the option flips considering→'viewing' **in the same write** (never clobbering an already-recorded interested/passed verdict on re-booking).
  3. **Mark as lost** — the missing failure exit flagged in the same assessment: a quiet danger-toned "Lost" button on every live pipeline card (confirm-gated) sets `complete + outcome:'lost'`, **keeps the stage it died at** (so Insights can see WHERE deals die via the `pipeline_stage:lost` event), keeps every sub-task/quote/option for history, clears any pending appointment gate, and is reopenable with the existing ← button (which already clears outcome). Lost cards show an ✗ badge (vs ✓ for extended/finished); complete jobs were already excluded from active counts and the Timeline. **`jobDelivered()` fix rode along**: its blanket "complete ⇒ delivered" shortcut would have made a lost pitch-stage package job silently burn package sessions (`packageUsed()`) — a lost job now only counts as delivered if its stage genuinely reached Delivery.
  4. **Real pre-existing data-loss bug found and fixed while wiring the preserve path**: `saveJob`'s edit branch rebuilt the record from the form and `dbPut` REPLACES the stored object — it preserved stage/invoice/quote links but **not `subTasks`, `milestones`, `timeEntries`, `timerStartedAt`, or `outcome`**, so an ordinary detail edit (including the pipeline card's own "Reschedule" button) silently wiped every sub-task (now including dated steps + their booking links), all milestones, logged time, a running timer, and a completed engagement's outcome. Now all six (plus the new `options`) are carried forward on edit. This existed since the tracking features shipped; the new dated-steps feature made it far more likely to bite (Reschedule → edit → save is now a suggested flow).
  5. 20 new i18n keys in both EN and TH dicts (incl. the `_re` realestate label variants); ~6 lines CSS on existing tokens.

  **Deliberately out of scope**: per-option quote-document linking (a quoted option just carries the status; the job-level quoteDocId flow is unchanged); bridging the client-record `deals[]`/viewing-log into options (they remain parallel views — flagged in the assessment as a later unification); seeding demo-data options; quote revisions without stage advance (assessment item 2, still open).

  `APP_VERSION`/`SW_VERSION` bumped 0.9.27 → 0.9.28. Verified with a new Playwright suite (28/28 — add/rename/status persistence, chosen auto-drop, book-viewing prefill + booking row + same-write status flip + verdict-preservation on re-book, card chip counts, options AND sub-tasks surviving a detail edit save (the §4 regression), lost set/keep-stage/clear-gate/✗-badge/cancel-leaves-live, lost package job burning zero sessions, lost jobs absent from the Timeline, ← reopen, realestate label swap, legacy-job compat) — plus the full regression battery re-run green: scheduling acceptance 67/67, 9 prior Playwright suites 112/112, 5 Node harnesses 62/62, 0 console errors throughout.

## Blocker/backlog clearance pass (from the 2026-07-16 product re-assessment)

- 2026-07-16 · orchestrator · BUILD + FIX — per direct user instruction "Run orchestration to clear blockers, backlog" after the orchestrated product re-assessment. Orchestration note: two assess agents mapped every code path (booking flow, backup cross-references, invoice status transitions, gate hooks — exact line refs); the two parallel implementers then both died mid-run on the session usage limit (second time today), leaving only `api/booking-requests.js` + `lib/rateLimit.js` complete — the orchestrator finished every remaining item inline from the maps. The user-only blockers (Neon schema run, Stripe Dashboard config, Vercel env vars, LINE Login channel, live smoke) remain the user's checklist; everything buildable shipped here:
  1. **Booking-confirm path (re-assessment blocker #5)** — `api/booking-requests.js` (new): GET lists pending public requests, POST confirms (atomic winner-takes-the-slot conditional UPDATE — two 'requested' bookings racing for one slot: first confirm books it, second gets 409 `slot_taken`) or declines (releases a held slot, never un-books a booked one), with best-effort LINE push to the client on confirm and `resolveDataOwner`/`canWrite` gating. Client side: `bookingRequestsList`/`bookingRequestResolve` in dataClient + a Confirm/Decline requests list in Settings ▸ LINE booking (with hold-expired hints). Before this, every public booking self-destructed when its 15-minute hold lapsed — 'confirmed'/'booked' were unreachable states.
  2. **Restore id-remap (blocker #6)** — `importBackup` now inserts referenced stores first (clients→services→invoices→documents→packages→dependents), records oldId→newId maps, and rewrites every id-based cross-reference on the way in (jobs.clientId/serviceId/invoiceId/quoteDocId/packageId, milestones[].invoiceId, timeEntries[].invoiceId, bookings.customerId, invoices.clientId, documents.clientId/invoiceId, packages/progressLogs.clientId, and followups' id-embedding key strings). Unmappable refs are nulled and counted ("(n broken links were reset)" toast) rather than left pointing at foreign rows. Cuid links pass through untouched. Previously the only device-migration path corrupted every relationship.
  3. **Server-side entitlement enforcement (blocker #7)** — `api/booking-slots.js` + `api/line-channel-connect.js` now resolve the data owner and gate writes with `canWrite` (402) + `hasFeature('lineBooking')` (403 `plan`); channel DELETE is deliberately auth-only (a locked account must always be able to disconnect its credential). `lib/crudHandler.js` gained a generic `beforeCreate` hook, used only by `api/clients.js` to enforce the Basic client cap server-side (402 `client_cap`; updates/deletes never capped — an over-cap downgraded account keeps what it has). These were client-side-only checks, bypassable with a bare fetch.
  4. **Stripe redirect fix (blocker #9)** — new `appUrl()` in lib/cors.js maps the bare GitHub-Pages origin to APP_ORIGIN (which carries the /Sidekickz project path); billing-checkout success/cancel + billing-portal return URLs now land on the app instead of 404ing at the domain root.
  5. **Honest Team copy (blocker #8, short-term)** — team_sub/team_needs_plan_hint/team_remove_confirm (EN+TH+index.html fallback) no longer promise a shared in-app VIEW that isn't built; the real read-cutover stays P2.
  6. **Honest "Earned this month" (P1)** — new `jobEarned()` predicate (stage reached 'paid' in the job's own order); Home hero, its stat row, and the goal card all now exclude inquiry-stage and lost deals they previously counted.
  7. **Invoice-paid reverse hook (P1)** — invoices.js fires `onInvoiceMarkedPaid` on any status TRANSITION to 'paid' (detail select or edit save); app.js advances the linked job through its 'paid' stage (narrow: linked+at-paid+not-complete+not-package-linked; markJobPaid's own inline invoice flip can't loop it). Users no longer record the same payment twice.
  8. **Dated-step reschedule + delete cleanup (P1)** — ✎ on dated steps opens the appointment modal in a new `edit` mode (everything prefilled incl. date/time); save mutates the step in place and reconciles its calendar booking in the same write (move it, delete it on exact→by, create it on by→exact). `deleteSubTask` now deletes the linked booking (`deleteBookingByCuid`) instead of orphaning it. The sub-task workflow assessment's #1 and #3 gaps.
  9. **Stage-gate opt-out (P1)** — Settings ▸ Manage checkbox (default ON, stored inverted as `stageGateOff` so legacy accounts stay gated); `gateAfterForwardMove` short-circuits when off.
  10. **Rate limiting (P1)** — `lib/rateLimit.js` fixed-window in-memory limiter (honest per-isolate-only limitation documented) applied to auth-login (10/min), auth-register + auth-register-line (5/min), booking-request (10/min — the hold-every-slot DoS), team-join (10/min).
  11. **Test battery committed to the repo (P1)** — tests/ now holds all 16 prior suites + run-all.sh + README (separate commit), plus this pass's three new suites.

  `APP_VERSION`/`SW_VERSION` bumped 0.9.28 → 0.9.29. Verified: new `tests/test-booking-confirm.mjs` (17/17 — confirm/decline state machine incl. the slot race, team resolution, locked-402), `tests/test-ratelimit.mjs` (12/12 — window mechanics, per-IP/per-key isolation, XFF parsing), `tests/check-blockers-p1.js` (22/22 — restore remap roundtrip incl. followup key rewrite, earned-this-month exactness (฿500 from a seeded paid+delivery/pitch/lost mix), reverse hook advancing paid→delivery + gating, ✎ reschedule moving the linked booking + delete cleanup, gate toggle off/on, booking-requests UI against the stubbed contract) — plus the FULL battery re-run green: Node 91/91 across 7 harnesses, Playwright 229/229 across 12 suites, 0 console errors. Still not live-exercised: everything on the user-only checklist above.

## Tiered-model orchestration pass (haiku → sonnet → opus → fable)

- 2026-07-16 · orchestrator · BUILD — per direct user instruction to run a multi-tier model orchestration over the remaining buildable backlog (skipping user-side blockers): Fable as architect (verified every item against live code first — two backlog items were ALREADY done and dropped: the stage-label rename and deploy workflow_dispatch), five Haiku L1 workers running full engineering loops on precisely-anchored small tasks (two chains: api-group ∥ app-group, sequential inside the app chain to avoid app.js collisions), two Sonnet L2 QC engineers with fix authority verifying every claim against the files and re-running targeted suites, one Opus L3 adversarial reviewer over the combined diff (returned ZERO findings — the L2 gate held), Fable final approval with the full battery. 8 agents, ~479k tokens, no session-limit deaths this time.
  1. **Stripe webhook out-of-order guard (P2)** — reconciliation, not ordering bookkeeping: created/updated events now fetch the CURRENT subscription from Stripe and apply that authoritative state; deleted events use the payload (already terminal); fetch failure falls back to the old behavior. No schema change needed.
  2. **Email sanity gate (P3)** — billing-checkout only passes username to Stripe as email when it matches a minimal real-shape regex ('a@b' junk no longer reaches receipts).
  3. **Mirror parity for in-place job writes (P1)** — new `mirrorJob()` helper; all 25 non-saveJob `dbPut('jobs',…)` sites (stage moves, gate resolution, sub-tasks/options/milestones/timers, lost/paid/finish paths) now fire-and-forget mirror, plus the invoices detail-status change. Closes the recorded "server copy of a job goes stale until the next full form save" gap — the parity work Team read-cutover needs anyway.
  4. **Home quick access (P2)** — Invoices/Documents buttons under the hero card (they took 3–4 taps in a "get paid" product); "Tax defaults (for M2)" dev jargon removed.
  5. **Follow-ups "Copy message" (P2)** — one-tap clipboard copy of a ready-to-send STATIC template per follow-up type (overdue/draft/stale/package), EN+TH, placeholders filled from the candidate's data, clipboard-API-with-textarea-fallback. Deliberately static — AI drafting was dropped by explicit user decision and stays dropped.
  6. **Silent delete failures fixed (P3)** — bookings/portfolio deletes now surface a delete_failed toast and skip the success path instead of pretending.
  Deliberately deferred to dedicated passes (too big for L1 loops): cloud restore API, Team read cutover, Thai docgen localization, guest→account adoption, calendar/slots unification, week view.

  `APP_VERSION`/`SW_VERSION` bumped 0.9.29 → 0.9.30. Verified: L2 re-ran targeted suites during QC (phase1-gates 13/13, blockers-p1 22/22, stripe-webhook 4/4); final-approval full battery green — Node 91/91 across 7 harnesses, Playwright 229/229 across 12 suites, 0 console errors.

## Squad campaign: the six deferred M/L items (passes A–E)

- 2026-07-17 · orchestrator · BUILD — per direct user instruction to "re-create another squad to handle these until complete": sequential dedicated passes (Sonnet implementers on architect-written anchored specs, per the workflow-dispatch skill's own size-gate — these were exactly the items too big for L1 loops), each committed on green before the next launched so session-limit fleet deaths (one hit mid-Pass-E; completed inline, the established recovery) could never lose more than one pass.
  - **Pass A — cloud restore + Team read cutover as ONE feature** (`709c381`): the architect insight that collapsed two L-items — the server GETs already existed and already resolve team ownership (resolveDataOwner), so both features are one client-side pull/hydrate engine: dataClient.pullAll() (parallel GETs, adjacent reverse maps, per-store failure isolation, expenses deliberately local-only) + importDataset() (the restore remap engine extracted from importBackup; skips stores absent from the payload so a cloud restore can't wipe local-only expenses) + restoreFromCloud() with one button, two labels ("Restore from cloud" / "Load your team's data"). Team settings copy honesty restored.
  - **Pass A.2 — lossless links** (`38a583c`): Pass A's implementer honestly flagged that id-based cross-refs can't survive a real pull (authoring-device autoincrement ids don't travel). Fixed with ref-cuid mirroring across 6 tables (idempotent alters riding the still-pending schema re-run) + two-tier resolution in importDataset (cuid = cross-device identity, oldId map = same-file backups, file path byte-identical). Residuals commented: nested milestone/timeEntry invoiceIds, followups keys.
  - **Pass B — guest→account adoption** (`3e0a265`): one-time per-account offer on first login when guest rows exist; adoption is a same-device uid swap so every id survives with ZERO remapping; account's own settings win on conflict; adopted clients ride migrateUpload when cloud is on.
  - **Pass C — LINE bookings materialize on the freelancer's calendar** (`bc56dbf`): confirming a request now creates a local calendar booking — local-time-correct (tested across a UTC/Bangkok date boundary), idempotent via a local-only lineBookingId marker, LINE-supplied strings never interpolated into markup. Residual: no two-way slot-blocking yet.
  - **Pass D — real hour-grid Week view** (`2edc410`): 7-day × 06:00–22:00 grid, positioned duration-proportional blocks, out-of-window clamping with hints, side-by-side overlaps, tap-to-edit / tap-empty-to-create-prefilled, horizontal scroll confined per repo rule.
  - **Pass E — Thai client-facing documents** (this commit): the Pass-E worker died on the session limit after writing only the EN key block; completed inline. Discovery: invoices.js print output was ALREADY fully localized — the real gap was docgen.js's contract/NDA/quote/receipt bodies, entirely EN-hardcoded. Now: every client-visible string through t() (formal Thai paperwork register — ใบเสนอราคา/ใบเสร็จรับเงิน/สัญญาจ้างบริการ/สัญญาไม่เปิดเผยข้อมูล), documents render in the app's CURRENT language at generation time (per-document language picker = recorded residual), and dates go through a new docDate() helper: Buddhist-Era (พ.ศ. = ค.ศ.+543, '17 ก.ค. 2569') in Thai mode, byte-identical ISO in English mode. User-customized titles kept verbatim; stock titles localize at render time. Residual: the docgen FORM chrome (internal UI) keeps its existing strings — this pass was scoped to the paperwork itself.

  `APP_VERSION`/`SW_VERSION` bumped 0.9.30 → 0.9.31. Verified per pass (cloud-restore 22/22, guest-adoption 12/12, slot-calendar 17/17, week-view 17/17, thai-docs 16/16 — the last incl. BE-year assertions and an EN byte-stability check) and with the full close-out battery: Node 91/91 across 7 harnesses, Playwright 313/313 across 17 suites, 0 console errors. All six deferred items now closed; the user-side deploy checklist (Neon schema re-run — now also carrying the A.2 ref-cuid columns — Stripe/LINE console, env vars, live smoke) remains the launch gate.
- [x] **Token-gated in-app schema migration (kills the wrong-Neon-database trap)** — retro-logged (shipped just after 0.9.31's entry, commit `1a2eb0f`): `api/admin-migrate.js` applies `sql/schema-core.sql` through the SAME `DATABASE_URL` the deployed app reads, so a hand-run `psql` against the wrong attached Neon database (the 2026-07-13 incident) is structurally impossible. Disabled unless `SETUP_TOKEN` is set (404), constant-time header check (403), GET = required-tables/columns status manifest (database NAME only, never the connection string), POST = apply + re-status. `lib/migrate.js` ($-quote-aware statement splitter, error-isolated run loop, manifest), `lib/schemaSql.js` (verbatim embed of the schema file, drift-guarded by `tests/test-schema-sync.mjs` 3/3), `tests/test-migrate.mjs` 17/17, `.env.example` documents the 4-step set-token → status → apply → REMOVE-token flow.
- [x] **Pass UX: task-flow friction revision (0.9.32)** — the user's end-to-end re-assessment asks, verbatim: incomplete-redo, incomplete-cancelled, easier appointments, and "app requires too much input." Four changes in one pass:
  1. **One-tap stage gate** — the after-advance appointment modal now prefills the step name from the arrived stage's action label (`t(STAGE_META[stage].action)`, e.g. "Send quote"), prefills the date to today+7, and defaults the date type per stage: **'exact' only for Pitch** (an inquiry/first meeting is a real calendar appointment), **'by' (deadline) for every other stage** — "send quote"/"send invoice"/"collect payment"/"deliver"/"follow up" are deadline-shaped. A non-pitch gate is now savable with zero typing; everything stays editable. Non-gate modes (add/repeat/edit) unchanged.
  2. **Lost with a why** — `markJobLost()`\'s bare `confirm()` replaced by a small reason-picker overlay (build-on-demand like the appt modal; overlay-click/Cancel back out with NO change): four optional single-select chips (client cancelled / no response / price too high / went with someone else) stored as `j.lostReason`, appended to the ✗ badge on the completed card, carried through `saveJob()`\'s preserve block, and logged as `pipeline_stage:lost:<reason>` for Insights.
  3. **Revise quote/invoice WITHOUT re-advancing** — new `reviseQuoteForJob()`/`reviseInvoiceForJob()` card buttons (shown whenever the linked doc exists on a live card) reuse the exact same docgen/invoice creation flows but set a revise flag that `onEngagementQuoteCreated`/`onEngagementInvoiceCreated` check FIRST: relink the paperwork, stage untouched, no re-gate. Kills the quote back-and-forth round trip (previously every revision = ← back + re-advance + re-answer the gate). Stale-flag safety: flags cleared at the top of `pipelineAction()` and in `openQuoteForJob()`.
  4. **Restore-fidelity fix (found in review, pre-existing)** — `outcome`, `pendingGateStage`, `options`, and the new `lostReason` were never in the jobs cloud-mirror payload: a lost job restored from cloud silently came back as a *successful* completed engagement, and options/buildings lists vanished. Added to `dataClient.js` toPayload/fromJobRow, `api/jobs.js` FIELDS, and `sql/schema-core.sql` (4 idempotent alters; `lib/schemaSql.js` regenerated, drift test green).
  `APP_VERSION`/`SW_VERSION` bumped 0.9.31 → 0.9.32. Verified: new `tests/check-ux-flow.js` 30/30 (gate defaults incl. pitch-exception, lost modal cancel/select/deselect, preserve-on-edit, revise-no-advance for both doc types, normal path still advances+gates), regression scheduling 67/67 + options-lost 28/28 (§8 updated to drive the new lost modal), schema-sync 3/3, full battery re-run at ship time.
- [x] **Demo forked to a dedicated /demo URL; "Try a demo" button removed from login** (user request 2026-07-17) — the sales-pitch demo is no longer an entry on the real login page: new repo-root `demo/index.html` (copied to `<site>/demo/` by `deploy-pages.yml`, alongside the existing `/` and `/gym/` builds) redirects to `login.html?demo=1`, which auto-runs the existing `startDemo()` flow (unchanged: fresh guest + persona picker + seeded dataset, wipe-confirm guard if the device already holds guest data). The demo entry is deliberately a dumb redirect, not a third app build — zero drift, same storage semantics as the removed button. login.html polls the app.js `db` binding before firing (startDemo needs IndexedDB open; the load event races bootLogin's async openDB). `tests/check-demo-data.js` re-pointed at the `?demo=1` entry — 22/22, onboarding 12/12. Note: login.html is precached by the service worker, so existing installs see the button disappear at the next SW version bump (riding the next pass's lockstep bump).
- [x] **Pass M2a: revenue loop part 1 — payment-link channel + payment slips on invoices (0.9.33)** — the first buildable slice of the M2 "get paid faster" milestone:
  1. **'Payment link' channel type** (Tier-0 Stripe-stack reuse) — a fifth `PAYMENT_CHANNEL_TYPES` entry: the freelancer pastes any hosted checkout URL (Stripe payment link, Ko-fi, …) in Settings ▸ Payment channels and it renders on the invoice detail/print channel block as a full-width tappable "Pay now · ฿amount" button. The app never touches the money. Hard safety rail: `safeHttpUrl()` — only `new URL()`-parseable http(s) URLs ever render as a live anchor (attribute-escaped, `rel="noopener noreferrer"`); `javascript:` or garbage falls back to the plain-text card.
  2. **Payment slips** — Thai payments are slip-driven: the client transfers via PromptPay/bank and sends a slip screenshot. The invoice detail modal now has a slip section: attach (file picker → FileReader → canvas downscale to longest-side ≤1200px JPEG q0.8, so IndexedDB rows and the mirror POST stay far under Vercel's ~4.5MB cap), thumbnails with a full-screen tap-to-view viewer, per-slip remove (confirm-guarded), and a one-tap **"Confirm payment received"** button that transitions the invoice to paid through the exact same shared `transitionInvoiceStatus()` path as the status select — including the `onInvoiceMarkedPaid` reverse hook that advances the linked pipeline card. `saveInvoice`'s edit path preserves `slips` (the form has no slip UI, and dbPut replaces). Mirrored end-to-end: `dataClient.js` maps, `api/invoices.js` FIELDS, `slips jsonb` schema alter (idempotent, rides the next `api/admin-migrate` run), `lib/schemaSql.js` regenerated.
  `APP_VERSION`/`SW_VERSION` bumped 0.9.32 → 0.9.33 (also delivers the /demo login-button removal to existing installs, see previous entry). Verified: new `tests/check-payments.js` 24/24 (paylink in settings + rendered anchor attrs + `javascript:` rejection, slip attach/downscale/persist/remove, confirm-paid advancing a linked job past 'paid', reload persistence), full battery re-run at ship time.
- [x] **Pass M2c: T-24h LINE booking reminders (server-side only, no app-version bump)** — hourly Vercel Cron (`vercel.json` crons entry) hits new `api/cron-reminders.js`: confirmed, LINE-originated (`client_line_user_id` set), not-yet-reminded bookings whose slot starts within the next 24h get one Thai-first bilingual LINE push (Asia/Bangkok-formatted date/time, service name, owner name — same en-GB formatting convention as the existing confirm push). Idempotency via new `bookings.reminder_sent_at` (idempotent alter, rides the next admin-migrate run): stamped ONLY after a successful push, so failures retry next hour and nothing ever double-sends; already-started bookings permanently fall out of the `starts_at > now()` bound (no "reminders" for past meetings). Security: same posture as SETUP_TOKEN — endpoint 404s until the user sets `CRON_SECRET` (documented in `.env.example`; Vercel auto-sends it as a Bearer header on cron invocations; unlike SETUP_TOKEN it stays set permanently). Factory-injectable (`createCronRemindersHandler({getSql, push})`, the booking-requests precedent). New `tests/test-cron-reminders.mjs` 21/21; schema-sync 3/3, migrate 17/17, booking-confirm 17/17 all green. User-side to enable: set CRON_SECRET in Vercel + redeploy (cron entries apply on deploy).
- [x] **Pass M2b: shareable client-facing public invoice page (0.9.34) — the revenue loop closes end-to-end** — freelancer taps **"Copy link"** on an invoice → sends it to the client (LINE/anywhere) → client opens `invoice.html?i=<invoiceCuid>` (book.html's self-contained public-page pattern; the 25-char random cuid IS the capability token, same trust model as `book.html?u=`), sees the full invoice (line items, VAT/WHT totals, status, notes, owner name) plus every payment channel — PromptPay EMVCo QR (invoices.js now exposes its pure QR primitives as `window.SidekickPromptPay`, proven to load standalone with zero console errors), "Pay now" payment-link button (http(s)-validated), bank/cash text — pays, and **uploads their transfer slip right on the page** (same ≤1200px JPEG q0.8 downscale pipeline, deliberately duplicated inline since the page is self-contained by design). New `api/invoice-public.js`: GET returns a strict whitelist (never the slip images — only `slipCount`; 404 indistinguishable for missing vs unknown; 30/min rate limit), POST appends the slip server-side (400 bad-image / 404 / 409 `slips_full` at 5 slips cap; 5/min rate limit) and **never changes status** — the freelancer's own one-tap "Confirm payment received" (M2a) stays the auditable confirmation event. Merge-back: opening an invoice in-app fire-and-forget fetches the server copy (`dataClient.invoiceFetchByCuid`) and appends client-uploaded slips the local record lacks (by id, one-directional — a locally-deleted slip never resurrects), re-renders + toasts "{n} new slip from your client". Share button gated on cloud-backup-enabled accounts (a guest/local invoice has no server copy to read).
  `APP_VERSION`/`SW_VERSION` bumped 0.9.33 → 0.9.34. Verified: new `tests/test-invoice-public.mjs` 17/17 (whitelist/leak/validation/cap) + `tests/check-invoice-public.js` 24/24 (QR canvas non-blank, paylink attrs, javascript: rejection, slip POST round-trip, 409 message, share-link copy, zero console errors proving standalone invoices.js load), full battery re-run at ship time. M2 milestone (revenue loop) is now fully shipped: M2a slips/paylink + M2b public page + M2c reminders.
- [x] **Pass M3-L1: unified product/service catalog with stock tracking (0.9.35)** — the product-catalogue assessment's Option 1, built: the Services catalog now holds both kinds. `kind: 'service'|'product'` (missing = 'service' — zero migration, every existing record and seed untouched), plus product-only `sku`, `stockQty` (null = untracked), `cost` (stored for later margin insights). Service modal gains a kind toggle + conditional product fields; the list shows 📦 vs 🏷️, sku sub-line, and a stock chip (amber ≤3, red "Out of stock" at 0). Invoice line-item picker: product options carry a 📦 prefix, tracked-out-of-stock options are disabled, and picker-created lines are stamped with `serviceId` (additive on lineItems — hand-typed lines stay unlinked). **Automatic stock decrement on payment**: shared `decrementStockForInvoicePaid()` wired into all three paid paths (detail-modal/confirm-slip shared transition, edit-form status save, pipeline markJobPaid) — floors at 0, idempotent via an `invoices.stockDecrementedAt` stamp so paid→sent→paid can never double-deduct. Public booking page filters products out server-side (`coalesce(kind,'service') <> 'product'` in api/booking-availability.js — products are invoiceable, not bookable). Mirrored end-to-end (dataClient maps, api/services.js + api/invoices.js FIELDS, idempotent schema alters, schemaSql regenerated). New `tests/check-catalog.js` 32/32; full battery re-run at ship time.
- [x] **Pass M3-L2: engagement items flow through the pipeline (0.9.36)** — products/extra services attach to an engagement while the deal is still forming and ride automatically into the paperwork. New `job.items[]` ({id, serviceId, name, qty, unitPrice} — name/price SNAPSHOTTED at add time so later catalog edits never rewrite history): an "Items on this engagement" section in the job modal (catalog picker with 📦 prefixes + disabled at 0 tracked stock, qty, live-persisted add/remove like the options section, wipe-guarded in saveJob's preserve block), a `🛒 {n} · ฿total` card chip next to the options chip, quote prefill (openQuoteForJob/reviseQuoteForJob append one line per item), and invoice prefill — bare-job path appends lines WITH serviceId; the quote→invoice path never double-appends but defensively re-derives serviceId by matching items to unclaimed prefill lines on name+qty+unitPrice (quote lines never carry serviceId), so M3-L1's paid-time stock decrement works end-to-end from a deal formed at Pitch: gym trainer adds 2 protein packs at pitch → quote → invoice → client pays → stock 5→3, exactly once. Mirrored (dataClient, api/jobs.js FIELDS, `jobs.items jsonb` idempotent alter, schemaSql regenerated). New `tests/check-items.js` 27/27; full battery at ship time.
- [x] **Pass M3-L3: public storefront — the commerce stack completes (0.9.37)** — the freelancer shares one link (`shop.html?u=<cuid>`, Settings ▸ Shop section with copy button); a client browses their PRODUCTS (public `api/shop-public.js` GET: kind='product', in-stock-or-untracked only), picks quantities (stepper clamps at tracked stock, running total, sticky order bar), and submits an order request with name/contact. Server REPRICES every line from the catalog (client-sent prices never trusted; any foreign/unknown cuid rejects the whole request — stored total can never disagree with the catalog), snapshots items+total into the new `order_requests` table (requested/confirmed/declined; rate-limited 5/min; response never echoes PII). In-app: pending orders listed in Settings (name·contact, items summary, total) with Confirm/Decline through new authed `api/order-requests.js` (booking-requests factory pattern: team-resolved owner, canWrite gate, atomic status flip → 409 on a raced double-confirm). **Confirm materializes a normal pipeline engagement with `items[]` pre-attached** (service_cuids resolved to local catalog ids, snapshots kept regardless, idempotence-guarded like LINE booking materialization) — so the entire existing chain runs untouched from there: quote/invoice prefill → share the public invoice link → client pays + uploads slip → one-tap confirm → stock decrement. No payment and no stock holds on the shop page itself (v1 accepts oversell risk; stock only moves at invoice-paid — commented in code). `order_requests` added to the migrate manifest; schemaSql regenerated. Implementer agent died at the session limit after the Node harness — the Playwright suite it had written was completed/fixed inline (a stepper-clamp click needed tolerance for the correctly-disabled + button). `tests/run-all.sh` also fixed to start ALL suite ports (the 5 newest suites crashed in the battery when their ad-hoc servers died — infrastructure, not app). New `tests/test-shop-public.mjs` 28/28 + `tests/check-shop.js` 27/27; full battery 35 suites all green at ship time (Node 177/177 + Playwright 477/477).
